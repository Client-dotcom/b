print("PhantomXPro")

-- تحميل WindUI
local success, result = pcall(function()
    local source = game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua", true)
    if source then
        local loaded = loadstring(source)
        if loaded then
            return loaded()
        end
    end
    return nil
end)

if success and result then
    local WindUI = result
    local Players = game:GetService("Players")
    local Workspace = game:GetService("Workspace")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local player = Players.LocalPlayer

    -- اختيار اللغة في البداية
    local languageChoice = nil
    WindUI:Popup({
        Title = "PhantomXPro - اختر اللغة / Select Language",
        Icon = "globe",
        Content = "الرجاء اختيار لغة السكربت\nPlease select script language",
        Buttons = {
            {
                Title = "العربية",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "Arabic"
                end
            },
            {
                Title = "English",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "English"
                end
            }
        }
    })

    -- الانتظار حتى يختار المستخدم اللغة
    repeat task.wait() until languageChoice ~= nil

    -- نصوص حسب اللغة
    local texts = {
        Arabic = {
            welcomeTitle = "PhantomXPro",
            welcomeContent = "⚠️ تنبيه هام:\nتشغيل السكربت يكون على مسؤوليتك الشخصية والكاملة، ونحن غير مسؤولين عن أي ضرر. مع أننا نعمل على تقديم سكربت بأعلى معايير الأمان الممكنة، هذا تحذير احتراطي في حال حدوث أي مشكلة غير متوقعة.نتمني لكم تجربة ممتعة ❤",
            discordTitle = "🎮 انضم لسيرفر الديسكورد",
            discordContent = "انضم للسيرڤر الرسمي للسكربت!\n• آخر التحديثات\n• مساعدة فنية\n• إقتراحات وتحسينات\n• تحديثات أمان\n\nالرابط: https://discord.gg/CgUa36sPNs",
            okButton = "موافق",
            copyButton = "نسخ الرابط وتفعيل السكربت",
            copiedTitle = "✅ تم نسخ الرابط",
            copiedContent = "تم تفعيل السكربت!\nرابط الديسكورد في الحافظة",
            windowTitle = "PhantomXPro",
            settingsTab = "الإعدادات",
            developerTab = "المطور",
            mainTab = "الرئيسية",
            themeDropdown = "اختر السمة",
            themeValues = {"داكن 🌙", "فاتح ☀️", "أزرق 🔵", "أخضر 💚", "أحمر 🔴"},
            updateButton = "تحديث السكربت",
            updateTitle = "جاري التحديث",
            updateContent = "بيتم إعادة تحميل السكربت...",
            closeButton = "أقفل السكربت",
            closeTitle = "تم الإغلاق",
            closeContent = "تم إغلاق السكربت بنجاح",
            discordButton = "انضم لسيرفر الديسكورد",
            discordCopied = "✅ تم نسخ الرابط",
            discordCopiedContent = "رابط الديسكورد في الحافظة",
            developerName = "Cypher",
            developerDesc = "مطور سكربتات روبلوكس",
            versionButton = "الإصدار 2.1.0",
            contactButton = "تواصل مع المطور",
            contactCopied = "✅ تم النسخ",
            contactContent = "اسم المستخدم: cypherclient",
            finalTitle = "أهلاً وسهلاً في PhantomXPro 🌟",
            finalContent = "السكربت شغّال وجاهز للاستخدام!\nتمتع بمميزاتنا الحصرية 🚀",
            
            -- Main Tab Texts
            espButton = "تشغيل ESP",
            espButtonOff = "إيقاف ESP",
            hitboxButton = "تشغيل Hitbox",
            hitboxButtonOff = "إيقاف Hitbox",
            hitboxSizeInput = "حجم الهيتبوكس",
            flightButton = "تشغيل الطيران",
            flightButtonOff = "إيقاف الطيران",
            targetPlayerDropdown = "اختر عدو",
            killEnemyButton = "قتل العدو",
            espActive = "ESP مفعّل",
            espInactive = "ESP غير مفعّل",
            hitboxActive = "Hitbox مفعّل",
            hitboxInactive = "Hitbox غير مفعّل",
            flightActive = "الطيران مفعّل",
            flightInactive = "الطيران غير مفعّل",
            espCleaned = "تم تنظيف ESP",
            hitboxCleaned = "تم تنظيف Hitbox",
            
            -- Kill Messages
            killingAll = "جاري قتل جميع الأعداء...",
            killingTarget = "جاري قتل الهدف...",
            killedAllSuccess = "✅ تم قتل جميع الأعداء",
            killedTargetSuccess = "✅ تم قتل الهدف",
            noTargetSelected = "❌ لم تختر هدفاً",
            noEnemiesFound = "❌ لا يوجد أعداء",
            selectAllOption = "جميع الأعداء",
            findingEnemies = "جاري البحث عن الأعداء...",
            changeStateButton = "تغيير الحالة",
            getPositionsButton = "جلب المواقع",
            stateChanged = "✅ تم تغيير الحالة",
            gotPositions = "✅ تم جلب المواقع"
        },
        English = {
            welcomeTitle = "PhantomXPro",
            welcomeContent = "⚠️ Important Warning:\nUsing the script is at your own personal and full responsibility, and we are not responsible for any damage. While we strive to provide a script with the highest possible security standards, this is a precautionary warning in case any unexpected problem occurs. We wish you a great experience ❤",
            discordTitle = "🎮 Join Discord Server",
            discordContent = "Join the official script server!\n• Latest updates\n• Technical help\n• Suggestions and improvements\n• Security updates\n\nLink: https://discord.gg/CgUa36sPNs",
            okButton = "OK",
            copyButton = "Copy Link & Activate Script",
            copiedTitle = "✅ Link Copied",
            copiedContent = "Script activated!\nDiscord link in clipboard",
            windowTitle = "PhantomXPro",
            settingsTab = "Settings",
            developerTab = "Developer",
            mainTab = "Main",
            themeDropdown = "Select Theme",
            themeValues = {"Dark 🌙", "Light ☀️", "Blue 🔵", "Green 💚", "Red 🔴"},
            updateButton = "Update Script",
            updateTitle = "Updating",
            updateContent = "Reloading script...",
            closeButton = "Close Script",
            closeTitle = "Closed",
            closeContent = "Script closed successfully",
            discordButton = "Join Discord Server",
            discordCopied = "✅ Link Copied",
            discordCopiedContent = "Discord link in clipboard",
            developerName = "Cypher",
            developerDesc = "Roblox Script Developer",
            versionButton = "Version 2.1.0",
            contactButton = "Contact Developer",
            contactCopied = "✅ Copied",
            contactContent = "Username: cypherclient",
            finalTitle = "Welcome to PhantomXPro 🌟",
            finalContent = "Script is running and ready to use!\nEnjoy our exclusive features 🚀",
            
            -- Main Tab Texts
            espButton = "Enable ESP",
            espButtonOff = "Disable ESP",
            hitboxButton = "Enable Hitbox",
            hitboxButtonOff = "Disable Hitbox",
            hitboxSizeInput = "Hitbox Size",
            flightButton = "Enable Flight",
            flightButtonOff = "Disable Flight",
            targetPlayerDropdown = "Select Enemy",
            killEnemyButton = "Kill Enemy",
            espActive = "ESP Active",
            espInactive = "ESP Inactive",
            hitboxActive = "Hitbox Active",
            hitboxInactive = "Hitbox Inactive",
            flightActive = "Flight Active",
            flightInactive = "Flight Inactive",
            espCleaned = "ESP cleaned",
            hitboxCleaned = "Hitbox cleaned",
            
            -- Kill Messages
            killingAll = "Killing all enemies...",
            killingTarget = "Killing target...",
            killedAllSuccess = "✅ Killed all enemies",
            killedTargetSuccess = "✅ Killed target",
            noTargetSelected = "❌ No target selected",
            noEnemiesFound = "❌ No enemies found",
            selectAllOption = "All Enemies",
            findingEnemies = "Finding enemies...",
            changeStateButton = "Change State",
            getPositionsButton = "Get Positions",
            stateChanged = "✅ State changed",
            gotPositions = "✅ Got positions"
        }
    }

    local t = texts[languageChoice]

    WindUI.TransparencyValue = 0.5
    WindUI:SetTheme("Dark")

    -- نافذة البوب أب الترحيبية
    WindUI:Popup({
        Title = t.welcomeTitle,
        Icon = "crown",
        Content = t.welcomeContent,
        Buttons = {
            {
                Title = t.okButton,
                Icon = "arrow-right",
                Variant = "Primary",
                Callback = function() end
            }
        }
    })

    -- نافذة Discord المنبثقة
    task.wait(3)
    WindUI:Popup({
        Title = t.discordTitle,
        Icon = "message-circle",
        Content = t.discordContent,
        Buttons = {
            {
                Title = t.copyButton,
                Icon = "copy",
                Variant = "Primary",
                Callback = function()
                    setclipboard("https://discord.gg/CgUa36sPNs")
                    WindUI:Notify({
                        Title = t.copiedTitle,
                        Content = t.copiedContent,
                        Duration = 3
                    })
                end
            }
        }
    })

    -- إنشاء النافذة الرئيسية
    local Window = WindUI:CreateWindow({
        Title = t.windowTitle,
        Icon = "crown", 
        Author = "By_Cypher",
        Folder = "PhantomXPro",
        Size = UDim2.fromOffset(550, 400), -- نافذة صغيرة
        Theme = "Dark",
        User = {
            Enabled = true,
            Anonymous = false,
            Callback = function()
                WindUI:Notify({
                    Title = languageChoice == "Arabic" and "معلوماتك" or "Your Info",
                    Content = (languageChoice == "Arabic" and "اسمك: " or "Name: ") .. player.Name,
                    Duration = 2
                })
            end
        },
        SideBarWidth = 180, -- شريط جانبي أصغر
        ScrollBarEnabled = true
    })

    -- الأقسام الرئيسية
    local Sections = {
        Main = Window:Section({ Title = t.mainTab, Opened = true }),
        Settings = Window:Section({ Title = t.settingsTab }),
        Credits = Window:Section({ Title = t.developerTab })
    }

    -- التبويبات
    local Tabs = {
        Main = Sections.Main:Tab({ Title = t.mainTab, Icon = "home" }),
        Config = Sections.Settings:Tab({ Title = t.settingsTab, Icon = "settings" }),
        Developer = Sections.Credits:Tab({ Title = t.developerTab, Icon = "user" })
    }

    -- متغيرات النظام
    local espEnabled = false
    local hitboxEnabled = false
    local flightEnabled = false
    local hitboxSize = 20
    local selectedTarget = t.selectAllOption
    local espUpdateLoop = nil
    local hitboxUpdateLoop = nil

    -- =============================================
    -- نظام الكشف عن الأعداء
    -- =============================================
    local function getPlayerInfo(targetPlayer)
        local info = {}
        local success, attributes = pcall(function()
            return targetPlayer:GetAttributes()
        end)
        
        if success then
            for attrName, attrValue in pairs(attributes) do
                local attrLower = tostring(attrName):lower()
                if attrLower == "team" then 
                    info.team = tostring(attrValue)
                elseif attrLower == "map" then 
                    info.map = tostring(attrValue)
                end
            end
        end
        return info
    end

    local function checkIfEnemy(myInfo, enemyInfo)
        local myTeam = myInfo.team or ""
        local enemyTeam = enemyInfo.team or ""
        local myMap = myInfo.map or ""
        local enemyMap = enemyInfo.map or ""
        
        if myMap ~= "" and enemyMap ~= "" and myMap ~= enemyMap then
            return false
        end
        
        if myTeam ~= "" and enemyTeam ~= "" and myTeam ~= enemyTeam then
            return true
        end
        return false
    end

    local function getAllEnemies()
        local enemies = {}
        local myInfo = getPlayerInfo(player)
        
        for _, otherPlayer in pairs(Players:GetPlayers()) do
            if otherPlayer ~= player then
                local enemyInfo = getPlayerInfo(otherPlayer)
                if checkIfEnemy(myInfo, enemyInfo) then
                    table.insert(enemies, otherPlayer)
                end
            end
        end
        
        return enemies
    end

    -- =============================================
    -- نظام ESP (Toggle)
    -- =============================================
    local function cleanESP()
        for _, obj in pairs(Workspace:GetChildren()) do
            if obj.Name:find("_EnemyChams") or obj.Name:find("_EnemyName") then
                obj:Destroy()
            end
        end
    end

    local function applyESP(enemyPlayer, isEnemy, myTeam, enemyTeam)
        if not enemyPlayer or enemyPlayer == player then return end
        
        local function applyChams(char)
            if not char then return end
            local oldChams = Workspace:FindFirstChild(enemyPlayer.Name .. "_EnemyChams")
            local oldName = Workspace:FindFirstChild(enemyPlayer.Name .. "_EnemyName")
            if oldChams then oldChams:Destroy() end
            if oldName then oldName:Destroy() end
            if not isEnemy then return end
            
            task.wait(0.1)
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            
            local chamColor
            if enemyTeam and enemyTeam:find("Blue") then
                chamColor = Color3.fromRGB(0, 100, 255)
            else
                chamColor = Color3.fromRGB(255, 50, 50)
            end
            
            local highlight = Instance.new("Highlight")
            highlight.Name = enemyPlayer.Name .. "_EnemyChams"
            highlight.Adornee = char
            highlight.FillColor = chamColor
            highlight.FillTransparency = 0.2
            highlight.OutlineColor = chamColor
            highlight.OutlineTransparency = 0
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            highlight.Parent = Workspace
            
            local billboard = Instance.new("BillboardGui")
            billboard.Name = enemyPlayer.Name .. "_EnemyName"
            billboard.Size = UDim2.new(0, 100, 0, 25)
            billboard.StudsOffset = Vector3.new(0, 3, 0)
            billboard.AlwaysOnTop = true
            billboard.Adornee = hrp
            billboard.Parent = Workspace
            
            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.Text = enemyPlayer.Name
            label.TextColor3 = chamColor
            label.TextSize = 11
            label.Font = Enum.Font.Gotham
            label.TextStrokeTransparency = 0.5
            label.Parent = billboard
        end
        
        if enemyPlayer.Character then
            applyChams(enemyPlayer.Character)
        end
        enemyPlayer.CharacterAdded:Connect(function(char)
            applyChams(char)
        end)
    end

    local function updateESP()
        if not espEnabled then return end
        
        local enemies = getAllEnemies()
        if #enemies == 0 then
            cleanESP()
            return
        end
        
        local myInfo = getPlayerInfo(player)
        for _, enemy in pairs(enemies) do
            local enemyInfo = getPlayerInfo(enemy)
            local isEnemy = checkIfEnemy(myInfo, enemyInfo)
            applyESP(enemy, isEnemy, myInfo.team, enemyInfo.team)
        end
    end

    local function startESPLoop()
        if espUpdateLoop then
            espUpdateLoop:Disconnect()
            espUpdateLoop = nil
        end
        
        cleanESP()
        
        if espEnabled then
            updateESP()
            
            espUpdateLoop = game:GetService("RunService").Heartbeat:Connect(function()
                updateESP()
            end)
        end
    end

    -- =============================================
    -- نظام Hitbox (Toggle)
    -- =============================================
    local function getPlayerTeam(targetPlayer)
        local success, attributes = pcall(function()
            return targetPlayer:GetAttributes()
        end)
        
        if success then
            for attrName, attrValue in pairs(attributes) do
                if tostring(attrName):lower() == "team" then
                    return tostring(attrValue)
                end
            end
        end
        
        if Workspace:FindFirstChild("Arenas") then
            for _, arena in pairs(Workspace.Arenas:GetChildren()) do
                if arena:FindFirstChild("Slots") then
                    if arena.Slots:FindFirstChild("Left") then
                        for i = 1, 4 do
                            local slot = arena.Slots.Left:FindFirstChild(tostring(i))
                            if slot and slot:FindFirstChild("Data") and slot.Data:FindFirstChild("Player") then
                                if slot.Data.Player.Value == targetPlayer then
                                    return "Left"
                                end
                            end
                        end
                    end
                    
                    if arena.Slots:FindFirstChild("Right") then
                        for i = 1, 4 do
                            local slot = arena.Slots.Right:FindFirstChild(tostring(i))
                            if slot and slot:FindFirstChild("Data") and slot.Data:FindFirstChild("Player") then
                                if slot.Data.Player.Value == targetPlayer then
                                    return "Right"
                                end
                            end
                        end
                    end
                end
            end
        end
        return nil
    end

    local function resetHRP(char)
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp and hrp:IsA("BasePart") then
                hrp.Size = Vector3.new(2, 1, 1)
                hrp.Transparency = 0
                hrp.CanCollide = true
                hrp.Color = Color3.fromRGB(255, 255, 255)
                hrp.Material = Enum.Material.Plastic
            end
        end
    end

    local function applyHitboxToEnemy(enemyPlayer, enemyTeam)
        if not enemyPlayer.Character then return end
        local hrp = enemyPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not hrp or not hrp:IsA("BasePart") then return end
        
        hrp.CanCollide = false
        hrp.Size = Vector3.new(hitboxSize, hitboxSize, hitboxSize)
        hrp.Transparency = 0.3
        
        if enemyTeam == "Right" or (enemyTeam and enemyTeam:find("Blue")) then
            hrp.Color = Color3.fromRGB(0, 100, 255)
        else
            hrp.Color = Color3.fromRGB(255, 50, 50)
        end
        hrp.Material = Enum.Material.Neon
    end

    local function updateHitboxes()
        if not hitboxEnabled then
            for _, otherPlayer in pairs(Players:GetPlayers()) do
                if otherPlayer ~= player and otherPlayer.Character then
                    resetHRP(otherPlayer.Character)
                end
            end
            return
        end
        
        local enemies = getAllEnemies()
        if #enemies == 0 then
            for _, otherPlayer in pairs(Players:GetPlayers()) do
                if otherPlayer ~= player and otherPlayer.Character then
                    resetHRP(otherPlayer.Character)
                end
            end
            return
        end
        
        local currentMyTeam = getPlayerTeam(player)
        if not currentMyTeam then return end
        
        for _, enemy in pairs(enemies) do
            local enemyTeam = getPlayerTeam(enemy)
            if enemyTeam and enemyTeam ~= currentMyTeam then
                applyHitboxToEnemy(enemy, enemyTeam)
            else
                if enemy.Character then
                    resetHRP(enemy.Character)
                end
            end
        end
    end

    local function startHitboxLoop()
        if hitboxUpdateLoop then
            hitboxUpdateLoop:Disconnect()
            hitboxUpdateLoop = nil
        end
        
        for _, otherPlayer in pairs(Players:GetPlayers()) do
            if otherPlayer ~= player and otherPlayer.Character then
                resetHRP(otherPlayer.Character)
            end
        end
        
        if hitboxEnabled then
            updateHitboxes()
            
            hitboxUpdateLoop = game:GetService("RunService").Heartbeat:Connect(function()
                updateHitboxes()
            end)
        end
    end

    -- =============================================
    -- نظام القتل
    -- =============================================
    local function killWithKnife(targetPlayer)
        pcall(function()
            ReplicatedStorage.KnifeKill:FireServer(targetPlayer, targetPlayer)
        end)
    end

    local function killSelectedTarget()
        if selectedTarget == t.selectAllOption then
            local enemies = getAllEnemies()
            
            if #enemies == 0 then
                WindUI:Notify({
                    Title = "Kill System",
                    Content = t.noEnemiesFound,
                    Duration = 2
                })
                return
            end
            
            WindUI:Notify({
                Title = "Kill System",
                Content = t.killingAll,
                Duration = 2
            })
            
            for _, enemy in pairs(enemies) do
                killWithKnife(enemy)
                task.wait(0.3)
            end
            
            WindUI:Notify({
                Title = "Kill System",
                Content = t.killedAllSuccess .. " (" .. #enemies .. ")",
                Duration = 3
            })
        else
            local target = Players:FindFirstChild(selectedTarget)
            if target then
                WindUI:Notify({
                    Title = "Kill System",
                    Content = t.killingTarget .. ": " .. target.Name,
                    Duration = 2
                })
                
                killWithKnife(target)
                
                WindUI:Notify({
                    Title = "Kill System",
                    Content = t.killedTargetSuccess .. ": " .. target.Name,
                    Duration = 3
                })
            else
                WindUI:Notify({
                    Title = "Kill System",
                    Content = t.noTargetSelected,
                    Duration = 3
                })
            end
        end
    end

    -- =============================================
    -- تبويب الرئيسية (Main)
    -- =============================================
    do
        -- ESP Toggle
        local espToggleHandle
        espToggleHandle = Tabs.Main:Toggle({
            Title = t.espButton,
            Icon = "eye",
            Size = "large",
            Value = false,
            Callback = function(state)
                espEnabled = state
                startESPLoop()
                WindUI:Notify({
                    Title = "ESP",
                    Content = state and t.espActive or t.espInactive,
                    Duration = 2
                })
                if state then
                    espToggleHandle:Set({Title = t.espButtonOff})
                else
                    espToggleHandle:Set({Title = t.espButton})
                end
            end
        })

        -- Hitbox Toggle
        local hitboxToggleHandle
        hitboxToggleHandle = Tabs.Main:Toggle({
            Title = t.hitboxButton,
            Icon = "target",
            Size = "large",
            Value = false,
            Callback = function(state)
                hitboxEnabled = state
                startHitboxLoop()
                WindUI:Notify({
                    Title = "Hitbox",
                    Content = state and t.hitboxActive or t.hitboxInactive,
                    Duration = 2
                })
                if state then
                    hitboxToggleHandle:Set({Title = t.hitboxButtonOff})
                else
                    hitboxToggleHandle:Set({Title = t.hitboxButton})
                end
            end
        })

        -- حجم Hitbox
        Tabs.Main:Input({
            Title = t.hitboxSizeInput,
            Icon = "maximize",
            Size = "large",
            Placeholder = "20",
            Callback = function(value)
                local num = tonumber(value)
                if num and num >= 1 and num <= 50 then
                    hitboxSize = math.floor(num)
                    if hitboxEnabled then
                        updateHitboxes()
                    end
                end
            end
        })

        -- Flight Toggle
        local flightToggleHandle
        flightToggleHandle = Tabs.Main:Toggle({
            Title = t.flightButton,
            Icon = "wind",
            Size = "large",
            Value = false,
            Callback = function(state)
                flightEnabled = state
                if state then
                    loadstring(game:HttpGet("https://pastebin.com/raw/5WLxDDvL"))()
                    flightToggleHandle:Set({Title = t.flightButtonOff})
                else
                    flightToggleHandle:Set({Title = t.flightButton})
                end
                WindUI:Notify({
                    Title = "Flight",
                    Content = state and t.flightActive or t.flightInactive,
                    Duration = 2
                })
            end
        })

        -- اختيار العدو
        local enemyDropdown = Tabs.Main:Dropdown({
            Title = t.targetPlayerDropdown,
            Icon = "users",
            Size = "large",
            Values = {t.selectAllOption},
            Callback = function(value)
                selectedTarget = value
            end
        })

        -- زر قتل العدو
        Tabs.Main:Button({
            Title = t.killEnemyButton,
            Icon = "skull",
            Size = "large",
            Variant = "Destructive",
            Callback = function()
                killSelectedTarget()
            end
        })

        -- زر تغيير الحالة
        Tabs.Main:Button({
            Title = t.changeStateButton,
            Icon = "activity",
            Size = "large",
            Callback = function()
                pcall(function()
                    game:GetService("ReplicatedStorage").SetStatePlr:FireServer("SET")
                    WindUI:Notify({
                        Title = "State",
                        Content = t.stateChanged,
                        Duration = 3
                    })
                end)
            end
        })

        -- زر جلب المواقع
        Tabs.Main:Button({
            Title = t.getPositionsButton,
            Icon = "map-pin",
            Size = "large",
            Callback = function()
                local positions = ""
                local enemies = getAllEnemies()
                
                for _, enemy in pairs(enemies) do
                    if enemy.Character and enemy.Character:FindFirstChild("HumanoidRootPart") then
                        local pos = enemy.Character.HumanoidRootPart.Position
                        positions = positions .. enemy.Name .. ": " .. 
                            math.floor(pos.X) .. ", " .. 
                            math.floor(pos.Y) .. ", " .. 
                            math.floor(pos.Z) .. "\n"
                    end
                end
                
                if positions ~= "" then
                    WindUI:Notify({
                        Title = "Positions",
                        Content = positions,
                        Duration = 5
                    })
                else
                    WindUI:Notify({
                        Title = "Positions",
                        Content = t.noEnemiesFound,
                        Duration = 3
                    })
                end
            end
        })
    end

    -- =============================================
    -- تبويب الإعدادات (Config)
    -- =============================================
    do
        Tabs.Config:Dropdown({
            Title = t.themeDropdown,
            Values = t.themeValues,
            Value = t.themeValues[1],
            Size = "large",
            Callback = function(value)
                local themeMap = {
                    [t.themeValues[1]] = "Dark",
                    [t.themeValues[2]] = "Light", 
                    [t.themeValues[3]] = "Sapphire",
                    [t.themeValues[4]] = "Emerald",
                    [t.themeValues[5]] = "Ruby"
                }
                WindUI:SetTheme(themeMap[value] or "Dark")
            end
        })

        Tabs.Config:Button({
            Title = t.updateButton,
            Icon = "refresh-cw",
            Size = "large",
            Callback = function()
                WindUI:Notify({
                    Title = t.updateTitle,
                    Content = t.updateContent,
                    Duration = 3
                })
                wait(2)
                loadstring(game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/Faq.txt"))()
            end
        })

        Tabs.Config:Button({
            Title = t.closeButton,
            Icon = "power",
            Size = "large",
            Callback = function()
                if espEnabled then cleanESP() end
                if hitboxEnabled then
                    for _, otherPlayer in pairs(Players:GetPlayers()) do
                        if otherPlayer ~= player and otherPlayer.Character then
                            resetHRP(otherPlayer.Character)
                        end
                    end
                end
                Window:Destroy()
                WindUI:Notify({
                    Title = t.closeTitle,
                    Content = t.closeContent,
                    Duration = 3
                })
            end
        })

        Tabs.Config:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "large",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
                WindUI:Notify({
                    Title = t.discordCopied,
                    Content = t.discordCopiedContent,
                    Duration = 3
                })
            end
        })
    end

    -- =============================================
    -- تبويب المطور (Developer)
    -- =============================================
    do
        Tabs.Developer:Paragraph({
            Title = t.developerName,
            Desc = t.developerDesc,
            Image = "user",
            ImageSize = 80,
            Color = Color3.fromHex("#FF6B35")
        })

        Tabs.Developer:Button({
            Title = t.versionButton,
            Icon = "tag",
            Size = "large",
            Callback = function()
                WindUI:Notify({
                    Title = "PhantomXPro v2.1.0",
                    Content = languageChoice == "Arabic" and "أحدث إصدار من السكربت" or "Latest script version",
                    Duration = 3
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "large",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
                WindUI:Notify({
                    Title = t.discordCopied,
                    Content = t.discordCopiedContent,
                    Duration = 3
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.contactButton,
            Icon = "mail",
            Size = "large",
            Callback = function()
                setclipboard("cypherclient")
                WindUI:Notify({
                    Title = t.contactCopied,
                    Content = t.contactContent,
                    Duration = 3
                })
            end
        })
    end

    -- =============================================
    -- تحديث قائمة الأعداء كل 3 ثواني
    -- =============================================
    spawn(function()
        while true do
            task.wait(3)
            
            local enemyList = {t.selectAllOption}
            local enemies = getAllEnemies()
            
            for _, enemy in pairs(enemies) do
                table.insert(enemyList, enemy.Name)
            end
            
            Tabs.Main:Get("Dropdown"):Set({Values = enemyList})
        end
    end)

    -- =============================================
    -- زر تبديل السمة في الشريط العلوي
    -- =============================================
    Window:CreateTopbarButton("theme-switcher", "moon", function()
        WindUI:SetTheme(WindUI:GetCurrentTheme() == "Dark" and "Light" or "Dark")
        WindUI:Notify({
            Title = "Theme Changed",
            Content = "Current theme: "..WindUI:GetCurrentTheme(),
            Duration = 2
        })
    end, 990)

    -- =============================================
    -- إشعار الترحيب النهائي
    -- =============================================
    task.wait(4)
    WindUI:Notify({
        Title = t.finalTitle,
        Content = t.finalContent,
        Duration = 5
    })
else
    print("Failed to load WindUI")
end