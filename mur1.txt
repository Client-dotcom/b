print("PhantomXPro - Fixed Version")

-- تحميل WindUI
local success, result = pcall(function()
    local source = game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua", true)
    if source then
        local loaded = loadstring(source)
        if loaded then
            return loaded()
        end
    end
    return nil
end)

if success and result then
    local WindUI = result
    local Players = game:GetService("Players")
    local Workspace = game:GetService("Workspace")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local player = Players.LocalPlayer

    -- اختيار اللغة
    local languageChoice = nil
    WindUI:Popup({
        Title = "PhantomXPro - اختر اللغة",
        Icon = "globe",
        Content = "الرجاء اختيار لغة السكربت",
        Buttons = {
            {
                Title = "العربية",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "Arabic"
                end
            },
            {
                Title = "English",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "English"
                end
            }
        }
    })

    repeat task.wait() until languageChoice ~= nil

    local texts = {
        Arabic = {
            mainTab = "الرئيسية",
            espOn = "🔴 تشغيل ESP",
            espOff = "🟢 إيقاف ESP",
            hitboxOn = "🔴 تشغيل Hitbox",
            hitboxOff = "🟢 إيقاف Hitbox",
            flightButton = "🛫 الطيران",
            flightActive = "🟢 الطيران مفعل",
            targetSelect = "🎯 اختر عدو",
            killButton = "💀 قتل",
            refreshButton = "🔄 تحديث",
            discordButton = "💬 Discord",
            closeButton = "⏹️ إغلاق",
            
            settingsTab = "الإعدادات",
            themeDropdown = "اختر السمة",
            themeValues = {"داكن 🌙", "فاتح ☀️", "أزرق 🔵", "أخضر 💚", "أحمر 🔴"},
            updateButton = "تحديث السكربت",
            
            developerTab = "المطور",
            developerName = "Cypher",
            developerDesc = "مطور سكربتات روبلوكس",
            versionButton = "الإصدار 2.1.0",
            contactButton = "تواصل مع المطور",
            
            -- إشعارات
            espActivated = "✅ ESP مفعّل على",
            espDeactivated = "❌ ESP موقّف",
            hitboxActivated = "✅ Hitbox مفعّل على",
            hitboxDeactivated = "❌ Hitbox موقّف",
            flightActivated = "✅ الطيران مفعّل",
            noEnemies = "⚠️ لا يوجد أعداء",
            killedAll = "✅ تم قتل",
            killedTarget = "✅ تم قتل الهدف",
            discordCopied = "✅ تم نسخ رابط الديسكورد",
            scriptLoaded = "🎮 PhantomXPro جاهز!",
            scriptClosed = "👋 تم إغلاق السكربت",
            enemiesRefreshed = "🔄 تم تحديث الأعداء"
        },
        English = {
            mainTab = "Main",
            espOn = "🔴 ESP ON",
            espOff = "🟢 ESP OFF",
            hitboxOn = "🔴 Hitbox ON",
            hitboxOff = "🟢 Hitbox OFF",
            flightButton = "🛫 Flight",
            flightActive = "🟢 Flight Active",
            targetSelect = "🎯 Select Enemy",
            killButton = "💀 Kill",
            refreshButton = "🔄 Refresh",
            discordButton = "💬 Discord",
            closeButton = "⏹️ Close",
            
            settingsTab = "Settings",
            themeDropdown = "Select Theme",
            themeValues = {"Dark 🌙", "Light ☀️", "Blue 🔵", "Green 💚", "Red 🔴"},
            updateButton = "Update Script",
            
            developerTab = "Developer",
            developerName = "Cypher",
            developerDesc = "Roblox Script Developer",
            versionButton = "Version 2.1.0",
            contactButton = "Contact Developer",
            
            -- Notifications
            espActivated = "✅ ESP Activated on",
            espDeactivated = "❌ ESP Deactivated",
            hitboxActivated = "✅ Hitbox Activated on",
            hitboxDeactivated = "❌ Hitbox Deactivated",
            flightActivated = "✅ Flight Activated",
            noEnemies = "⚠️ No enemies found",
            killedAll = "✅ Killed",
            killedTarget = "✅ Killed target",
            discordCopied = "✅ Discord link copied",
            scriptLoaded = "🎮 PhantomXPro Ready!",
            scriptClosed = "👋 Script closed",
            enemiesRefreshed = "🔄 Enemies refreshed"
        }
    }

    local t = texts[languageChoice]

    WindUI.TransparencyValue = 0.2
    WindUI:SetTheme("Dark")

    -- إنشاء النافذة
    local Window = WindUI:CreateWindow({
        Title = "PhantomXPro",
        Icon = "shield",
        Author = "By Cypher",
        Folder = "PhantomXPro",
        Size = UDim2.fromOffset(550, 420),
        Theme = "Dark",
        SideBarWidth = 180
    })

    -- الأقسام
    local MainSection = Window:Section({ Title = "PhantomXPro", Opened = true })
    local SettingsSection = Window:Section({ Title = t.settingsTab })
    local DeveloperSection = Window:Section({ Title = t.developerTab })

    -- التبويبات
    local Tabs = {
        Main = MainSection:Tab({ Title = t.mainTab, Icon = "home" }),
        Settings = SettingsSection:Tab({ Title = t.settingsTab, Icon = "settings" }),
        Developer = DeveloperSection:Tab({ Title = t.developerTab, Icon = "user" })
    }

    -- =============================================
    -- نظام كشف الأعداء (معدل)
    -- =============================================
    local function getRealEnemies()
        -- معلوماتي
        local myAttrs = player:GetAttributes()
        local myTeam = nil
        local myMap = nil
        
        for attrName, attrValue in pairs(myAttrs) do
            local nameLower = tostring(attrName):lower()
            if nameLower:find("team") then
                myTeam = tostring(attrValue)
            elseif nameLower:find("map") then
                myMap = tostring(attrValue)
            end
        end
        
        -- البحث عن الأعداء
        local enemies = {}
        
        for _, otherPlayer in pairs(Players:GetPlayers()) do
            if otherPlayer ~= player then
                -- يتحقق إذا اللاعب في الماب (كاراكتر موجود)
                if otherPlayer.Character and otherPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    local otherAttrs = otherPlayer:GetAttributes()
                    local otherTeam = nil
                    local otherMap = nil
                    
                    for attrName, attrValue in pairs(otherAttrs) do
                        local nameLower = tostring(attrName):lower()
                        if nameLower:find("team") then
                            otherTeam = tostring(attrValue)
                        elseif nameLower:find("map") then
                            otherMap = tostring(attrValue)
                        end
                    end
                    
                    -- نفس الماب وفريق مختلف = عدو حقيقي
                    if myMap and otherMap and myMap == otherMap then
                        if myTeam and otherTeam and myTeam ~= otherTeam then
                            table.insert(enemies, otherPlayer)
                        end
                    end
                end
            end
        end
        
        return enemies
    end

    -- =============================================
    -- نظام ESP (معدل - بدون مشاكل)
    -- =============================================
    local espEnabled = false
    local espObjects = {}

    local function cleanESP()
        for _, obj in pairs(espObjects) do
            if obj and obj.Parent then
                obj:Destroy()
            end
        end
        espObjects = {}
    end

    local function createESPForEnemy(enemyPlayer)
        if not enemyPlayer or enemyPlayer == player then return end
        
        local function createESP(char)
            if not char then return end
            
            -- حذف القديم
            for i = #espObjects, 1, -1 do
                local obj = espObjects[i]
                if obj and obj.Name:find(enemyPlayer.Name) then
                    obj:Destroy()
                    table.remove(espObjects, i)
                end
            end
            
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            
            -- Highlight
            local highlight = Instance.new("Highlight")
            highlight.Name = enemyPlayer.Name .. "_ESP"
            highlight.Adornee = char
            highlight.FillColor = Color3.fromRGB(255, 50, 50)
            highlight.FillTransparency = 0.3
            highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
            highlight.OutlineTransparency = 0
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            highlight.Parent = Workspace
            
            -- Name Tag
            local billboard = Instance.new("BillboardGui")
            billboard.Name = enemyPlayer.Name .. "_Name"
            billboard.Size = UDim2.new(0, 120, 0, 30)
            billboard.StudsOffset = Vector3.new(0, 3.5, 0)
            billboard.AlwaysOnTop = true
            billboard.Adornee = hrp
            billboard.Parent = Workspace
            
            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.Text = enemyPlayer.Name
            label.TextColor3 = Color3.fromRGB(255, 100, 100)
            label.TextSize = 14
            label.Font = Enum.Font.GothamBold
            label.TextStrokeTransparency = 0.3
            label.Parent = billboard
            
            -- تخزين
            table.insert(espObjects, highlight)
            table.insert(espObjects, billboard)
        end
        
        -- ESP للكاراكتر الحالي
        if enemyPlayer.Character then
            createESP(enemyPlayer.Character)
        end
    end

    local function activateESP()
        cleanESP()
        
        local enemies = getRealEnemies()
        
        for _, enemy in pairs(enemies) do
            createESPForEnemy(enemy)
        end
        
        espEnabled = true
        return enemies
    end

    local function deactivateESP()
        cleanESP()
        espEnabled = false
    end

    -- =============================================
    -- نظام Hitbox (معدل - يضل شغال)
    -- =============================================
    local hitboxEnabled = false
    local hitboxObjects = {}

    local function applyHitbox(char)
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        
        hrp.CanCollide = false
        hrp.Size = Vector3.new(25, 25, 25)
        hrp.Transparency = 0.7
        hrp.Color = Color3.fromRGB(255, 100, 100)
        hrp.Material = Enum.Material.Neon
    end

    local function activateHitbox()
        -- تنظيف القديم
        for _, data in pairs(hitboxObjects) do
            if data.hrp and data.hrp.Parent then
                -- ما نرجع Hitbox لحالته الأصلية، نتركه
            end
        end
        hitboxObjects = {}
        
        local enemies = getRealEnemies()
        
        for _, enemy in pairs(enemies) do
            if enemy.Character then
                applyHitbox(enemy.Character)
                table.insert(hitboxObjects, {player = enemy, hrp = enemy.Character.HumanoidRootPart})
            end
        end
        
        hitboxEnabled = true
        return enemies
    end

    local function deactivateHitbox()
        hitboxEnabled = false
        -- ما ننظف الـ Hitbox، نتركه
    end

    -- =============================================
    -- نظام Flight
    -- =============================================
    local flightLoaded = false

    local function activateFlight()
        if not flightLoaded then
            loadstring(game:HttpGet("https://pastebin.com/raw/5WLxDDvL"))()
            flightLoaded = true
        end
    end

    -- =============================================
    -- نظام القتل والـ Dropdown (معدل)
    -- =============================================
    local selectedTarget = "All Enemies"
    local enemyNames = {"All Enemies"}
    local enemyDropdownRef = nil -- راح نخزن الـ Dropdown هنا

    local function refreshEnemyNames()
        local enemies = getRealEnemies()
        local names = {"All Enemies"}
        
        for _, enemy in pairs(enemies) do
            table.insert(names, enemy.Name)
        end
        
        enemyNames = names
        return enemies
    end

    local function updateDropdown()
        if enemyDropdownRef then
            enemyDropdownRef:Set({Values = enemyNames})
        end
    end

    local function killTarget()
        if selectedTarget == "All Enemies" then
            local enemies = getRealEnemies()
            
            if #enemies == 0 then
                WindUI:Notify({
                    Title = "Kill",
                    Content = t.noEnemies,
                    Duration = 2
                })
                return
            end
            
            for _, enemy in pairs(enemies) do
                pcall(function()
                    ReplicatedStorage.KnifeKill:FireServer(enemy, enemy)
                end)
                task.wait(0.2)
            end
            
            WindUI:Notify({
                Title = "Kill",
                Content = t.killedAll .. " " .. #enemies .. " " .. (languageChoice == "Arabic" and "عدو" or "enemies"),
                Duration = 3
            })
        else
            local target = Players:FindFirstChild(selectedTarget)
            if target then
                pcall(function()
                    ReplicatedStorage.KnifeKill:FireServer(target, target)
                end)
                WindUI:Notify({
                    Title = "Kill",
                    Content = t.killedTarget .. ": " .. target.Name,
                    Duration = 3
                })
            end
        end
    end

    -- =============================================
    -- تبويب الرئيسية
    -- =============================================
    
    -- زر ESP
    local espButton = Tabs.Main:Button({
        Title = t.espOn,
        Icon = "eye",
        Size = "large",
        Callback = function()
            if espEnabled then
                deactivateESP()
                espButton:Set({Title = t.espOn})
                WindUI:Notify({
                    Title = "ESP",
                    Content = t.espDeactivated,
                    Duration = 2
                })
            else
                local enemies = activateESP()
                local count = #enemies
                espButton:Set({Title = t.espOff .. " (" .. count .. ")"})
                WindUI:Notify({
                    Title = "ESP",
                    Content = t.espActivated .. " " .. count .. " " .. (languageChoice == "Arabic" and "عدو" or "enemies"),
                    Duration = 3
                })
            end
        end
    })

    -- زر Hitbox
    local hitboxButton = Tabs.Main:Button({
        Title = t.hitboxOn,
        Icon = "target",
        Size = "large",
        Callback = function()
            if hitboxEnabled then
                deactivateHitbox()
                hitboxButton:Set({Title = t.hitboxOn})
                WindUI:Notify({
                    Title = "Hitbox",
                    Content = t.hitboxDeactivated,
                    Duration = 2
                })
            else
                local enemies = activateHitbox()
                local count = #enemies
                hitboxButton:Set({Title = t.hitboxOff .. " (" .. count .. ")"})
                WindUI:Notify({
                    Title = "Hitbox",
                    Content = t.hitboxActivated .. " " .. count .. " " .. (languageChoice == "Arabic" and "عدو" or "enemies"),
                    Duration = 3
                })
            end
        end
    })

    -- زر Flight
    local flightButton = Tabs.Main:Button({
        Title = t.flightButton,
        Icon = "wind",
        Size = "large",
        Callback = function()
            activateFlight()
            flightButton:Set({Title = t.flightActive})
            WindUI:Notify({
                Title = "Flight",
                Content = t.flightActivated,
                Duration = 3
            })
        end
    })

    -- Dropdown اختيار العدو (بدون Set)
    enemyDropdownRef = Tabs.Main:Dropdown({
        Title = t.targetSelect,
        Icon = "users",
        Size = "large",
        Values = enemyNames,
        Callback = function(value)
            selectedTarget = value
        end
    })

    -- زر القتل
    Tabs.Main:Button({
        Title = t.killButton,
        Icon = "skull",
        Size = "large",
        Variant = "Destructive",
        Callback = function()
            killTarget()
        end
    })

    -- زر تحديث الأعداء
    Tabs.Main:Button({
        Title = t.refreshButton,
        Icon = "refresh-cw",
        Size = "large",
        Callback = function()
            local enemies = refreshEnemyNames()
            local count = #enemies
            
            -- تحديث الـ Dropdown بدون Set
            -- نعيد إنشاء القائمة بدل Set
            local dropdown = Tabs.Main:Get("Dropdown")
            if dropdown then
                -- نحذف القديم وننشئ جديد
                dropdown:Destroy()
                enemyDropdownRef = Tabs.Main:Dropdown({
                    Title = t.targetSelect,
                    Icon = "users",
                    Size = "large",
                    Values = enemyNames,
                    Callback = function(value)
                        selectedTarget = value
                    end
                })
            end
            
            WindUI:Notify({
                Title = "Refresh",
                Content = t.enemiesRefreshed .. " (" .. count .. ")",
                Duration = 2
            })
            
            -- إذا ESP شغال، نحدثه
            if espEnabled then
                deactivateESP()
                task.wait(0.1)
                activateESP()
            end
        end
    })

    -- =============================================
    -- تبويب الإعدادات
    -- =============================================
    do
        Tabs.Settings:Dropdown({
            Title = t.themeDropdown,
            Values = t.themeValues,
            Value = t.themeValues[1],
            Size = "large",
            Callback = function(value)
                local themeMap = {
                    [t.themeValues[1]] = "Dark",
                    [t.themeValues[2]] = "Light", 
                    [t.themeValues[3]] = "Sapphire",
                    [t.themeValues[4]] = "Emerald",
                    [t.themeValues[5]] = "Ruby"
                }
                WindUI:SetTheme(themeMap[value] or "Dark")
            end
        })

        Tabs.Settings:Button({
            Title = t.updateButton,
            Icon = "refresh-cw",
            Size = "large",
            Callback = function()
                WindUI:Notify({
                    Title = "Update",
                    Content = "جاري إعادة التحميل...",
                    Duration = 3
                })
                task.wait(2)
                loadstring(game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/Faq.txt"))()
            end
        })

        Tabs.Settings:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "large",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
                WindUI:Notify({
                    Title = "Discord",
                    Content = t.discordCopied,
                    Duration = 3
                })
            end
        })

        Tabs.Settings:Button({
            Title = t.closeButton,
            Icon = "power",
            Size = "large",
            Variant = "Destructive",
            Callback = function()
                deactivateESP()
                deactivateHitbox()
                Window:Destroy()
                WindUI:Notify({
                    Title = "Close",
                    Content = t.scriptClosed,
                    Duration = 2
                })
            end
        })
    end

    -- =============================================
    -- تبويب المطور
    -- =============================================
    do
        Tabs.Developer:Paragraph({
            Title = t.developerName,
            Desc = t.developerDesc,
            Image = "user",
            ImageSize = 80,
            Color = Color3.fromHex("#FF6B35")
        })

        Tabs.Developer:Button({
            Title = t.versionButton,
            Icon = "tag",
            Size = "large",
            Callback = function()
                WindUI:Notify({
                    Title = "PhantomXPro v2.1.0",
                    Content = languageChoice == "Arabic" and "أحدث إصدار من السكربت" or "Latest script version",
                    Duration = 3
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "large",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
                WindUI:Notify({
                    Title = "Discord",
                    Content = t.discordCopied,
                    Duration = 3
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.contactButton,
            Icon = "mail",
            Size = "large",
            Callback = function()
                setclipboard("cypherclient")
                WindUI:Notify({
                    Title = "Contact",
                    Content = languageChoice == "Arabic" and "اسم المستخدم: cypherclient" or "Username: cypherclient",
                    Duration = 3
                })
            end
        })
    end

    -- =============================================
    -- بدء التشغيل
    -- =============================================
    task.wait(2)
    
    -- تحديث أولي
    local initialEnemies = refreshEnemyNames()
    
    -- إشعار الترحيب
    WindUI:Notify({
        Title = "PhantomXPro",
        Content = t.scriptLoaded .. " (" .. #initialEnemies .. " " .. (languageChoice == "Arabic" and "عدو" or "enemies") .. ")",
        Duration = 4
    })

    -- تحديث الـ Dropdown أول مرة
    if enemyDropdownRef then
        local dropdown = Tabs.Main:Get("Dropdown")
        if dropdown then
            dropdown:Destroy()
            enemyDropdownRef = Tabs.Main:Dropdown({
                Title = t.targetSelect,
                Icon = "users",
                Size = "large",
                Values = enemyNames,
                Callback = function(value)
                    selectedTarget = value
                end
            })
        end
    end

else
    print("Failed to load WindUI")
end