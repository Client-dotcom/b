print("PhantomXPro - Final ESP System")

-- تحميل WindUI
local success, result = pcall(function()
    local source = game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua", true)
    if source then
        local loaded = loadstring(source)
        if loaded then
            return loaded()
        end
    end
    return nil
end)

if success and result then
    local WindUI = result
    local Players = game:GetService("Players")
    local Workspace = game:GetService("Workspace")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local player = Players.LocalPlayer

    -- اختيار اللغة
    local languageChoice = nil
    WindUI:Popup({
        Title = "PhantomXPro - اختر اللغة",
        Icon = "globe",
        Content = "الرجاء اختيار لغة السكربت",
        Buttons = {
            {
                Title = "العربية",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "Arabic"
                end
            },
            {
                Title = "English",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "English"
                end
            }
        }
    })

    repeat task.wait() until languageChoice ~= nil

    local texts = {
        Arabic = {
            mainTab = "الرئيسية",
            espToggle = "ESP",
            hitboxToggle = "Hitbox",
            hitboxSize = "حجم Hitbox",
            flightToggle = "الطيران",
            targetSelect = "اختر عدو",
            killButton = "قتل",
            selectAll = "جميع الأعداء",
            espOn = "ESP مفعّل",
            espOff = "ESP معطّل",
            hitboxOn = "Hitbox مفعّل",
            hitboxOff = "Hitbox معطّل",
            flightOn = "الطيران مفعّل",
            flightOff = "الطيران معطّل",
            killedAll = "تم قتل جميع الأعداء",
            killedTarget = "تم قتل الهدف",
            noEnemies = "لا يوجد أعداء",
            flightActivated = "تم تفعيل الطيران",
            
            settingsTab = "الإعدادات",
            themeDropdown = "اختر السمة",
            themeValues = {"داكن 🌙", "فاتح ☀️", "أزرق 🔵", "أخضر 💚", "أحمر 🔴"},
            updateButton = "تحديث السكربت",
            closeButton = "إغلاق السكربت",
            discordButton = "انضم لسيرفر الديسكورد",
            
            developerTab = "المطور",
            developerName = "Cypher",
            developerDesc = "مطور سكربتات روبلوكس",
            versionButton = "الإصدار 2.1.0",
            contactButton = "تواصل مع المطور",
        },
        English = {
            mainTab = "Main",
            espToggle = "ESP",
            hitboxToggle = "Hitbox",
            hitboxSize = "Hitbox Size",
            flightToggle = "Flight",
            targetSelect = "Select Enemy",
            killButton = "Kill",
            selectAll = "All Enemies",
            espOn = "ESP Enabled",
            espOff = "ESP Disabled",
            hitboxOn = "Hitbox Enabled",
            hitboxOff = "Hitbox Disabled",
            flightOn = "Flight Enabled",
            flightOff = "Flight Disabled",
            killedAll = "Killed all enemies",
            killedTarget = "Killed target",
            noEnemies = "No enemies",
            flightActivated = "Flight activated",
            
            settingsTab = "Settings",
            themeDropdown = "Select Theme",
            themeValues = {"Dark 🌙", "Light ☀️", "Blue 🔵", "Green 💚", "Red 🔴"},
            updateButton = "Update Script",
            closeButton = "Close Script",
            discordButton = "Join Discord Server",
            
            developerTab = "Developer",
            developerName = "Cypher",
            developerDesc = "Roblox Script Developer",
            versionButton = "Version 2.1.0",
            contactButton = "Contact Developer",
        }
    }

    local t = texts[languageChoice]

    WindUI.TransparencyValue = 0.2
    WindUI:SetTheme("Dark")

    -- إنشاء نافذة
    local Window = WindUI:CreateWindow({
        Title = "PhantomXPro",
        Icon = "shield",
        Author = "By Cypher",
        Folder = "PhantomXPro",
        Size = UDim2.fromOffset(550, 380),
        Theme = "Dark",
        User = {
            Enabled = true,
            Anonymous = false,
            Callback = function()
                WindUI:Notify({
                    Title = languageChoice == "Arabic" and "معلوماتك" or "Your Info",
                    Content = (languageChoice == "Arabic" and "اسمك: " or "Name: ") .. player.Name,
                    Duration = 2
                })
            end
        },
        SideBarWidth = 180,
        ScrollBarEnabled = true
    })

    -- قسم واحد
    local MainSection = Window:Section({ Title = "PhantomXPro", Opened = true })

    -- التبويبات
    local Tabs = {
        Main = MainSection:Tab({ Title = t.mainTab, Icon = "home" }),
        Settings = MainSection:Tab({ Title = t.settingsTab, Icon = "settings" }),
        Developer = MainSection:Tab({ Title = t.developerTab, Icon = "user" })
    }

    -- =============================================
    -- نظام الفريق والماب (يتذكرهم)
    -- =============================================
    local cachedMyTeam = nil
    local cachedMyMap = nil
    local lastMapCheck = 0
    local mapCheckInterval = 30 -- كل 30 ثانية يفحص الماب

    local function getPlayerInfo(targetPlayer)
        local info = {team = nil, map = nil}
        
        local success, attrs = pcall(function()
            return targetPlayer:GetAttributes()
        end)
        
        if success then
            for attrName, attrValue in pairs(attrs) do
                local attrLower = tostring(attrName):lower()
                local attrStr = tostring(attrValue)
                
                if attrLower == "team" or attrLower == "teamname" then
                    info.team = attrStr
                elseif attrLower == "map" or attrLower == "mapname" or attrLower == "currentmap" then
                    info.map = attrStr
                end
            end
        end
        
        return info
    end

    local function updateMyInfo()
        local currentTime = tick()
        
        -- إذا مر 30 ثانية يفحص الماب من جديد
        if currentTime - lastMapCheck > mapCheckInterval or not cachedMyTeam or not cachedMyMap then
            local myInfo = getPlayerInfo(player)
            cachedMyTeam = myInfo.team
            cachedMyMap = myInfo.map
            lastMapCheck = currentTime
            
            print("تم تحديث معلوماتي:")
            print("- الفريق:", cachedMyTeam or "غير معروف")
            print("- الماب:", cachedMyMap or "غير معروف")
        end
    end

    -- =============================================
    -- نظام الكشف عن الأعداء
    -- =============================================
    local function getAllEnemies()
        updateMyInfo()
        
        local enemies = {}
        
        -- إذا ما عندي معلومات، أعتبر كل اللاعبين أعداء
        if not cachedMyTeam then
            for _, otherPlayer in pairs(Players:GetPlayers()) do
                if otherPlayer ~= player then
                    table.insert(enemies, otherPlayer)
                end
            end
            return enemies
        end
        
        -- إذا عندي معلومات، أدور على الأعداء
        for _, otherPlayer in pairs(Players:GetPlayers()) do
            if otherPlayer ~= player then
                local enemyInfo = getPlayerInfo(otherPlayer)
                
                -- شرط 1: نفس الماب
                local sameMap = true
                if cachedMyMap and enemyInfo.map then
                    sameMap = (cachedMyMap == enemyInfo.map)
                end
                
                -- شرط 2: فريق مختلف
                local differentTeam = true
                if cachedMyTeam and enemyInfo.team then
                    differentTeam = (cachedMyTeam ~= enemyInfo.team)
                end
                
                -- إذا في نفس الماب وفريق مختلف = عدو
                if sameMap and differentTeam then
                    table.insert(enemies, otherPlayer)
                end
            end
        end
        
        return enemies
    end

    -- =============================================
    -- نظام ESP (مرة واحدة فقط)
    -- =============================================
    local espEnabled = false
    local espObjects = {}
    local espConnections = {}

    local function cleanESP()
        -- تنظيف كل الـ ESP
        for _, obj in pairs(espObjects) do
            if obj and obj.Parent then
                obj:Destroy()
            end
        end
        espObjects = {}
        
        -- قطع الـ Connections
        for _, conn in pairs(espConnections) do
            if conn then
                conn:Disconnect()
            end
        end
        espConnections = {}
    end

    local function createESPForEnemy(enemyPlayer)
        if not enemyPlayer or enemyPlayer == player then return end
        
        local function createESP(char)
            if not char then return end
            
            -- حذف القديم
            for i = #espObjects, 1, -1 do
                local obj = espObjects[i]
                if obj and obj.Name:find(enemyPlayer.Name) then
                    obj:Destroy()
                    table.remove(espObjects, i)
                end
            end
            
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            
            -- Highlight
            local highlight = Instance.new("Highlight")
            highlight.Name = enemyPlayer.Name .. "_ESP"
            highlight.Adornee = char
            highlight.FillColor = Color3.fromRGB(255, 50, 50)
            highlight.FillTransparency = 0.3
            highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
            highlight.OutlineTransparency = 0
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            highlight.Parent = Workspace
            
            -- Name Tag
            local billboard = Instance.new("BillboardGui")
            billboard.Name = enemyPlayer.Name .. "_Name"
            billboard.Size = UDim2.new(0, 120, 0, 30)
            billboard.StudsOffset = Vector3.new(0, 3.5, 0)
            billboard.AlwaysOnTop = true
            billboard.Adornee = hrp
            billboard.Parent = Workspace
            
            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.Text = enemyPlayer.Name
            label.TextColor3 = Color3.fromRGB(255, 100, 100)
            label.TextSize = 14
            label.Font = Enum.Font.GothamBold
            label.TextStrokeTransparency = 0.3
            label.Parent = billboard
            
            -- تخزين
            table.insert(espObjects, highlight)
            table.insert(espObjects, billboard)
            
            -- حدث الموت
            local humanoid = char:FindFirstChild("Humanoid")
            if humanoid then
                local conn = humanoid.Died:Connect(function()
                    task.wait(2)
                    if highlight then highlight:Destroy() end
                    if billboard then billboard:Destroy() end
                end)
                table.insert(espConnections, conn)
            end
        end
        
        -- إنشاء ESP للكاراكتر الحالي
        if enemyPlayer.Character then
            createESP(enemyPlayer.Character)
        end
        
        -- عند تغيير الكاراكتر
        local conn = enemyPlayer.CharacterAdded:Connect(function(char)
            task.wait(0.5)
            createESP(char)
        end)
        table.insert(espConnections, conn)
    end

    local function refreshESP()
        if not espEnabled then return end
        
        cleanESP() -- تنظيف القديم
        
        -- إنشاء ESP جديد لكل الأعداء
        local enemies = getAllEnemies()
        for _, enemy in pairs(enemies) do
            createESPForEnemy(enemy)
        end
        
        return #enemies
    end

    local function toggleESP(state)
        espEnabled = state
        
        if state then
            local enemyCount = refreshESP()
            WindUI:Notify({
                Title = "ESP",
                Content = t.espOn .. " (" .. enemyCount .. " عدو)",
                Duration = 3
            })
        else
            cleanESP()
            WindUI:Notify({
                Title = "ESP",
                Content = t.espOff,
                Duration = 2
            })
        end
    end

    -- =============================================
    -- تحديث قائمة الأعداء
    -- =============================================
    local selectedTarget = t.selectAll
    
    local function updateEnemyList()
        local enemies = getAllEnemies()
        local enemyList = {t.selectAll}
        
        for _, enemy in pairs(enemies) do
            table.insert(enemyList, enemy.Name)
        end
        
        -- تحديث Dropdown
        if Tabs.Main then
            local dropdown = Tabs.Main:Get("enemyDropdown")
            if dropdown then
                dropdown:Set({Values = enemyList})
            end
        end
        
        return #enemies
    end

    -- =============================================
    -- نظام تحديث تلقائي كل 10 ثواني
    -- =============================================
    local updateLoop = nil
    
    local function startAutoUpdate()
        if updateLoop then
            updateLoop:Disconnect()
        end
        
        updateLoop = game:GetService("RunService").Heartbeat:Connect(function()
            task.wait(10) -- انتظر 10 ثواني
            
            -- تحديث معلوماتي
            updateMyInfo()
            
            -- تحديث قائمة الأعداء
            updateEnemyList()
            
            -- إذا الESP شغال، نحدثه
            if espEnabled then
                refreshESP()
            end
        end)
    end

    -- =============================================
    -- تبويب الرئيسية
    -- =============================================
    
    -- ESP Toggle
    local espToggleHandle
    espToggleHandle = Tabs.Main:Toggle({
        Title = t.espToggle,
        Icon = "eye",
        Size = "large",
        Value = false,
        Callback = function(state)
            espToggleHandle:Set({Title = state and "ESP ✓" or t.espToggle})
            toggleESP(state)
        end
    })

    -- Hitbox Toggle (اختياري)
    local hitboxToggleHandle
    hitboxToggleHandle = Tabs.Main:Toggle({
        Title = t.hitboxToggle,
        Icon = "target",
        Size = "large",
        Value = false,
        Callback = function(state)
            hitboxToggleHandle:Set({Title = state and "Hitbox ✓" or t.hitboxToggle})
            WindUI:Notify({
                Title = "Hitbox",
                Content = state and t.hitboxOn or t.hitboxOff,
                Duration = 2
            })
        end
    })

    -- Flight Toggle
    local flightToggleHandle
    flightToggleHandle = Tabs.Main:Toggle({
        Title = t.flightToggle,
        Icon = "wind",
        Size = "large",
        Value = false,
        Callback = function(state)
            flightToggleHandle:Set({Title = state and "الطيران ✓" or t.flightToggle})
            if state then
                loadstring(game:HttpGet("https://pastebin.com/raw/5WLxDDvL"))()
                WindUI:Notify({
                    Title = "Flight",
                    Content = t.flightActivated,
                    Duration = 3
                })
            end
        end
    })

    -- اختيار العدو
    local enemyDropdown
    enemyDropdown = Tabs.Main:Dropdown({
        Title = t.targetSelect,
        Icon = "users",
        Size = "large",
        Values = {t.selectAll},
        Callback = function(value)
            selectedTarget = value
        end
    })

    -- زر القتل
    Tabs.Main:Button({
        Title = t.killButton,
        Icon = "skull",
        Size = "large",
        Variant = "Destructive",
        Callback = function()
            if selectedTarget == t.selectAll then
                local enemies = getAllEnemies()
                
                if #enemies == 0 then
                    WindUI:Notify({
                        Title = "Kill System",
                        Content = t.noEnemies,
                        Duration = 2
                    })
                    return
                end
                
                for _, enemy in pairs(enemies) do
                    pcall(function()
                        ReplicatedStorage.KnifeKill:FireServer(enemy, enemy)
                    end)
                    task.wait(0.3)
                end
                
                WindUI:Notify({
                    Title = "Kill System",
                    Content = t.killedAll .. " (" .. #enemies .. ")",
                    Duration = 3
                })
            else
                local target = Players:FindFirstChild(selectedTarget)
                if target then
                    pcall(function()
                        ReplicatedStorage.KnifeKill:FireServer(target, target)
                    end)
                    WindUI:Notify({
                        Title = "Kill System",
                        Content = t.killedTarget .. ": " .. target.Name,
                        Duration = 3
                    })
                end
            end
        end
    })

    -- زر تحديث يدوي
    Tabs.Main:Button({
        Title = "تحديث الآن",
        Icon = "refresh-cw",
        Size = "large",
        Callback = function()
            updateMyInfo()
            local enemyCount = updateEnemyList()
            
            if espEnabled then
                refreshESP()
            end
            
            WindUI:Notify({
                Title = "تحديث",
                Content = "تم التحديث! (" .. enemyCount .. " عدو)",
                Duration = 3
            })
        end
    })

    -- =============================================
    -- تبويب الإعدادات
    -- =============================================
    do
        Tabs.Settings:Dropdown({
            Title = t.themeDropdown,
            Values = t.themeValues,
            Value = t.themeValues[1],
            Size = "large",
            Callback = function(value)
                local themeMap = {
                    [t.themeValues[1]] = "Dark",
                    [t.themeValues[2]] = "Light", 
                    [t.themeValues[3]] = "Sapphire",
                    [t.themeValues[4]] = "Emerald",
                    [t.themeValues[5]] = "Ruby"
                }
                WindUI:SetTheme(themeMap[value] or "Dark")
            end
        })

        Tabs.Settings:Button({
            Title = t.updateButton,
            Icon = "refresh-cw",
            Size = "large",
            Callback = function()
                WindUI:Notify({
                    Title = "Update",
                    Content = "جاري إعادة التحميل...",
                    Duration = 3
                })
                task.wait(2)
                loadstring(game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/Faq.txt"))()
            end
        })

        Tabs.Settings:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "large",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
                WindUI:Notify({
                    Title = "تم النسخ",
                    Content = "رابط الديسكورد في الحافظة",
                    Duration = 3
                })
            end
        })

        Tabs.Settings:Button({
            Title = t.closeButton,
            Icon = "power",
            Size = "large",
            Variant = "Destructive",
            Callback = function()
                cleanESP()
                if updateLoop then
                    updateLoop:Disconnect()
                end
                Window:Destroy()
                WindUI:Notify({
                    Title = "إغلاق",
                    Content = "تم إغلاق السكربت",
                    Duration = 2
                })
            end
        })
    end

    -- =============================================
    -- تبويب المطور
    -- =============================================
    do
        Tabs.Developer:Paragraph({
            Title = t.developerName,
            Desc = t.developerDesc,
            Image = "user",
            ImageSize = 80,
            Color = Color3.fromHex("#FF6B35")
        })

        Tabs.Developer:Button({
            Title = t.versionButton,
            Icon = "tag",
            Size = "large",
            Callback = function()
                WindUI:Notify({
                    Title = "PhantomXPro v2.1.0",
                    Content = languageChoice == "Arabic" and "أحدث إصدار من السكربت" or "Latest script version",
                    Duration = 3
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "large",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
                WindUI:Notify({
                    Title = "تم النسخ",
                    Content = "رابط الديسكورد في الحافظة",
                    Duration = 3
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.contactButton,
            Icon = "mail",
            Size = "large",
            Callback = function()
                setclipboard("cypherclient")
                WindUI:Notify({
                    Title = "تم النسخ",
                    Content = "اسم المستخدم: cypherclient",
                    Duration = 3
                })
            end
        })
    end

    -- =============================================
    -- بداية التشغيل
    -- =============================================
    
    -- تحديث أولي بعد ثانيتين
    task.wait(2)
    
    -- جلب معلوماتي للمرة الأولى
    updateMyInfo()
    
    -- تحديث قائمة الأعداء
    local initialEnemies = updateEnemyList()
    
    -- بدء التحديث التلقائي
    startAutoUpdate()
    
    -- إشعار التشغيل
    WindUI:Notify({
        Title = "PhantomXPro",
        Content = "تم تحميل السكربت! (" .. initialEnemies .. " عدو)",
        Duration = 4
    })
    
    -- عرض معلوماتي
    WindUI:Notify({
        Title = "معلوماتي",
        Content = "الفريق: " .. (cachedMyTeam or "غير معروف") .. 
                "\nالماب: " .. (cachedMyMap or "غير معروف"),
        Duration = 5
    })

else
    print("Failed to load WindUI")
end