print("PhantomXPro")

-- الطريقة الصحيحة لتحميل WindUI
local success, result = pcall(function()
    local source = game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua", true)
    if source then
        local loaded = loadstring(source)
        if loaded then
            return loaded()
        end
    end
    return nil
end)

if success and result then
    local WindUI = result
    local Players = game:GetService("Players")
    local player = Players.LocalPlayer
    local RunService = game:GetService("RunService")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Workspace = game:GetService("Workspace")
    local UserInputService = game:GetService("UserInputService")
    local Mouse = player:GetMouse()

    -- اختيار اللغة في البداية
    local languageChoice = nil
    WindUI:Popup({
        Title = "PhantomXPro - اختر اللغة / Select Language",
        Icon = "globe",
        Content = "الرجاء اختيار لغة السكربت\nPlease select script language",
        Buttons = {
            {
                Title = "العربية",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "Arabic"
                end
            },
            {
                Title = "English",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "English"
                end
            }
        }
    })

    -- الانتظار حتى يختار المستخدم اللغة
    repeat task.wait() until languageChoice ~= nil

    -- نصوص حسب اللغة
    local texts = {
        Arabic = {
            welcomeTitle = "PhantomXPro",
            welcomeContent = "⚠️ تنبيه هام:\nتشغيل السكربت يكون على مسؤوليتك الشخصية والكاملة، ونحن غير مسؤولين عن أي ضرر. مع أننا نعمل على تقديم سكربت بأعلى معايير الأمان الممكنة، هذا تحذير احتراطي في حال حدوث أي مشكلة غير متوقعة.نتمني لكم تجربة ممتعة ❤",
            discordTitle = "🎮 انضم لسيرفر الديسكورد",
            discordContent = "انضم للسيرڤر الرسمي للسكربت!\n• آخر التحديثات\n• مساعدة فنية\n• إقتراحات وتحسينات\n• تحديثات أمان\n\nالرابط: https://discord.gg/CgUa36sPNs",
            okButton = "موافق",
            copyButton = "نسخ الرابط وتفعيل السكربت",
            copiedTitle = "✅ تم نسخ الرابط",
            copiedContent = "تم تفعيل السكربت!\nرابط الديسكورد في الحافظة",
            windowTitle = "PhantomXPro",
            settingsTab = "الإعدادات",
            mainTab = "الأدوات الرئيسية",
            developerTab = "المطور",
            themeDropdown = "اختر السمة",
            themeValues = {"داكن 🌙", "فاتح ☀️", "أزرق 🔵", "أخضر 💚", "أحمر 🔴"},
            updateButton = "تحديث السكربت",
            updateTitle = "جاري التحديث",
            updateContent = "بيتم إعادة تحميل السكربت...",
            closeButton = "أقفل السكربت",
            closeTitle = "تم الإغلاق",
            closeContent = "تم إغلاق السكربت بنجاح",
            discordButton = "انضم لسيرفر الديسكورد",
            discordCopied = "✅ تم نسخ الرابط",
            discordCopiedContent = "رابط الديسكورد في الحافظة",
            developerName = "Cypher",
            developerDesc = "مطور سكربتات روبلوكس",
            versionButton = "الإصدار 2.1.0",
            contactButton = "تواصل مع المطور",
            contactCopied = "✅ تم النسخ",
            contactContent = "اسم المستخدم: cypherclient",
            finalTitle = "أهلاً وسهلاً في PhantomXPro 🌟",
            finalContent = "السكربت شغّال وجاهز للاستخدام!\nتمتع بمميزاتنا الحصرية 🚀",
            espButtonOn = "ESP - مفعل",
            espButtonOff = "ESP - معطل",
            espTitle = "ESP للمراقبة",
            espContent = "تم تفعيل نظام كشف الأعداء",
            espContentOff = "تم إيقاف نظام كشف الأعداء",
            hitboxButtonOn = "Hitbox - مفعل",
            hitboxButtonOff = "Hitbox - معطل",
            hitboxTitle = "Hitbox للأعداء",
            hitboxContent = "تم تفعيل نظام Hitbox",
            hitboxContentOff = "تم إيقاف نظام Hitbox",
            flyButtonOn = "طيران - مفعل",
            flyButtonOff = "طيران - معطل",
            flyTitle = "نظام الطيران",
            flyContent = "تم تحميل نظام الطيران",
            hitboxSlider = "حجم Hitbox:",
            hitboxSliderDesc = "اضبط حجم الهيتبوكس من 1 إلى 50",
            hitboxSliderNotify = "✅ تم تغيير حجم الهيتبوكس إلى",
            aimAssistButtonOn = "مساعدة التصويب - مفعل",
            aimAssistButtonOff = "مساعدة التصويب - معطل",
            aimAssistTitle = "نظام المساعدة في التصويب",
            aimAssistContent = "تم تفعيل نظام المساعدة في التصويب",
            aimAssistContentOff = "تم إيقاف نظام المساعدة في التصويب",
            aimAssistSlider = "مسافة التصويب:",
            aimAssistSliderDesc = "اضبط مسافة التصويب من 1 إلى 10000",
            aimAssistSliderNotify = "✅ تم تغيير مسافة التصويب إلى",
            clickShootButtonOn = "تصوير بالكليك - مفعل",
            clickShootButtonOff = "تصوير بالكليك - معطل",
            clickShootTitle = "نظام التصوير بالكليك",
            clickShootContent = "تم تفعيل التصوير عند الضغط",
            clickShootContentOff = "تم إيقاف التصوير عند الضغط",
            autoShootButtonOn = "تصوير تلقائي - مفعل",
            autoShootButtonOff = "تصوير تلقائي - معطل",
            autoShootTitle = "نظام التصوير التلقائي",
            autoShootContent = "تم تفعيل التصوير التلقائي",
            autoShootContentOff = "تم إيقاف التصوير التلقائي",
            mvsEspButtonOn = "MVS ESP - مفعل",
            mvsEspButtonOff = "MVS ESP - معطل",
            mvsEspTitle = "نظام إبراز الأعداء",
            mvsEspContent = "تم تفعيل إبراز الأعداء",
            mvsEspContentOff = "تم إيقاف إبراز الأعداء"
        },
        English = {
            welcomeTitle = "PhantomXPro",
            welcomeContent = "⚠️ Important Warning:\nUsing the script is at your own personal and full responsibility, and we are not responsible for any damage. While we strive to provide a script with the highest possible security standards, this is a precautionary warning in case any unexpected problem occurs. We wish you a great experience ❤",
            discordTitle = "🎮 Join Discord Server",
            discordContent = "Join the official script server!\n• Latest updates\n• Technical help\n• Suggestions and improvements\n• Security updates\n\nLink: https://discord.gg/CgUa36sPNs",
            okButton = "OK",
            copyButton = "Copy Link & Activate Script",
            copiedTitle = "✅ Link Copied",
            copiedContent = "Script activated!\nDiscord link in clipboard",
            windowTitle = "PhantomXPro",
            settingsTab = "Settings",
            mainTab = "Main Tools",
            developerTab = "Developer",
            themeDropdown = "Select Theme",
            themeValues = {"Dark 🌙", "Light ☀️", "Blue 🔵", "Green 💚", "Red 🔴"},
            updateButton = "Update Script",
            updateTitle = "Updating",
            updateContent = "Reloading script...",
            closeButton = "Close Script",
            closeTitle = "Closed",
            closeContent = "Script closed successfully",
            discordButton = "Join Discord Server",
            discordCopied = "✅ Link Copied",
            discordCopiedContent = "Discord link in clipboard",
            developerName = "Cypher",
            developerDesc = "Roblox Script Developer",
            versionButton = "Version 2.1.0",
            contactButton = "Contact Developer",
            contactCopied = "✅ Copied",
            contactContent = "Username: cypherclient",
            finalTitle = "Welcome to PhantomXPro 🌟",
            finalContent = "Script is running and ready to use!\nEnjoy our exclusive features 🚀",
            espButtonOn = "ESP - Enabled",
            espButtonOff = "ESP - Disabled",
            espTitle = "ESP System",
            espContent = "Enemy detection system activated",
            espContentOff = "Enemy detection system deactivated",
            hitboxButtonOn = "Hitbox - Enabled",
            hitboxButtonOff = "Hitbox - Disabled",
            hitboxTitle = "Enemy Hitbox",
            hitboxContent = "Hitbox system activated",
            hitboxContentOff = "Hitbox system deactivated",
            flyButtonOn = "Fly - Enabled",
            flyButtonOff = "Fly - Disabled",
            flyTitle = "Fly System",
            flyContent = "Fly system loaded successfully",
            hitboxSlider = "Hitbox Size:",
            hitboxSliderDesc = "Adjust hitbox size from 1 to 50",
            hitboxSliderNotify = "✅ Hitbox size changed to",
            aimAssistButtonOn = "Aim Assist - Enabled",
            aimAssistButtonOff = "Aim Assist - Disabled",
            aimAssistTitle = "Aim Assist System",
            aimAssistContent = "Aim assist system activated",
            aimAssistContentOff = "Aim assist system deactivated",
            aimAssistSlider = "Aim Distance:",
            aimAssistSliderDesc = "Adjust aim distance from 1 to 10000",
            aimAssistSliderNotify = "✅ Aim distance changed to",
            clickShootButtonOn = "Click Shoot - Enabled",
            clickShootButtonOff = "Click Shoot - Disabled",
            clickShootTitle = "Click Shoot System",
            clickShootContent = "Click shooting activated",
            clickShootContentOff = "Click shooting deactivated",
            autoShootButtonOn = "Auto Shoot - Enabled",
            autoShootButtonOff = "Auto Shoot - Disabled",
            autoShootTitle = "Auto Shoot System",
            autoShootContent = "Auto shooting activated",
            autoShootContentOff = "Auto shooting deactivated",
            mvsEspButtonOn = "MVS ESP - Enabled",
            mvsEspButtonOff = "MVS ESP - Disabled",
            mvsEspTitle = "Enemy Highlight System",
            mvsEspContent = "Enemy highlighting activated",
            mvsEspContentOff = "Enemy highlighting deactivated"
        }
    }

    local t = texts[languageChoice]

    WindUI.TransparencyValue = 0.5
    
    -- بدء السمة بشكل آمن
    pcall(function()
        WindUI:SetTheme("Dark")
    end)

    -- نافذة البوب أب الترحيبية
    WindUI:Popup({
        Title = t.welcomeTitle,
        Icon = "crown",
        Content = t.welcomeContent,
        Buttons = {
            {
                Title = t.okButton,
                Icon = "arrow-right",
                Variant = "Primary",
                Callback = function() end
            }
        }
    })

    -- نافذة Discord المنبثقة
    task.wait(5)
    WindUI:Popup({
        Title = t.discordTitle,
        Icon = "message-circle",
        Content = t.discordContent,
        Buttons = {
            {
                Title = t.copyButton,
                Icon = "copy",
                Variant = "Primary",
                Callback = function()
                    pcall(function()
                        setclipboard("https://discord.gg/CgUa36sPNs")
                    end)
                    WindUI:Notify({
                        Title = t.copiedTitle,
                        Content = t.copiedContent,
                        Duration = 4
                    })
                end
            }
        }
    })

    -- إنشاء النافذة الرئيسية
    local Window = WindUI:CreateWindow({
        Title = t.windowTitle,
        Icon = "crown", 
        Author = "By_Cypher",
        Folder = "PhantomXPro",
        Size = UDim2.fromOffset(800, 750),
        Theme = "Dark",
        User = {
            Enabled = true,
            Anonymous = false,
            Callback = function()
                WindUI:Notify({
                    Title = languageChoice == "Arabic" and "معلوماتك" or "Your Info",
                    Content = (languageChoice == "Arabic" and "اسمك: " or "Name: ") .. player.Name,
                    Duration = 3
                })
            end
        },
        SideBarWidth = 250,
        ScrollBarEnabled = true
    })

    -- الأقسام الرئيسية
    local Sections = {
        Main = Window:Section({ Title = t.mainTab, Opened = true }),
        Settings = Window:Section({ Title = t.settingsTab, Opened = false }),
        Credits = Window:Section({ Title = t.developerTab, Opened = false })
    }

    -- التبويبات
    local Tabs = {
        Main = Sections.Main:Tab({ Title = t.mainTab, Icon = "grid" }),
        Config = Sections.Settings:Tab({ Title = t.settingsTab, Icon = "settings" }),
        Developer = Sections.Credits:Tab({ Title = t.developerTab, Icon = "user" })
    }

    -- متغيرات الحالة
    local ESPState = false
    local HitboxState = false
    local FlyState = false
    local AimAssistState = false
    local ClickShootState = false
    local AutoShootState = false
    local MVSESPState = false
    local CurrentHitboxSize = 20
    local AimDistance = 250
    local ESPScriptLoaded = false
    local HitboxScriptLoaded = false
    local FlyScriptLoaded = false

    -- =============================================
    -- دوال MVS من الملف الثاني (مع حماية من الأخطاء)
    -- =============================================
    
    -- متغيرات MVS
    local currentWeapon = nil
    local isWeaponEquipped = false
    local lastFireTime = 0
    local fireDelay = 1.5
    local espHighlights = {}
    _G.BlockClick = false
    
    -- دوال إضافية لـ MVS
    local function getTeamFrames()
        local success, redFrame, blueFrame = pcall(function()
            local playerGui = player:FindFirstChildOfClass("PlayerGui")
            if not playerGui then return nil, nil end
            local main = playerGui:FindFirstChild("Main")
            if not main then return nil, nil end
            local mainGameFrame = main:FindFirstChild("MainGameFrame")
            if not mainGameFrame then return nil, nil end
            local playersFrame = mainGameFrame:FindFirstChild("PlayersFrame")
            if not playersFrame then return nil, nil end
            return playersFrame:FindFirstChild("TeamRedFrame"), playersFrame:FindFirstChild("TeamBlueFrame")
        end)
        if success then
            return redFrame, blueFrame
        end
        return nil, nil
    end
    
    local function getFrameChildrenNames(frame)
        local names = {}
        if not frame then return names end
        for _, child in ipairs(frame:GetChildren()) do
            if child:IsA("Frame") then
                table.insert(names, child.Name)
            end
        end
        return names
    end
    
    local function sanitizeName(name)
        return tostring(name):gsub("%s+", "")
    end
    
    local function getEnemyPlayers()
        local enemyPlayerInstances = {}
        local enemyTeamColor = Color3.fromRGB(255, 0, 0)
        
        local success, result = pcall(function()
            local redFrame, blueFrame = getTeamFrames()
            if not redFrame or not blueFrame then return {}, {} end
        
            local redPlayers = getFrameChildrenNames(redFrame)
            local bluePlayers = getFrameChildrenNames(blueFrame)
        
            local isLocalPlayerInRed = false
            local isLocalPlayerInBlue = false
        
            local sanitizedLocalPlayerName = sanitizeName(player.Name)
        
            for _, name in ipairs(redPlayers) do
                if sanitizeName(name) == sanitizedLocalPlayerName then
                    isLocalPlayerInRed = true
                    break
                end
            end
        
            for _, name in ipairs(bluePlayers) do
                if sanitizeName(name) == sanitizedLocalPlayerName then
                    isLocalPlayerInBlue = true
                    break
                end
            end
        
            if not isLocalPlayerInRed and not isLocalPlayerInBlue then
                return {}, {}
            end
        
            local enemyFrame = isLocalPlayerInRed and blueFrame or redFrame
            local enemyNames = getFrameChildrenNames(enemyFrame)
        
            for _, name in ipairs(enemyNames) do
                local enemyPlayer = Players:FindFirstChild(name)
                if enemyPlayer and enemyPlayer ~= player then
                    table.insert(enemyPlayerInstances, enemyPlayer)
                end
            end
        
            enemyTeamColor = isLocalPlayerInRed and Color3.fromRGB(0, 0, 255) or Color3.fromRGB(255, 0, 0)
            
            return enemyPlayerInstances, enemyTeamColor
        end)
        
        if success then
            return enemyPlayerInstances, enemyTeamColor
        else
            return {}, Color3.fromRGB(255, 0, 0)
        end
    end
    
    local function updateWeaponScripts()
        pcall(function()
            if not currentWeapon or not currentWeapon.Parent or not currentWeapon:IsDescendantOf(game) then
                return
            end
    
            local scripts = {}
            for _, descendant in ipairs(currentWeapon:GetDescendants()) do
                if descendant:IsA("Script") or descendant:IsA("LocalScript") then
                    table.insert(scripts, descendant)
                end
            end
            
            if currentWeapon:FindFirstChild("Client") then
                table.insert(scripts, currentWeapon:FindFirstChild("Client"))
            end
            if currentWeapon:FindFirstChild("GunScript") then
                table.insert(scripts, currentWeapon:FindFirstChild("GunScript"))
            end
    
            for _, script in ipairs(scripts) do
                if script and script.Disabled ~= _G.BlockClick then
                    script.Disabled = _G.BlockClick
                end
            end
        end)
    end
    
    local function initializeWeaponScript()
        pcall(function()
            local character = player.Character
            if not character then
                currentWeapon = nil
                isWeaponEquipped = false
                return
            end
    
            local equippedTool
            for _, child in ipairs(character:GetChildren()) do
                if child:IsA("Tool") and child:FindFirstChild("fire") then
                    equippedTool = child
                    break
                end
            end
    
            if equippedTool and currentWeapon ~= equippedTool then
                currentWeapon = equippedTool
                isWeaponEquipped = true
                updateWeaponScripts()
            elseif not equippedTool and isWeaponEquipped then
                currentWeapon = nil
                isWeaponEquipped = false
            end
        end)
    end
    
    local function updateBlockState()
        pcall(function()
            if (ClickShootState or AutoShootState) and _G.BlockClick then
                _G.BlockClick = false
                updateWeaponScripts()
            elseif not (ClickShootState or AutoShootState) and not _G.BlockClick then
                _G.BlockClick = true
                updateWeaponScripts()
            end
        end)
    end
    
    local function getClosestPlayer()
        local closestPlayer = nil
        local minDistance = AimDistance or 100
        
        pcall(function()
            local players = getEnemyPlayers()
            if not players or type(players) ~= "table" then return nil end
    
            local character = player.Character
            if not character or not character:IsDescendantOf(Workspace) then return nil end
            local hrp = character:FindFirstChild("HumanoidRootPart")
            if not hrp then return nil end
    
            for _, enemyPlayer in ipairs(players) do
                if enemyPlayer and typeof(enemyPlayer) == "Instance" and enemyPlayer:IsA("Player") then
                    local targetCharacter = enemyPlayer.Character
                    if targetCharacter and targetCharacter:IsDescendantOf(Workspace) then
                        local targetHrp = targetCharacter:FindFirstChild("HumanoidRootPart")
                        local targetHumanoid = targetCharacter:FindFirstChild("Humanoid")
                        if targetHrp and targetHumanoid and targetHumanoid.Health > 0 then
                            local success, distance = pcall(function() 
                                return (hrp.Position - targetHrp.Position).Magnitude 
                            end)
                            if success and distance < minDistance then
                                minDistance = distance
                                closestPlayer = enemyPlayer
                            end
                        end
                    end
                end
            end
        end)
        
        return closestPlayer
    end
    
    local function canSeeTarget(targetPlayer)
        if not targetPlayer then return false end
        
        local canSee = false
        
        pcall(function()
            local localCharacter = player.Character
            if not localCharacter or not localCharacter:IsDescendantOf(Workspace) then return false end
            local targetCharacter = targetPlayer.Character
            if not targetCharacter or not targetCharacter:IsDescendantOf(Workspace) then return false end
    
            local localHrp = localCharacter:FindFirstChild("HumanoidRootPart")
            local targetHrp = targetCharacter:FindFirstChild("HumanoidRootPart")
            local targetHumanoid = targetCharacter:FindFirstChild("Humanoid")
    
            if not localHrp or not targetHrp or not targetHumanoid then return false end
    
            local success, distance = pcall(function() 
                return (localHrp.Position - targetHrp.Position).Magnitude 
            end)
            if not success or distance > (AimDistance or 100) then return false end
    
            local raycastParams = RaycastParams.new()
            raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
            raycastParams.FilterDescendantsInstances = { localCharacter }
            raycastParams.IgnoreWater = true
            raycastParams.CollisionGroup = "Default"
    
            local direction = targetHrp.Position - localHrp.Position
            local raycastResult = Workspace:Raycast(
                localHrp.Position, 
                direction.Unit * math.min(distance, AimDistance or 100), 
                raycastParams
            )
    
            if not raycastResult then 
                canSee = true 
                return 
            end
            local hitPart = raycastResult.Instance
            if hitPart and hitPart:IsDescendantOf(targetCharacter) then
                canSee = true
            end
        end)
        
        return canSee
    end
    
    local function shootLogic()
        if tick() - lastFireTime < fireDelay then return end
        lastFireTime = tick()
        if not isWeaponEquipped then return end
        
        pcall(function()
            local target = getClosestPlayer()
            if not target then return end
            if not canSeeTarget(target) then return end
    
            local localCharacter = player.Character
            if not localCharacter or not localCharacter:IsDescendantOf(Workspace) then return end
            local localHrp = localCharacter:FindFirstChild("HumanoidRootPart")
            if not localHrp then return end
    
            local targetCharacter = target.Character
            if not targetCharacter or not targetCharacter:IsDescendantOf(Workspace) then return end
            local targetHrp = targetCharacter:FindFirstChild("HumanoidRootPart")
            if not targetHrp then return end
    
            local predictedPosition = targetHrp.Position
            local handle = currentWeapon:FindFirstChild("Handle") or currentWeapon
            if not handle then return end
    
            if currentWeapon:FindFirstChild("fire") then
                currentWeapon.fire:FireServer()
            end
            
            if ReplicatedStorage:FindFirstChild("LocalBeam") then
                ReplicatedStorage.LocalBeam:Fire(handle, predictedPosition)
            end
            
            if currentWeapon:FindFirstChild("showBeam") then
                currentWeapon.showBeam:FireServer(predictedPosition, localHrp.Position, handle)
            end
            
            if currentWeapon:FindFirstChild("kill") then
                local direction = (predictedPosition - localHrp.Position).Unit
                currentWeapon.kill:FireServer(target, direction, localHrp.Position)
            end
        end)
    end
    
    -- نظام MVS ESP
    local function removeHighlight(targetPlayer)
        if espHighlights[targetPlayer] then
            pcall(function() 
                espHighlights[targetPlayer]:Destroy() 
            end)
            espHighlights[targetPlayer] = nil
        end
    end
    
    local function updateHighlight(targetPlayer, color)
        pcall(function()
            local character = targetPlayer.Character
            if not character then removeHighlight(targetPlayer) return end
    
            local highlight = espHighlights[targetPlayer]
            if not highlight or not highlight.Parent then
                if highlight then removeHighlight(targetPlayer) end
                highlight = Instance.new("Highlight")
                highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                highlight.FillTransparency = 0.5
                highlight.OutlineTransparency = 0
                highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                highlight.Parent = character
                espHighlights[targetPlayer] = highlight
            end
    
            highlight.Adornee = character
            highlight.FillColor = color
        end)
    end
    
    local function updateAllHighlights()
        if not MVSESPState then
            for targetPlayer, _ in pairs(espHighlights) do 
                removeHighlight(targetPlayer) 
            end
            return
        end
        
        pcall(function()
            local enemyPlayers, enemyColor = getEnemyPlayers()
            if not enemyPlayers then return end
    
            local currentEnemies = {}
            for _, targetPlayer in ipairs(enemyPlayers) do
                currentEnemies[targetPlayer] = true
                updateHighlight(targetPlayer, enemyColor)
            end
    
            for targetPlayer, _ in pairs(espHighlights) do
                if not currentEnemies[targetPlayer] then 
                    removeHighlight(targetPlayer) 
                end
            end
        end)
    end
    
    -- =============================================
    -- تبويب الأدوات الرئيسية (Main)
    -- =============================================
    do
        -- زر ESP
        local espButtonText = t.espButtonOff
        local espButton = Tabs.Main:Button({
            Title = espButtonText,
            Icon = "eye",
            Size = "xlarge",
            Callback = function()
                ESPState = not ESPState
                
                if ESPState then
                    espButtonText = t.espButtonOn
                    WindUI:Notify({
                        Title = t.espTitle,
                        Content = t.espContent,
                        Duration = 3
                    })
                    
                    -- تحميل سكربت ESP
                    pcall(function()
                        loadstring(game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/Enemy%20detc.txt", true))()
                    end)
                    ESPScriptLoaded = true
                    
                    -- تحديث النص في الزر
                    espButton:Destroy()
                    espButton = Tabs.Main:Button({
                        Title = espButtonText,
                        Icon = "eye",
                        Size = "xlarge",
                        Callback = function()
                            ESPState = not ESPState
                            
                            if not ESPState then
                                espButtonText = t.espButtonOff
                                WindUI:Notify({
                                    Title = t.espTitle,
                                    Content = t.espContentOff,
                                    Duration = 3
                                })
                                
                                -- تنظيف ESP
                                pcall(function()
                                    for _, obj in pairs(Workspace:GetChildren()) do
                                        if obj.Name:find("_EnemyChams") or obj.Name:find("_EnemyName") then
                                            obj:Destroy()
                                        end
                                    end
                                end)
                                ESPScriptLoaded = false
                                
                                -- تحديث النص في الزر
                                espButton:Destroy()
                                espButton = Tabs.Main:Button({
                                    Title = espButtonText,
                                    Icon = "eye",
                                    Size = "xlarge",
                                    Callback = function()
                                        ESPState = not ESPState
                                        
                                        if ESPState then
                                            espButtonText = t.espButtonOn
                                            WindUI:Notify({
                                                Title = t.espTitle,
                                                Content = t.espContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحميل سكربت ESP
                                            pcall(function()
                                                loadstring(game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/Enemy%20detc.txt", true))()
                                            end)
                                            ESPScriptLoaded = true
                                            
                                            -- تحديث النص في الزر
                                            espButton:Destroy()
                                            espButton = Tabs.Main:Button({
                                                Title = espButtonText,
                                                Icon = "eye",
                                                Size = "xlarge",
                                                Callback = function()
                                                    -- كود التبديل...
                                                end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- زر Hitbox
        local hitboxButtonText = t.hitboxButtonOff
        local hitboxButton = Tabs.Main:Button({
            Title = hitboxButtonText,
            Icon = "target",
            Size = "xlarge",
            Callback = function()
                HitboxState = not HitboxState
                
                if HitboxState then
                    hitboxButtonText = t.hitboxButtonOn
                    WindUI:Notify({
                        Title = t.hitboxTitle,
                        Content = t.hitboxContent,
                        Duration = 3
                    })
                    
                    -- تحميل سكربت Hitbox مع حجم محدد
                    pcall(function()
                        local hitboxScript = game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/hitbox%20.txt", true)
                        hitboxScript = hitboxScript:gsub("local HitboxSize = 20", "local HitboxSize = " .. CurrentHitboxSize)
                        loadstring(hitboxScript)()
                    end)
                    HitboxScriptLoaded = true
                    
                    -- تحديث النص في الزر
                    hitboxButton:Destroy()
                    hitboxButton = Tabs.Main:Button({
                        Title = hitboxButtonText,
                        Icon = "target",
                        Size = "xlarge",
                        Callback = function()
                            HitboxState = not HitboxState
                            
                            if not HitboxState then
                                hitboxButtonText = t.hitboxButtonOff
                                WindUI:Notify({
                                    Title = t.hitboxTitle,
                                    Content = t.hitboxContentOff,
                                    Duration = 3
                                })
                                
                                -- تنظيف Hitbox
                                pcall(function()
                                    local gui = player:FindFirstChild("PlayerGui")
                                    if gui then
                                        local hitboxGui = gui:FindFirstChild("HitboxControl")
                                        if hitboxGui then
                                            hitboxGui:Destroy()
                                        end
                                    end
                                    
                                    -- إرجاع حجم HRP الطبيعي لجميع اللاعبين
                                    for _, otherPlayer in pairs(Players:GetPlayers()) do
                                        if otherPlayer ~= player and otherPlayer.Character then
                                            local hrp = otherPlayer.Character:FindFirstChild("HumanoidRootPart")
                                            if hrp and hrp:IsA("BasePart") then
                                                hrp.Size = Vector3.new(2, 1, 1)
                                                hrp.Transparency = 0
                                                hrp.CanCollide = true
                                                hrp.Color = Color3.fromRGB(255, 255, 255)
                                            end
                                        end
                                    end
                                end)
                                HitboxScriptLoaded = false
                                
                                -- تحديث النص في الزر
                                hitboxButton:Destroy()
                                hitboxButton = Tabs.Main:Button({
                                    Title = hitboxButtonText,
                                    Icon = "target",
                                    Size = "xlarge",
                                    Callback = function()
                                        HitboxState = not HitboxState
                                        
                                        if HitboxState then
                                            hitboxButtonText = t.hitboxButtonOn
                                            WindUI:Notify({
                                                Title = t.hitboxTitle,
                                                Content = t.hitboxContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحميل سكربت Hitbox مع حجم محدد
                                            pcall(function()
                                                local hitboxScript = game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/hitbox%20.txt", true)
                                                hitboxScript = hitboxScript:gsub("local HitboxSize = 20", "local HitboxSize = " .. CurrentHitboxSize)
                                                loadstring(hitboxScript)()
                                            end)
                                            HitboxScriptLoaded = true
                                            
                                            -- تحديث النص في الزر
                                            hitboxButton:Destroy()
                                            hitboxButton = Tabs.Main:Button({
                                                Title = hitboxButtonText,
                                                Icon = "target",
                                                Size = "xlarge",
                                                Callback = function()
                                                    -- كود التبديل...
                                                end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- سلايدر حجم Hitbox
        Tabs.Main:Slider({
            Title = t.hitboxSlider,
            Desc = t.hitboxSliderDesc,
            Value = CurrentHitboxSize,
            Min = 1,
            Max = 50,
            Step = 1,
            Callback = function(value)
                CurrentHitboxSize = value
                WindUI:Notify({
                    Title = t.hitboxSliderNotify,
                    Content = tostring(value),
                    Duration = 3
                })
                
                -- إذا كان Hitbox مفعلاً، نعيد تحميله بالحجم الجديد
                if HitboxState and HitboxScriptLoaded then
                    pcall(function()
                        -- تنظيف الـ GUI القديم
                        local gui = player:FindFirstChild("PlayerGui")
                        if gui then
                            local hitboxGui = gui:FindFirstChild("HitboxControl")
                            if hitboxGui then
                                hitboxGui:Destroy()
                            end
                        end
                        
                        -- تحميل سكربت جديد بالحجم الجديد
                        local hitboxScript = game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/hitbox%20.txt", true)
                        hitboxScript = hitboxScript:gsub("local HitboxSize = 20", "local HitboxSize = " .. CurrentHitboxSize)
                        loadstring(hitboxScript)()
                    end)
                end
            end
        })

        -- زر الطيران
        local flyButtonText = t.flyButtonOff
        local flyButton = Tabs.Main:Button({
            Title = flyButtonText,
            Icon = "wind",
            Size = "xlarge",
            Callback = function()
                FlyState = not FlyState
                
                if FlyState then
                    flyButtonText = t.flyButtonOn
                    WindUI:Notify({
                        Title = t.flyTitle,
                        Content = t.flyContent,
                        Duration = 3
                    })
                    
                    -- تحميل سكربت الطيران
                    pcall(function()
                        loadstring(game:HttpGet("https://pastebin.com/raw/5WLxDDvL", true))()
                    end)
                    FlyScriptLoaded = true
                    
                    -- تحديث النص في الزر
                    flyButton:Destroy()
                    flyButton = Tabs.Main:Button({
                        Title = flyButtonText,
                        Icon = "wind",
                        Size = "xlarge",
                        Callback = function()
                            FlyState = not FlyState
                            
                            if not FlyState then
                                flyButtonText = t.flyButtonOff
                                -- إيقاف الطيران
                                pcall(function()
                                    local humanoid = player.Character and player.Character:FindFirstChild("Humanoid")
                                    if humanoid then
                                        humanoid.PlatformStand = false
                                    end
                                end)
                                FlyScriptLoaded = false
                                
                                -- تحديث النص في الزر
                                flyButton:Destroy()
                                flyButton = Tabs.Main:Button({
                                    Title = flyButtonText,
                                    Icon = "wind",
                                    Size = "xlarge",
                                    Callback = function()
                                        FlyState = not FlyState
                                        
                                        if FlyState then
                                            flyButtonText = t.flyButtonOn
                                            WindUI:Notify({
                                                Title = t.flyTitle,
                                                Content = t.flyContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحميل سكربت الطيران
                                            pcall(function()
                                                loadstring(game:HttpGet("https://pastebin.com/raw/5WLxDDvL", true))()
                                            end)
                                            FlyScriptLoaded = true
                                            
                                            -- تحديث النص في الزر
                                            flyButton:Destroy()
                                            flyButton = Tabs.Main:Button({
                                                Title = flyButtonText,
                                                Icon = "wind",
                                                Size = "xlarge",
                                                Callback = function()
                                                    -- كود التبديل...
                                                end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- زر MVS ESP (النظام الجديد)
        local mvsEspButtonText = t.mvsEspButtonOff
        local mvsEspButton = Tabs.Main:Button({
            Title = mvsEspButtonText,
            Icon = "star",
            Size = "xlarge",
            Callback = function()
                MVSESPState = not MVSESPState
                
                if MVSESPState then
                    mvsEspButtonText = t.mvsEspButtonOn
                    WindUI:Notify({
                        Title = t.mvsEspTitle,
                        Content = t.mvsEspContent,
                        Duration = 3
                    })
                    
                    -- تحديث النص في الزر
                    mvsEspButton:Destroy()
                    mvsEspButton = Tabs.Main:Button({
                        Title = mvsEspButtonText,
                        Icon = "star",
                        Size = "xlarge",
                        Callback = function()
                            MVSESPState = not MVSESPState
                            
                            if not MVSESPState then
                                mvsEspButtonText = t.mvsEspButtonOff
                                WindUI:Notify({
                                    Title = t.mvsEspTitle,
                                    Content = t.mvsEspContentOff,
                                    Duration = 3
                                })
                                
                                -- تنظيف highlights
                                for targetPlayer, _ in pairs(espHighlights) do
                                    removeHighlight(targetPlayer)
                                end
                                
                                -- تحديث النص في الزر
                                mvsEspButton:Destroy()
                                mvsEspButton = Tabs.Main:Button({
                                    Title = mvsEspButtonText,
                                    Icon = "star",
                                    Size = "xlarge",
                                    Callback = function()
                                        MVSESPState = not MVSESPState
                                        
                                        if MVSESPState then
                                            mvsEspButtonText = t.mvsEspButtonOn
                                            WindUI:Notify({
                                                Title = t.mvsEspTitle,
                                                Content = t.mvsEspContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحديث النص في الزر
                                            mvsEspButton:Destroy()
                                            mvsEspButton = Tabs.Main:Button({
                                                Title = mvsEspButtonText,
                                                Icon = "star",
                                                Size = "xlarge",
                                                Callback = function()
                                                    -- كود التبديل...
                                                end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- زر مساعدة التصويب (Aim Assist)
        local aimAssistButtonText = t.aimAssistButtonOff
        local aimAssistButton = Tabs.Main:Button({
            Title = aimAssistButtonText,
            Icon = "crosshair",
            Size = "xlarge",
            Callback = function()
                AimAssistState = not AimAssistState
                
                if AimAssistState then
                    aimAssistButtonText = t.aimAssistButtonOn
                    WindUI:Notify({
                        Title = t.aimAssistTitle,
                        Content = t.aimAssistContent,
                        Duration = 3
                    })
                    
                    -- بدء النظام
                    pcall(function()
                        if player.Backpack then
                            player.Backpack.ChildRemoved:Connect(function(child)
                                if child:IsA("Tool") and child == currentWeapon then
                                    isWeaponEquipped = false
                                    task.wait(0.1)
                                    initializeWeaponScript()
                                end
                            end)
                            player.Backpack.ChildAdded:Connect(function(child)
                                if child:IsA("Tool") and child:FindFirstChild("fire") and currentWeapon and child.Name == currentWeapon.Name then
                                    currentWeapon = nil
                                    isWeaponEquipped = false
                                    updateWeaponScripts()
                                end
                            end)
                        end
                    end)
                    
                    task.spawn(function()
                        while AimAssistState and task.wait(0.1) do
                            initializeWeaponScript()
                        end
                    end)
                    
                    -- تحديث النص في الزر
                    aimAssistButton:Destroy()
                    aimAssistButton = Tabs.Main:Button({
                        Title = aimAssistButtonText,
                        Icon = "crosshair",
                        Size = "xlarge",
                        Callback = function()
                            AimAssistState = not AimAssistState
                            
                            if not AimAssistState then
                                aimAssistButtonText = t.aimAssistButtonOff
                                WindUI:Notify({
                                    Title = t.aimAssistTitle,
                                    Content = t.aimAssistContentOff,
                                    Duration = 3
                                })
                                
                                -- تحديث النص في الزر
                                aimAssistButton:Destroy()
                                aimAssistButton = Tabs.Main:Button({
                                    Title = aimAssistButtonText,
                                    Icon = "crosshair",
                                    Size = "xlarge",
                                    Callback = function()
                                        AimAssistState = not AimAssistState
                                        
                                        if AimAssistState then
                                            aimAssistButtonText = t.aimAssistButtonOn
                                            WindUI:Notify({
                                                Title = t.aimAssistTitle,
                                                Content = t.aimAssistContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحديث النص في الزر
                                            aimAssistButton:Destroy()
                                            aimAssistButton = Tabs.Main:Button({
                                                Title = aimAssistButtonText,
                                                Icon = "crosshair",
                                                Size = "xlarge",
                                                Callback = function()
                                                    -- كود التبديل...
                                                end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- زر التصوير بالكليك (Click Shoot)
        local clickShootButtonText = t.clickShootButtonOff
        local clickShootButton = Tabs.Main:Button({
            Title = clickShootButtonText,
            Icon = "mouse-pointer",
            Size = "xlarge",
            Callback = function()
                ClickShootState = not ClickShootState
                
                if ClickShootState then
                    clickShootButtonText = t.clickShootButtonOn
                    WindUI:Notify({
                        Title = t.clickShootTitle,
                        Content = t.clickShootContent,
                        Duration = 3
                    })
                    
                    -- تحديث حالة الحظر
                    updateBlockState()
                    
                    -- تحديث النص في الزر
                    clickShootButton:Destroy()
                    clickShootButton = Tabs.Main:Button({
                        Title = clickShootButtonText,
                        Icon = "mouse-pointer",
                        Size = "xlarge",
                        Callback = function()
                            ClickShootState = not ClickShootState
                            
                            if not ClickShootState then
                                clickShootButtonText = t.clickShootButtonOff
                                WindUI:Notify({
                                    Title = t.clickShootTitle,
                                    Content = t.clickShootContentOff,
                                    Duration = 3
                                })
                                
                                -- تحديث حالة الحظر
                                updateBlockState()
                                
                                -- تحديث النص في الزر
                                clickShootButton:Destroy()
                                clickShootButton = Tabs.Main:Button({
                                    Title = clickShootButtonText,
                                    Icon = "mouse-pointer",
                                    Size = "xlarge",
                                    Callback = function()
                                        ClickShootState = not ClickShootState
                                        
                                        if ClickShootState then
                                            clickShootButtonText = t.clickShootButtonOn
                                            WindUI:Notify({
                                                Title = t.clickShootTitle,
                                                Content = t.clickShootContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحديث حالة الحظر
                                            updateBlockState()
                                            
                                            -- تحديث النص في الزر
                                            clickShootButton:Destroy()
                                            clickShootButton = Tabs.Main:Button({
                                                Title = clickShootButtonText,
                                                Icon = "mouse-pointer",
                                                Size = "xlarge",
                                                Callback = function()
                                                    -- كود التبديل...
                                                end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- زر التصوير التلقائي (Auto Shoot)
        local autoShootButtonText = t.autoShootButtonOff
        local autoShootButton = Tabs.Main:Button({
            Title = autoShootButtonText,
            Icon = "zap",
            Size = "xlarge",
            Callback = function()
                AutoShootState = not AutoShootState
                
                if AutoShootState then
                    autoShootButtonText = t.autoShootButtonOn
                    WindUI:Notify({
                        Title = t.autoShootTitle,
                        Content = t.autoShootContent,
                        Duration = 3
                    })
                    
                    -- تحديث حالة الحظر
                    updateBlockState()
                    
                    -- بدء التصوير التلقائي
                    task.spawn(function()
                        while AutoShootState and RunService.Heartbeat:Wait() do
                            if isWeaponEquipped and currentWeapon and not _G.BlockClick then
                                local target = getClosestPlayer()
                                if target and canSeeTarget(target) then
                                    shootLogic()
                                end
                            end
                        end
                    end)
                    
                    -- تحديث النص في الزر
                    autoShootButton:Destroy()
                    autoShootButton = Tabs.Main:Button({
                        Title = autoShootButtonText,
                        Icon = "zap",
                        Size = "xlarge",
                        Callback = function()
                            AutoShootState = not AutoShootState
                            
                            if not AutoShootState then
                                autoShootButtonText = t.autoShootButtonOff
                                WindUI:Notify({
                                    Title = t.autoShootTitle,
                                    Content = t.autoShootContentOff,
                                    Duration = 3
                                })
                                
                                -- تحديث حالة الحظر
                                updateBlockState()
                                
                                -- تحديث النص في الزر
                                autoShootButton:Destroy()
                                autoShootButton = Tabs.Main:Button({
                                    Title = autoShootButtonText,
                                    Icon = "zap",
                                    Size = "xlarge",
                                    Callback = function()
                                        AutoShootState = not AutoShootState
                                        
                                        if AutoShootState then
                                            autoShootButtonText = t.autoShootButtonOn
                                            WindUI:Notify({
                                                Title = t.autoShootTitle,
                                                Content = t.autoShootContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحديث حالة الحظر
                                            updateBlockState()
                                            
                                            -- تحديث النص في الزر
                                            autoShootButton:Destroy()
                                            autoShootButton = Tabs.Main:Button({
                                                Title = autoShootButtonText,
                                                Icon = "zap",
                                                Size = "xlarge",
                                                Callback = function()
                                                    -- كود التبديل...
                                                end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- سلايدر مسافة التصويب
        Tabs.Main:Slider({
            Title = t.aimAssistSlider,
            Desc = t.aimAssistSliderDesc,
            Value = AimDistance,
            Min = 1,
            Max = 10000,
            Step = 10,
            Callback = function(value)
                AimDistance = value
                WindUI:Notify({
                    Title = t.aimAssistSliderNotify,
                    Content = tostring(value),
                    Duration = 3
                })
            end
        })
    end

    -- =============================================
    -- تبويب الإعدادات (Config)
    -- =============================================
    do
        Tabs.Config:Dropdown({
            Title = t.themeDropdown,
            Values = t.themeValues,
            Value = t.themeValues[1],
            Size = "xlarge",
            Callback = function(value)
                local themeMap = {
                    [t.themeValues[1]] = "Dark",
                    [t.themeValues[2]] = "Light", 
                    [t.themeValues[3]] = "Sapphire",
                    [t.themeValues[4]] = "Emerald",
                    [t.themeValues[5]] = "Ruby"
                }
                pcall(function()
                    WindUI:SetTheme(themeMap[value] or "Dark")
                end)
            end
        })

        -- زر تحديث السكربت
        Tabs.Config:Button({
            Title = t.updateButton,
            Icon = "refresh-cw",
            Size = "xlarge",
            Callback = function()
                WindUI:Notify({
                    Title = t.updateTitle,
                    Content = t.updateContent,
                    Duration = 3
                })
                wait(2)
                pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/Faq.txt", true))()
                end)
            end
        })

        -- زر إغلاق السكربت
        Tabs.Config:Button({
            Title = t.closeButton,
            Icon = "power",
            Size = "xlarge",
            Callback = function()
                -- تنظيف جميع السكربتات أولاً
                
                -- تنظيف ESP
                pcall(function()
                    for _, obj in pairs(Workspace:GetChildren()) do
                        if obj.Name:find("_EnemyChams") or obj.Name:find("_EnemyName") then
                            obj:Destroy()
                        end
                    end
                end)
                
                -- تنظيف Hitbox
                pcall(function()
                    local gui = player:FindFirstChild("PlayerGui")
                    if gui then
                        local hitboxGui = gui:FindFirstChild("HitboxControl")
                        if hitboxGui then
                            hitboxGui:Destroy()
                        end
                    end
                    
                    -- إرجاع حجم HRP الطبيعي
                    for _, otherPlayer in pairs(Players:GetPlayers()) do
                        if otherPlayer ~= player and otherPlayer.Character then
                            local hrp = otherPlayer.Character:FindFirstChild("HumanoidRootPart")
                            if hrp and hrp:IsA("BasePart") then
                                hrp.Size = Vector3.new(2, 1, 1)
                                hrp.Transparency = 0
                                hrp.CanCollide = true
                                hrp.Color = Color3.fromRGB(255, 255, 255)
                            end
                        end
                    end
                end)
                
                -- إيقاف الطيران
                pcall(function()
                    local humanoid = player.Character and player.Character:FindFirstChild("Humanoid")
                    if humanoid then
                        humanoid.PlatformStand = false
                    end
                end)
                
                -- تنظيف MVS ESP
                for targetPlayer, _ in pairs(espHighlights) do
                    removeHighlight(targetPlayer)
                end
                
                -- إغلاق النافذة
                pcall(function()
                    Window:Destroy()
                end)
                WindUI:Notify({
                    Title = t.closeTitle,
                    Content = t.closeContent,
                    Duration = 3
                })
            end
        })

        -- زر دخول الديسكورد
        Tabs.Config:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "xlarge",
            Callback = function()
                pcall(function()
                    setclipboard("https://discord.gg/CgUa36sPNs")
                end)
                WindUI:Notify({
                    Title = t.discordCopied,
                    Content = t.discordCopiedContent,
                    Duration = 4
                })
            end
        })
    end

    -- =============================================
    -- تبويب المطور (Credits)
    -- =============================================
    do
        Tabs.Developer:Paragraph({
            Title = t.developerName,
            Desc = t.developerDesc,
            Image = "user",
            ImageSize = 80,
            Color = Color3.fromHex("#FF6B35")
        })

        -- معلومات المطور
        Tabs.Developer:Button({
            Title = t.versionButton,
            Icon = "tag",
            Size = "xlarge",
            Callback = function()
                WindUI:Notify({
                    Title = "PhantomXPro v2.1.0",
                    Content = languageChoice == "Arabic" and "أحدث إصدار من السكربت" or "Latest script version",
                    Duration = 4
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "xlarge",
            Callback = function()
                pcall(function()
                    setclipboard("https://discord.gg/CgUa36sPNs")
                end)
                WindUI:Notify({
                    Title = t.discordCopied,
                    Content = t.discordCopiedContent,
                    Duration = 4
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.contactButton,
            Icon = "mail",
            Size = "xlarge",
            Callback = function()
                pcall(function()
                    setclipboard("cypherclient")
                end)
                WindUI:Notify({
                    Title = t.contactCopied,
                    Content = t.contactContent,
                    Duration = 4
                })
            end
        })
    end

    -- التنظيف عند الإغلاق
    Window:OnDestroy(function()
        WindUI:Notify({
            Title = languageChoice == "Arabic" and "إغلاق السكربت" or "Script Closed",
            Content = languageChoice == "Arabic" and "تم إغلاق PhantomXPro بنجاح" or "PhantomXPro closed successfully",
            Duration = 3
        })
    end)

    -- دالة تتبع الشخصيات للـ MVS ESP
    local function trackCharacter(targetPlayer)
        local function onCharAdded(character)
            task.wait(0.1)
            if MVSESPState then updateAllHighlights() end
        end
        targetPlayer.CharacterAdded:Connect(onCharAdded)
    end
    
    -- تهيئة تتبع اللاعبين
    for _, targetPlayer in ipairs(Players:GetPlayers()) do
        if targetPlayer ~= player then trackCharacter(targetPlayer) end
    end
    
    Players.PlayerAdded:Connect(function(targetPlayer)
        if targetPlayer ~= player then trackCharacter(targetPlayer) end
    end)
    
    player.CharacterAdded:Connect(function()
        task.wait(0.1)
        if MVSESPState then updateAllHighlights() end
    end)
    
    Players.PlayerRemoving:Connect(function(targetPlayer)
        removeHighlight(targetPlayer)
    end)
    
    -- تحديث highlights باستمرار
    RunService.Heartbeat:Connect(function()
        if MVSESPState then pcall(updateAllHighlights) end
    end)
    
    -- توصيل الضغط بالماوس لـ Click Shoot
    Mouse.Button1Down:Connect(function()
        if ClickShootState and isWeaponEquipped and currentWeapon and not _G.BlockClick then
            local target = getClosestPlayer()
            if target and canSeeTarget(target) then
                shootLogic()
            end
        end
    end)

    -- إشعار الترحيب النهائي
    task.wait(4)
    WindUI:Notify({
        Title = t.finalTitle,
        Content = t.finalContent,
        Duration = 8
    })
else
    print("Failed to load WindUI")
end