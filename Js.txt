-- MVS Complete Script - No Problems Version
print("MVS Complete Script - Stable Version")

-- تحميل WindUI
local WindUI = nil
local success, result = pcall(function()
    local source = game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua", true)
    if source then
        local loaded = loadstring(source)
        if loaded then
            return loaded()
        end
    end
    return nil
end)

if success and result then
    WindUI = result
    
    -- إعدادات
    WindUI.TransparencyValue = 0.5
    WindUI:SetTheme("Dark")
    
    -- خدمات اللعبة
    local Players = game:GetService("Players")
    local player = Players.LocalPlayer
    local RunService = game:GetService("RunService")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Workspace = game:GetService("Workspace")
    local Mouse = player:GetMouse()
    
    -- =============================================
    -- نافذة الترحيب
    -- =============================================
    task.wait(1)
    WindUI:Popup({
        Title = "🎯 MVS PRO SYSTEM",
        Icon = "target",
        Content = "نظام التصويب الاحترافي الكامل\n\n• نظام Auto Shoot كامل\n• نظام Click Shoot كامل\n• بدون مشاكل\n• بدون أخطاء",
        Buttons = {
            {
                Title = "ابدأ الآن",
                Icon = "play",
                Variant = "Primary",
                Callback = function()
                    print("🚀 System started successfully")
                end
            }
        }
    })
    
    -- =============================================
    -- إنشاء النافذة الرئيسية
    -- =============================================
    local Window = WindUI:CreateWindow({
        Title = "🎯 MVS PRO SYSTEM",
        Icon = "target",
        Author = "MVS Professional",
        Folder = "MVS_Pro",
        Size = UDim2.fromOffset(600, 600),
        Theme = "Dark"
    })
    
    -- =============================================
    -- الأقسام والتبويبات
    -- =============================================
    local Sections = {
        Main = Window:Section({ Title = "الأدوات الرئيسية", Opened = true }),
        Settings = Window:Section({ Title = "الإعدادات", Opened = false })
    }
    
    local Tabs = {
        Shooting = Sections.Main:Tab({ Title = "نظام التصويب", Icon = "target" }),
        Weapons = Sections.Main:Tab({ Title = "الأسلحة", Icon = "package" }),
        Config = Sections.Settings:Tab({ Title = "الإعدادات", Icon = "settings" })
    }
    
    -- =============================================
    -- متغيرات النظام
    -- =============================================
    local AutoShootState = false
    local ClickShootState = false
    local currentWeapon = nil
    local isWeaponEquipped = false
    local lastFireTime = 0
    local fireDelay = 0.15
    local AimDistance = 10000
    
    -- =============================================
    -- دوال النظام (من mur1.txt الأصلية)
    -- =============================================
    
    -- دالة الحصول على الفريق
    local function getTeamFrames()
        local success, redFrame, blueFrame = pcall(function()
            local playerGui = player:FindFirstChildOfClass("PlayerGui")
            if not playerGui then return nil, nil end
            local main = playerGui:FindFirstChild("Main")
            if not main then return nil, nil end
            local mainGameFrame = main:FindFirstChild("MainGameFrame")
            if not mainGameFrame then return nil, nil end
            local playersFrame = mainGameFrame:FindFirstChild("PlayersFrame")
            if not playersFrame then return nil, nil end
            return playersFrame:FindFirstChild("TeamRedFrame"), playersFrame:FindFirstChild("TeamBlueFrame")
        end)
        if success then
            return redFrame, blueFrame
        end
        return nil, nil
    end
    
    local function getFrameChildrenNames(frame)
        local names = {}
        if not frame then return names end
        for _, child in ipairs(frame:GetChildren()) do
            if child:IsA("Frame") then
                table.insert(names, child.Name)
            end
        end
        return names
    end
    
    local function sanitizeName(name)
        return tostring(name):gsub("%s+", "")
    end
    
    -- دالة الحصول على الأعداء (الأصلية من mur1.txt)
    local function getEnemyPlayers()
        local redFrame, blueFrame = getTeamFrames()
        if not redFrame or not blueFrame then return {}, {} end

        local redPlayers = getFrameChildrenNames(redFrame)
        local bluePlayers = getFrameChildrenNames(blueFrame)

        local isLocalPlayerInRed = false
        local isLocalPlayerInBlue = false

        local sanitizedLocalPlayerName = sanitizeName(player.Name)

        for _, name in ipairs(redPlayers) do
            if sanitizeName(name) == sanitizedLocalPlayerName then
                isLocalPlayerInRed = true
                break
            end
        end

        for _, name in ipairs(bluePlayers) do
            if sanitizeName(name) == sanitizedLocalPlayerName then
                isLocalPlayerInBlue = true
                break
            end
        end

        if not isLocalPlayerInRed and not isLocalPlayerInBlue then
            return {}, {}
        end

        local enemyFrame = isLocalPlayerInRed and blueFrame or redFrame
        local enemyNames = getFrameChildrenNames(enemyFrame)

        local enemyPlayerInstances = {}
        for _, name in ipairs(enemyNames) do
            local enemyPlayer = Players:FindFirstChild(name)
            if enemyPlayer and enemyPlayer ~= player then
                table.insert(enemyPlayerInstances, enemyPlayer)
            end
        end

        return enemyPlayerInstances
    end
    
    -- دالة تهيئة السلاح (الأصلية من mur1.txt)
    local function initializeWeaponScript()
        local character = player.Character
        if not character then
            currentWeapon = nil
            isWeaponEquipped = false
            return
        end

        local equippedTool
        for _, child in ipairs(character:GetChildren()) do
            if child:IsA("Tool") and child:FindFirstChild("fire") then
                equippedTool = child
                break
            end
        end

        if equippedTool and currentWeapon ~= equippedTool then
            currentWeapon = equippedTool
            isWeaponEquipped = true
        elseif not equippedTool and isWeaponEquipped then
            currentWeapon = nil
            isWeaponEquipped = false
        end
    end
    
    -- دالة أقرب لاعب (الأصلية من mur1.txt)
    local function getClosestPlayer()
        local players = getEnemyPlayers()
        if not players or type(players) ~= "table" then return nil end

        local character = player.Character
        if not character then return nil end
        local hrp = character:FindFirstChild("HumanoidRootPart")
        if not hrp then return nil end

        local closestPlayer, minDistance = nil, AimDistance or 100
        for _, enemyPlayer in ipairs(players) do
            if enemyPlayer and typeof(enemyPlayer) == "Instance" and enemyPlayer:IsA("Player") then
                local targetCharacter = enemyPlayer.Character
                if targetCharacter then
                    local targetHrp = targetCharacter:FindFirstChild("HumanoidRootPart")
                    local targetHumanoid = targetCharacter:FindFirstChild("Humanoid")
                    if targetHrp and targetHumanoid and targetHumanoid.Health > 0 then
                        local distance = (hrp.Position - targetHrp.Position).Magnitude
                        if distance < minDistance then
                            minDistance = distance
                            closestPlayer = enemyPlayer
                        end
                    end
                end
            end
        end
        return closestPlayer
    end
    
    -- دالة الرؤية (مبسطة بدون مشاكل)
    local function canSeeTarget(targetPlayer)
        if not targetPlayer then return false end

        local character = player.Character
        if not character then return false end
        local targetCharacter = targetPlayer.Character
        if not targetCharacter then return false end

        local hrp = character:FindFirstChild("HumanoidRootPart")
        local targetHrp = targetCharacter:FindFirstChild("HumanoidRootPart")

        if not hrp or not targetHrp then return false end

        local distance = (hrp.Position - targetHrp.Position).Magnitude
        if distance > AimDistance then return false end

        -- محاولة Raycast مبسطة
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
        raycastParams.FilterDescendantsInstances = { character }
        raycastParams.IgnoreWater = true

        local direction = (targetHrp.Position - hrp.Position).Unit
        local raycastResult = Workspace:Raycast(
            hrp.Position,
            direction * math.min(distance, AimDistance),
            raycastParams
        )

        return not raycastResult
    end
    
    -- دالة التصوير الكاملة (من mur1.txt)
    local function shootLogic()
        if tick() - lastFireTime < fireDelay then return end
        lastFireTime = tick()
        if not isWeaponEquipped then return end
        
        local target = getClosestPlayer()
        if not target then return end
        
        -- لا نتحقق من الرؤية هنا (يطلق مباشرة)
        -- if not canSeeTarget(target) then return end

        local character = player.Character
        if not character then return end
        local hrp = character:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        local targetCharacter = target.Character
        if not targetCharacter then return end
        local targetHrp = targetCharacter:FindFirstChild("HumanoidRootPart")
        if not targetHrp then return end

        local predictedPosition = targetHrp.Position
        local handle = currentWeapon:FindFirstChild("Handle") or currentWeapon
        if not handle then return end

        -- إطلاق النار (fire)
        pcall(function() 
            if currentWeapon:FindFirstChild("fire") then
                currentWeapon.fire:FireServer() 
            end
        end)
        
        -- أشعة الليزر
        pcall(function()
            if ReplicatedStorage:FindFirstChild("LocalBeam") then
                ReplicatedStorage.LocalBeam:Fire(handle, predictedPosition)
            end
            if currentWeapon:FindFirstChild("showBeam") then
                currentWeapon.showBeam:FireServer(predictedPosition, hrp.Position, handle)
            end
        end)
        
        -- القتل (kill)
        pcall(function()
            if currentWeapon:FindFirstChild("kill") then
                local direction = (predictedPosition - hrp.Position).Unit
                currentWeapon.kill:FireServer(target, direction, hrp.Position)
            end
        end)
        
        return true
    end
    
    -- =============================================
    -- تبويب نظام التصويب (Shooting)
    -- =============================================
    
    -- Toggle Auto Shoot (زر ثابت لا يختفي)
    local autoShootToggle = Tabs.Shooting:Toggle({
        Title = "⚡ Auto Shoot",
        Desc = "التصوير التلقائي - يطلق عند وجود أهداف",
        Value = false,
        Size = "xlarge",
        Callback = function(state)
            AutoShootState = state
            if state then
                WindUI:Notify({
                    Title = "✅ Auto Shoot مفعل",
                    Content = "النظام يبحث عن أهداف ويطلق تلقائياً",
                    Duration = 3,
                    Icon = "zap"
                })
                print("⚡ Auto Shoot Enabled")
            else
                WindUI:Notify({
                    Title = "⏹️ Auto Shoot معطل",
                    Content = "تم إيقاف التصوير التلقائي",
                    Duration = 2,
                    Icon = "power"
                })
                print("⏹️ Auto Shoot Disabled")
            end
        end
    })
    
    -- Toggle Click Shoot (زر ثابت لا يختفي)
    local clickShootToggle = Tabs.Shooting:Toggle({
        Title = "🎯 Click Shoot",
        Desc = "التصوير بالماوس - يطلق عند الضغط",
        Value = false,
        Size = "xlarge",
        Callback = function(state)
            ClickShootState = state
            if state then
                WindUI:Notify({
                    Title = "✅ Click Shoot مفعل",
                    Content = "اضغط بالماوس للتصويب",
                    Duration = 3,
                    Icon = "mouse-pointer"
                })
                print("🎯 Click Shoot Enabled")
            else
                WindUI:Notify({
                    Title = "⏹️ Click Shoot معطل",
                    Content = "تم إيقاف التصوير بالماوس",
                    Duration = 2,
                    Icon = "power"
                })
                print("⏹️ Click Shoot Disabled")
            end
        end
    })
    
    -- زر تشغيل الكل
    Tabs.Shooting:Button({
        Title = "🚀 تشغيل كل الأنظمة",
        Icon = "play",
        Size = "medium",
        Variant = "Success",
        Callback = function()
            AutoShootState = true
            ClickShootState = true
            autoShootToggle:Set(true)
            clickShootToggle:Set(true)
            WindUI:Notify({
                Title = "🚀 كل الأنظمة مفعلة",
                Content = "Auto Shoot و Click Shoot مفعلان الآن",
                Duration = 3
            })
        end
    })
    
    -- زر إيقاف الكل
    Tabs.Shooting:Button({
        Title = "⏹️ إيقاف كل الأنظمة",
        Icon = "power",
        Size = "medium",
        Variant = "Destructive",
        Callback = function()
            AutoShootState = false
            ClickShootState = false
            autoShootToggle:Set(false)
            clickShootToggle:Set(false)
            WindUI:Notify({
                Title = "⏹️ كل الأنظمة معطلة",
                Content = "Auto Shoot و Click Shoot معطلان الآن",
                Duration = 3
            })
        end
    })
    
    -- زر اختبار التصويب
    Tabs.Shooting:Button({
        Title = "🎯 اختبار التصويب الآن",
        Icon = "target",
        Size = "medium",
        Callback = function()
            if isWeaponEquipped and currentWeapon then
                local target = getClosestPlayer()
                if target then
                    local success = shootLogic()
                    if success then
                        WindUI:Notify({
                            Title = "✅ تم التصويب",
                            Content = "تم التصويب على: " .. target.Name,
                            Duration = 3
                        })
                    else
                        WindUI:Notify({
                            Title = "⚠️ لم يتم التصويب",
                            Content = "تعذر التصويب في الوقت الحالي",
                            Duration = 3
                        })
                    end
                else
                    WindUI:Notify({
                        Title = "🎯 لا يوجد أهداف",
                        Content = "لم يتم العثور على أهداف للتصويب",
                        Duration = 3
                    })
                end
            else
                WindUI:Notify({
                    Title = "🔫 لا يوجد سلاح",
                    Content = "أمسك سلاحاً أولاً",
                    Duration = 3
                })
            end
        end
    })
    
    -- =============================================
    -- تبويب الأسلحة (Weapons)
    -- =============================================
    
    -- زر تحديث السلاح
    Tabs.Weapons:Button({
        Title = "🔄 تحديث حالة السلاح",
        Icon = "refresh-cw",
        Size = "xlarge",
        Callback = function()
            initializeWeaponScript()
            if isWeaponEquipped and currentWeapon then
                WindUI:Notify({
                    Title = "✅ سلاح معبأ",
                    Content = "السلاح الحالي: " .. currentWeapon.Name,
                    Duration = 3
                })
            else
                WindUI:Notify({
                    Title = "🔫 لا يوجد سلاح",
                    Content = "لم يتم العثور على سلاح",
                    Duration = 3
                })
            end
        end
    })
    
    -- عرض معلومات السلاح
    Tabs.Weapons:Paragraph({
        Title = "معلومات السلاح الحالي",
        Desc = "سيتم تحديث المعلومات تلقائياً",
        Image = "package"
    })
    
    -- زر إعادة تعيين النظام
    Tabs.Weapons:Button({
        Title = "🔄 إعادة تعيين النظام",
        Icon = "refresh-cw",
        Size = "medium",
        Callback = function()
            initializeWeaponScript()
            WindUI:Notify({
                Title = "🔄 تم إعادة التعيين",
                Content = "تم تحديث جميع أنظمة الأسلحة",
                Duration = 2
            })
        end
    })
    
    -- =============================================
    -- تبويب الإعدادات (Config)
    -- =============================================
    
    -- سلايدر سرعة التصوير
    Tabs.Config:Slider({
        Title = "⚡ سرعة التصوير",
        Desc = "كلما قل الرقم زادت السرعة (0.1 - 2.0)",
        Value = fireDelay,
        Min = 0.05,
        Max = 2.0,
        Step = 0.05,
        Callback = function(value)
            fireDelay = value
            WindUI:Notify({
                Title = "⚡ تم تغيير السرعة",
                Content = "سرعة التصوير: كل " .. value .. " ثانية",
                Duration = 3
            })
        end
    })
    
    -- سلايدر مسافة التصويب
    Tabs.Config:Slider({
        Title = "📏 مسافة التصويب",
        Desc = "المسافة القصوى للتصويب (100 - 10000)",
        Value = AimDistance,
        Min = 100,
        Max = 10000,
        Step = 100,
        Callback = function(value)
            AimDistance = value
            WindUI:Notify({
                Title = "📏 تم تغيير المسافة",
                Content = "مسافة التصويب: " .. value .. " وحدة",
                Duration = 3
            })
        end
    })
    
    -- زر المعلومات
    Tabs.Config:Button({
        Title = "📊 معلومات النظام",
        Icon = "info",
        Size = "xlarge",
        Callback = function()
            local weaponName = currentWeapon and currentWeapon.Name or "لا يوجد"
            local targetCount = #getEnemyPlayers()
            
            WindUI:Popup({
                Title = "📊 معلومات النظام الكاملة",
                Icon = "bar-chart",
                Content = "🎯 Auto Shoot: " .. (AutoShootState and "✅ مفعل" or "❌ معطل") ..
                        "\n🎯 Click Shoot: " .. (ClickShootState and "✅ مفعل" or "❌ معطل") ..
                        "\n\n🔫 السلاح: " .. weaponName ..
                        "\n🎯 عدد الأهداف: " .. targetCount ..
                        "\n⚡ سرعة التصوير: كل " .. fireDelay .. " ثانية" ..
                        "\n📏 مسافة التصويب: " .. AimDistance .. " وحدة" ..
                        "\n\n👤 اللاعب: " .. player.Name,
                Buttons = {
                    {
                        Title = "تم",
                        Icon = "check",
                        Callback = function() end
                    }
                }
            })
        end
    })
    
    -- زر إغلاق السكربت
    Tabs.Config:Button({
        Title = "⏹️ إغلاق السكربت",
        Icon = "power",
        Size = "xlarge",
        Variant = "Destructive",
        Callback = function()
            Window:Destroy()
            WindUI:Notify({
                Title = "👋 وداعاً",
                Content = "تم إغلاق نظام MVS بنجاح",
                Duration = 3
            })
        end
    })
    
    -- =============================================
    -- نظام التشغيل الرئيسي
    -- =============================================
    
    -- تهيئة الأسلحة عند البدء
    initializeWeaponScript()
    
    -- نظام تحديث الأسلحة تلقائياً
    task.spawn(function()
        while task.wait(0.5) do
            initializeWeaponScript()
        end
    end)
    
    -- نظام Auto Shoot (يعمل مثل السابق)
    task.spawn(function()
        while RunService.Heartbeat:Wait() do
            if AutoShootState then
                if isWeaponEquipped and currentWeapon then
                    shootLogic()
                end
            end
        end
    end)
    
    -- نظام Click Shoot مع الماوس
    Mouse.Button1Down:Connect(function()
        if ClickShootState then
            if isWeaponEquipped and currentWeapon then
                shootLogic()
            end
        end
    end)
    
    -- =============================================
    -- إشعار البدء النهائي
    -- =============================================
    task.wait(3)
    WindUI:Notify({
        Title = "🎯 MVS PRO SYSTEM READY",
        Content = "النظام جاهز للعمل!\nAuto Shoot يعمل عند وجود أهداف",
        Duration = 5
    })
    
    print("=========================================")
    print("✅ MVS PRO SYSTEM LOADED SUCCESSFULLY")
    print("👤 Player:", player.Name)
    print("⚡ Auto Shoot:", AutoShootState and "ENABLED" or "DISABLED")
    print("🎯 Click Shoot:", ClickShootState and "ENABLED" or "DISABLED")
    print("=========================================")
    
else
    print("❌ ERROR: Failed to load WindUI")
end