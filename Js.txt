print("PhantomXPro")

-- الطريقة الصحيحة لتحميل WindUI
local success, result = pcall(function()
    local source = game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua", true)
    if source then
        local loaded = loadstring(source)
        if loaded then
            return loaded()
        end
    end
    return nil
end)

if success and result then
    local WindUI = result
    local Players = game:GetService("Players")
    local player = Players.LocalPlayer
    local RunService = game:GetService("RunService")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Workspace = game:GetService("Workspace")
    local UserInputService = game:GetService("UserInputService")

    -- اختيار اللغة في البداية
    local languageChoice = nil
    WindUI:Popup({
        Title = "PhantomXPro - اختر اللغة / Select Language",
        Icon = "globe",
        Content = "الرجاء اختيار لغة السكربت\nPlease select script language",
        Buttons = {
            {
                Title = "العربية",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "Arabic"
                end
            },
            {
                Title = "English",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    languageChoice = "English"
                end
            }
        }
    })

    -- الانتظار حتى يختار المستخدم اللغة
    repeat task.wait() until languageChoice ~= nil

    -- نصوص حسب اللغة
    local texts = {
        Arabic = {
            welcomeTitle = "PhantomXPro",
            welcomeContent = "⚠️ تنبيه هام:\nتشغيل السكربت يكون على مسؤوليتك الشخصية والكاملة، ونحن غير مسؤولين عن أي ضرر. مع أننا نعمل على تقديم سكربت بأعلى معايير الأمان الممكنة، هذا تحذير احتراصي في حال حدوث أي مشكلة غير متوقعة. نتمنى لكم تجربة ممتعة ❤",
            discordTitle = "🎮 انضم لسيرفر الديسكورد",
            discordContent = "انضم للسيرفر الرسمي للسكربت!\n• آخر التحديثات\n• مساعدة فنية\n• اقتراحات وتحسينات\n• تحديثات أمان\n\nالرابط: https://discord.gg/CgUa36sPNs",
            okButton = "موافق",
            copyButton = "نسخ الرابط وتفعيل السكربت",
            copiedTitle = "✅ تم نسخ الرابط",
            copiedContent = "تم تفعيل السكربت!\nرابط الديسكورد في الحافظة",
            windowTitle = "PhantomXPro",
            settingsTab = "الإعدادات",
            mainTab = "الأدوات الرئيسية",
            developerTab = "المطور",
            mvsTab = "MVS - ألعاب اطلاق النار",
            themeDropdown = "اختر السمة",
            themeValues = {"داكن 🌙", "فاتح ☀️", "أزرق 🔵", "أخضر 💚", "أحمر 🔴"},
            updateButton = "تحديث السكربت",
            updateTitle = "جاري التحديث",
            updateContent = "بيتم إعادة تحميل السكربت...",
            closeButton = "أقفل السكربت",
            closeTitle = "تم الإغلاق",
            closeContent = "تم إغلاق السكربت بنجاح",
            discordButton = "انضم لسيرفر الديسكورد",
            discordCopied = "✅ تم نسخ الرابط",
            discordCopiedContent = "رابط الديسكورد في الحافظة",
            developerName = "Cypher",
            developerDesc = "مطور سكربتات روبلوكس",
            versionButton = "الإصدار 2.1.0",
            contactButton = "تواصل مع المطور",
            contactCopied = "✅ تم النسخ",
            contactContent = "اسم المستخدم: cypherclient",
            finalTitle = "أهلاً وسهلاً في PhantomXPro 🌟",
            finalContent = "السكربت شغّال وجاهز للاستخدام!\nتمتع بمميزاتنا الحصرية 🚀",
            espButtonOn = "ESP - مفعل",
            espButtonOff = "ESP - معطل",
            espTitle = "ESP للمراقبة",
            espContent = "تم تفعيل نظام كشف الأعداء",
            espContentOff = "تم إيقاف نظام كشف الأعداء",
            hitboxButtonOn = "Hitbox - مفعل",
            hitboxButtonOff = "Hitbox - معطل",
            hitboxTitle = "Hitbox للأعداء",
            hitboxContent = "تم تفعيل نظام Hitbox",
            hitboxContentOff = "تم إيقاف نظام Hitbox",
            flyButtonOn = "طيران - مفعل",
            flyButtonOff = "طيران - معطل",
            flyTitle = "نظام الطيران",
            flyContent = "تم تحميل نظام الطيران",
            hitboxSlider = "حجم Hitbox:",
            hitboxSliderDesc = "اضبط حجم الهيتبوكس من 1 إلى 50",
            hitboxSliderNotify = "✅ تم تغيير حجم الهيتبوكس إلى",
            
            -- MVS Features - Arabic
            mvsTitle = "1kzuya MVS System",
            mvsWelcome = "نظام MVS المحمل بنجاح - لألعاب اطلاق النار",
            aimAssistTitle = "🎯 نظام المساعدة في التصويب",
            aimDistanceTitle = "📏 مسافة التصويب",
            aimDistanceDesc = "اضبط مسافة التصويب (1-10000)",
            clickShootTitle = "🖱️ تصويب بالنقر",
            clickShootDesc = "اطلق النار بالنقر على الماوس",
            autoShootTitle = "⚡ تصويب تلقائي",
            autoShootDesc = "اطلق النار تلقائياً على الأعداء",
            teamESPButtonOn = "👥 ESP الفريق - مفعل",
            teamESPButtonOff = "👥 ESP الفريق - معطل",
            teamESPTitle = "ESP لفريق الأعداء",
            teamESPContent = "تم تفعيل ESP لفريق الأعداء",
            teamESPContentOff = "تم إيقاف ESP لفريق الأعداء",
            easyWinLabel = "🎮 فوز سهل - MVS System",
            aimDistanceSet = "✅ تم ضبط المسافة على: ",
            aimDistanceError = "⚠️ أدخل رقم صحيح",
            shootEnabled = "✅ تم تفعيل",
            shootDisabled = "❌ تم إيقاف",
            weaponDetection = "🔫 اكتشاف الأسلحة: ",
            enemyDetection = "🎯 اكتشاف الأعداء: ",
            noWeaponFound = "❌ لم يتم العثور على سلاح",
            weaponFound = "✅ تم اكتشاف سلاح: ",
            shootingActive = "🔫 التصويب نشط",
            shootingInactive = "🔴 التصويب غير نشط"
        },
        English = {
            welcomeTitle = "PhantomXPro",
            welcomeContent = "⚠️ Important Warning:\nUsing the script is at your own personal and full responsibility, and we are not responsible for any damage. While we strive to provide a script with the highest possible security standards, this is a precautionary warning in case any unexpected problem occurs. We wish you a great experience ❤",
            discordTitle = "🎮 Join Discord Server",
            discordContent = "Join the official script server!\n• Latest updates\n• Technical help\n• Suggestions and improvements\n• Security updates\n\nLink: https://discord.gg/CgUa36sPNs",
            okButton = "OK",
            copyButton = "Copy Link & Activate Script",
            copiedTitle = "✅ Link Copied",
            copiedContent = "Script activated!\nDiscord link in clipboard",
            windowTitle = "PhantomXPro",
            settingsTab = "Settings",
            mainTab = "Main Tools",
            developerTab = "Developer",
            mvsTab = "MVS - Shooting Games",
            themeDropdown = "Select Theme",
            themeValues = {"Dark 🌙", "Light ☀️", "Blue 🔵", "Green 💚", "Red 🔴"},
            updateButton = "Update Script",
            updateTitle = "Updating",
            updateContent = "Reloading script...",
            closeButton = "Close Script",
            closeTitle = "Closed",
            closeContent = "Script closed successfully",
            discordButton = "Join Discord Server",
            discordCopied = "✅ Link Copied",
            discordCopiedContent = "Discord link in clipboard",
            developerName = "Cypher",
            developerDesc = "Roblox Script Developer",
            versionButton = "Version 2.1.0",
            contactButton = "Contact Developer",
            contactCopied = "✅ Copied",
            contactContent = "Username: cypherclient",
            finalTitle = "Welcome to PhantomXPro 🌟",
            finalContent = "Script is running and ready to use!\nEnjoy our exclusive features 🚀",
            espButtonOn = "ESP - Enabled",
            espButtonOff = "ESP - Disabled",
            espTitle = "ESP System",
            espContent = "Enemy detection system activated",
            espContentOff = "Enemy detection system deactivated",
            hitboxButtonOn = "Hitbox - Enabled",
            hitboxButtonOff = "Hitbox - Disabled",
            hitboxTitle = "Enemy Hitbox",
            hitboxContent = "Hitbox system activated",
            hitboxContentOff = "Hitbox system deactivated",
            flyButtonOn = "Fly - Enabled",
            flyButtonOff = "Fly - Disabled",
            flyTitle = "Fly System",
            flyContent = "Fly system loaded successfully",
            hitboxSlider = "Hitbox Size:",
            hitboxSliderDesc = "Adjust hitbox size from 1 to 50",
            hitboxSliderNotify = "✅ Hitbox size changed to",
            
            -- MVS Features - English
            mvsTitle = "1kzuya MVS System",
            mvsWelcome = "MVS System Loaded Successfully - For Shooting Games",
            aimAssistTitle = "🎯 Aim Assist System",
            aimDistanceTitle = "📏 Aim Distance",
            aimDistanceDesc = "Set aim distance (1-10000)",
            clickShootTitle = "🖱️ Click Shoot",
            clickShootDesc = "Shoot by clicking mouse",
            autoShootTitle = "⚡ Auto Shoot",
            autoShootDesc = "Automatically shoot at enemies",
            teamESPButtonOn = "👥 Team ESP - Enabled",
            teamESPButtonOff = "👥 Team ESP - Disabled",
            teamESPTitle = "Enemy Team ESP",
            teamESPContent = "Enemy team ESP activated",
            teamESPContentOff = "Enemy team ESP deactivated",
            easyWinLabel = "🎮 Easy Win - MVS System",
            aimDistanceSet = "✅ Distance set to: ",
            aimDistanceError = "⚠️ Enter a valid number",
            shootEnabled = "✅ Enabled",
            shootDisabled = "❌ Disabled",
            weaponDetection = "🔫 Weapon Detection: ",
            enemyDetection = "🎯 Enemy Detection: ",
            noWeaponFound = "❌ No weapon found",
            weaponFound = "✅ Weapon detected: ",
            shootingActive = "🔫 Shooting active",
            shootingInactive = "🔴 Shooting inactive"
        }
    }

    local t = texts[languageChoice]

    WindUI.TransparencyValue = 0.5
    WindUI:SetTheme("Dark")

    -- نافذة البوب أب الترحيبية
    WindUI:Popup({
        Title = t.welcomeTitle,
        Icon = "crown",
        Content = t.welcomeContent,
        Buttons = {
            {
                Title = t.okButton,
                Icon = "arrow-right",
                Variant = "Primary",
                Callback = function() end
            }
        }
    })

    -- نافذة Discord المنبثقة
    task.wait(3)
    WindUI:Popup({
        Title = t.discordTitle,
        Icon = "message-circle",
        Content = t.discordContent,
        Buttons = {
            {
                Title = t.copyButton,
                Icon = "copy",
                Variant = "Primary",
                Callback = function()
                    setclipboard("https://discord.gg/CgUa36sPNs")
                    WindUI:Notify({
                        Title = t.copiedTitle,
                        Content = t.copiedContent,
                        Duration = 4
                    })
                end
            }
        }
    })

    -- إنشاء النافذة الرئيسية
    local Window = WindUI:CreateWindow({
        Title = t.windowTitle,
        Icon = "crown", 
        Author = "By_Cypher",
        Folder = "PhantomXPro",
        Size = UDim2.fromOffset(800, 650),
        Theme = "Dark",
        User = {
            Enabled = true,
            Anonymous = false,
            Callback = function()
                WindUI:Notify({
                    Title = languageChoice == "Arabic" and "معلوماتك" or "Your Info",
                    Content = (languageChoice == "Arabic" and "اسمك: " or "Name: ") .. player.Name,
                    Duration = 3
                })
            end
        },
        SideBarWidth = 250,
        ScrollBarEnabled = true
    })

    -- الأقسام الرئيسية
    local Sections = {
        Main = Window:Section({ Title = t.mainTab, Opened = true }),
        MVS = Window:Section({ Title = t.mvsTab, Opened = false }),
        Settings = Window:Section({ Title = t.settingsTab, Opened = false }),
        Credits = Window:Section({ Title = t.developerTab, Opened = false })
    }

    -- التبويبات
    local Tabs = {
        Main = Sections.Main:Tab({ Title = t.mainTab, Icon = "grid" }),
        MVS_Tools = Sections.MVS:Tab({ Title = t.mvsTab, Icon = "target" }),
        Config = Sections.Settings:Tab({ Title = t.settingsTab, Icon = "settings" }),
        Developer = Sections.Credits:Tab({ Title = t.developerTab, Icon = "user" })
    }

    -- متغيرات الحالة
    local ESPState = false
    local HitboxState = false
    local FlyState = false
    local CurrentHitboxSize = 20
    local ESPScriptLoaded = false
    local HitboxScriptLoaded = false
    local FlyScriptLoaded = false

    -- =============================================
    -- متغيرات نظام MVS (بدون واجهة خاصة)
    -- =============================================
    local MVS = {
        -- الحالات
        ClickShootEnabled = false,
        AutoShootEnabled = false,
        TeamESPEnabled = false,
        AimDistance = 250,
        
        -- المتغيرات الداخلية
        CurrentWeapon = nil,
        IsWeaponEquipped = false,
        LastFireTime = 0,
        FireDelay = 1.5,
        MVSEspHighlights = {},
        BlockClick = false,
        
        -- إشعارات MVS
        Notifications = {
            weaponDetected = function(weaponName)
                WindUI:Notify({
                    Title = t.weaponDetection,
                    Content = t.weaponFound .. weaponName,
                    Duration = 3
                })
            end,
            weaponNotFound = function()
                WindUI:Notify({
                    Title = t.weaponDetection,
                    Content = t.noWeaponFound,
                    Duration = 3
                })
            end,
            shootingState = function(enabled)
                WindUI:Notify({
                    Title = t.shootingActive,
                    Content = enabled and t.shootEnabled or t.shootDisabled,
                    Duration = 3
                })
            end,
            enemyDetected = function(count)
                WindUI:Notify({
                    Title = t.enemyDetection,
                    Content = languageChoice == "Arabic" and "تم اكتشاف " .. count .. " عدو" or count .. " enemies detected",
                    Duration = 3
                })
            end
        }
    }

    -- =============================================
    -- تبويب الأدوات الرئيسية (Main)
    -- =============================================
    do
        -- زر ESP
        local espButtonText = t.espButtonOff
        local espButton = Tabs.Main:Button({
            Title = espButtonText,
            Icon = "eye",
            Size = "xlarge",
            Callback = function()
                ESPState = not ESPState
                
                if ESPState then
                    espButtonText = t.espButtonOn
                    WindUI:Notify({
                        Title = t.espTitle,
                        Content = t.espContent,
                        Duration = 3
                    })
                    
                    -- تحميل سكربت ESP
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/Enemy%20detc.txt", true))()
                    ESPScriptLoaded = true
                    
                    -- تحديث النص في الزر
                    espButton:Destroy()
                    espButton = Tabs.Main:Button({
                        Title = espButtonText,
                        Icon = "eye",
                        Size = "xlarge",
                        Callback = function()
                            ESPState = not ESPState
                            
                            if not ESPState then
                                espButtonText = t.espButtonOff
                                WindUI:Notify({
                                    Title = t.espTitle,
                                    Content = t.espContentOff,
                                    Duration = 3
                                })
                                
                                -- تنظيف ESP
                                for _, obj in pairs(game:GetService("Workspace"):GetChildren()) do
                                    if obj.Name:find("_EnemyChams") or obj.Name:find("_EnemyName") then
                                        obj:Destroy()
                                    end
                                end
                                ESPScriptLoaded = false
                                
                                -- تحديث النص في الزر
                                espButton:Destroy()
                                espButton = Tabs.Main:Button({
                                    Title = espButtonText,
                                    Icon = "eye",
                                    Size = "xlarge",
                                    Callback = function()
                                        ESPState = not ESPState
                                        
                                        if ESPState then
                                            espButtonText = t.espButtonOn
                                            WindUI:Notify({
                                                Title = t.espTitle,
                                                Content = t.espContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحميل سكربت ESP
                                            loadstring(game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/Enemy%20detc.txt", true))()
                                            ESPScriptLoaded = true
                                            
                                            -- تحديث النص في الزر
                                            espButton:Destroy()
                                            espButton = Tabs.Main:Button({
                                                Title = espButtonText,
                                                Icon = "eye",
                                                Size = "xlarge",
                                                Callback = function() end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- زر Hitbox
        local hitboxButtonText = t.hitboxButtonOff
        local hitboxButton = Tabs.Main:Button({
            Title = hitboxButtonText,
            Icon = "target",
            Size = "xlarge",
            Callback = function()
                HitboxState = not HitboxState
                
                if HitboxState then
                    hitboxButtonText = t.hitboxButtonOn
                    WindUI:Notify({
                        Title = t.hitboxTitle,
                        Content = t.hitboxContent,
                        Duration = 3
                    })
                    
                    -- تحميل سكربت Hitbox مع حجم محدد
                    local hitboxScript = game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/hitbox%20.txt", true)
                    hitboxScript = hitboxScript:gsub("local HitboxSize = 20", "local HitboxSize = " .. CurrentHitboxSize)
                    loadstring(hitboxScript)()
                    HitboxScriptLoaded = true
                    
                    -- تحديث النص في الزر
                    hitboxButton:Destroy()
                    hitboxButton = Tabs.Main:Button({
                        Title = hitboxButtonText,
                        Icon = "target",
                        Size = "xlarge",
                        Callback = function()
                            HitboxState = not HitboxState
                            
                            if not HitboxState then
                                hitboxButtonText = t.hitboxButtonOff
                                WindUI:Notify({
                                    Title = t.hitboxTitle,
                                    Content = t.hitboxContentOff,
                                    Duration = 3
                                })
                                
                                -- تنظيف Hitbox
                                local gui = player:FindFirstChild("PlayerGui")
                                if gui then
                                    local hitboxGui = gui:FindFirstChild("HitboxControl")
                                    if hitboxGui then
                                        hitboxGui:Destroy()
                                    end
                                end
                                
                                -- إرجاع حجم HRP الطبيعي لجميع اللاعبين
                                for _, otherPlayer in pairs(Players:GetPlayers()) do
                                    if otherPlayer ~= player and otherPlayer.Character then
                                        local hrp = otherPlayer.Character:FindFirstChild("HumanoidRootPart")
                                        if hrp and hrp:IsA("BasePart") then
                                            hrp.Size = Vector3.new(2, 1, 1)
                                            hrp.Transparency = 0
                                            hrp.CanCollide = true
                                            hrp.Color = Color3.fromRGB(255, 255, 255)
                                        end
                                    end
                                end
                                HitboxScriptLoaded = false
                                
                                -- تحديث النص في الزر
                                hitboxButton:Destroy()
                                hitboxButton = Tabs.Main:Button({
                                    Title = hitboxButtonText,
                                    Icon = "target",
                                    Size = "xlarge",
                                    Callback = function()
                                        HitboxState = not HitboxState
                                        
                                        if HitboxState then
                                            hitboxButtonText = t.hitboxButtonOn
                                            WindUI:Notify({
                                                Title = t.hitboxTitle,
                                                Content = t.hitboxContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحميل سكربت Hitbox مع حجم محدد
                                            local hitboxScript = game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/hitbox%20.txt", true)
                                            hitboxScript = hitboxScript:gsub("local HitboxSize = 20", "local HitboxSize = " .. CurrentHitboxSize)
                                            loadstring(hitboxScript)()
                                            HitboxScriptLoaded = true
                                            
                                            -- تحديث النص في الزر
                                            hitboxButton:Destroy()
                                            hitboxButton = Tabs.Main:Button({
                                                Title = hitboxButtonText,
                                                Icon = "target",
                                                Size = "xlarge",
                                                Callback = function() end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- سلايدر حجم Hitbox
        Tabs.Main:Slider({
            Title = t.hitboxSlider,
            Desc = t.hitboxSliderDesc,
            Value = CurrentHitboxSize,
            Min = 1,
            Max = 50,
            Step = 1,
            Callback = function(value)
                CurrentHitboxSize = value
                WindUI:Notify({
                    Title = t.hitboxSliderNotify,
                    Content = tostring(value),
                    Duration = 3
                })
                
                -- إذا كان Hitbox مفعلاً، نعيد تحميله بالحجم الجديد
                if HitboxState and HitboxScriptLoaded then
                    -- تنظيف الـ GUI القديم
                    local gui = player:FindFirstChild("PlayerGui")
                    if gui then
                        local hitboxGui = gui:FindFirstChild("HitboxControl")
                        if hitboxGui then
                            hitboxGui:Destroy()
                        end
                    end
                    
                    -- تحميل سكربت جديد بالحجم الجديد
                    local hitboxScript = game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/hitbox%20.txt", true)
                    hitboxScript = hitboxScript:gsub("local HitboxSize = 20", "local HitboxSize = " .. CurrentHitboxSize)
                    loadstring(hitboxScript)()
                end
            end
        })

        -- زر الطيران
        local flyButtonText = t.flyButtonOff
        local flyButton = Tabs.Main:Button({
            Title = flyButtonText,
            Icon = "wind",
            Size = "xlarge",
            Callback = function()
                FlyState = not FlyState
                
                if FlyState then
                    flyButtonText = t.flyButtonOn
                    WindUI:Notify({
                        Title = t.flyTitle,
                        Content = t.flyContent,
                        Duration = 3
                    })
                    
                    -- تحميل سكربت الطيران
                    loadstring(game:HttpGet("https://pastebin.com/raw/5WLxDDvL", true))()
                    FlyScriptLoaded = true
                    
                    -- تحديث النص في الزر
                    flyButton:Destroy()
                    flyButton = Tabs.Main:Button({
                        Title = flyButtonText,
                        Icon = "wind",
                        Size = "xlarge",
                        Callback = function()
                            FlyState = not FlyState
                            
                            if not FlyState then
                                flyButtonText = t.flyButtonOff
                                -- إيقاف الطيران
                                local humanoid = player.Character and player.Character:FindFirstChild("Humanoid")
                                if humanoid then
                                    humanoid.PlatformStand = false
                                end
                                FlyScriptLoaded = false
                                
                                -- تحديث النص في الزر
                                flyButton:Destroy()
                                flyButton = Tabs.Main:Button({
                                    Title = flyButtonText,
                                    Icon = "wind",
                                    Size = "xlarge",
                                    Callback = function()
                                        FlyState = not FlyState
                                        
                                        if FlyState then
                                            flyButtonText = t.flyButtonOn
                                            WindUI:Notify({
                                                Title = t.flyTitle,
                                                Content = t.flyContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحميل سكربت الطيران
                                            loadstring(game:HttpGet("https://pastebin.com/raw/5WLxDDvL", true))()
                                            FlyScriptLoaded = true
                                            
                                            -- تحديث النص في الزر
                                            flyButton:Destroy()
                                            flyButton = Tabs.Main:Button({
                                                Title = flyButtonText,
                                                Icon = "wind",
                                                Size = "xlarge",
                                                Callback = function() end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })
    end

    -- =============================================
    -- تبويب MVS (ميزات MVS متكاملة مع WindUI)
    -- =============================================
    do
        -- إشعار الترحيب
        WindUI:Notify({
            Title = t.mvsTitle,
            Content = t.mvsWelcome,
            Duration = 5
        })

        -- علامة "Easy Win"
        Tabs.MVS_Tools:Heading({
            Title = t.easyWinLabel,
            Color = Color3.fromHex("#FFD700")
        })

        -- قسم المساعدة في التصويب
        Tabs.MVS_Tools:Heading({
            Title = t.aimAssistTitle,
            Color = Color3.fromHex("#00FF00")
        })

        -- إدخال مسافة التصويب
        local aimDistanceInput = Tabs.MVS_Tools:Input({
            Title = t.aimDistanceTitle,
            Desc = t.aimDistanceDesc,
            Icon = "maximize-2",
            Placeholder = "250",
            Default = "250",
            ClearTextOnFocus = false,
            Callback = function(text)
                local num = tonumber(text)
                if num then
                    MVS.AimDistance = math.clamp(num, 1, 10000)
                    WindUI:Notify({
                        Title = t.aimDistanceTitle,
                        Content = t.aimDistanceSet .. MVS.AimDistance,
                        Duration = 3
                    })
                else
                    WindUI:Notify({
                        Title = t.aimDistanceTitle,
                        Content = t.aimDistanceError,
                        Duration = 3
                    })
                end
            end
        })

        -- Toggle Click Shoot
        local clickShootToggle = Tabs.MVS_Tools:Toggle({
            Title = t.clickShootTitle,
            Icon = "mouse-pointer",
            Desc = t.clickShootDesc,
            Callback = function(state)
                MVS.ClickShootEnabled = state
                MVS.Notifications.shootingState(state)
                MVS:UpdateBlockState()
            end
        })

        -- Toggle Auto Shoot
        local autoShootToggle = Tabs.MVS_Tools:Toggle({
            Title = t.autoShootTitle,
            Icon = "zap",
            Desc = t.autoShootDesc,
            Callback = function(state)
                MVS.AutoShootEnabled = state
                MVS.Notifications.shootingState(state)
                MVS:UpdateBlockState()
            end
        })

        -- زر Team ESP
        local teamESPButtonText = t.teamESPButtonOff
        local teamESPButton = Tabs.MVS_Tools:Button({
            Title = teamESPButtonText,
            Icon = "users",
            Size = "xlarge",
            Callback = function()
                MVS.TeamESPEnabled = not MVS.TeamESPEnabled
                
                if MVS.TeamESPEnabled then
                    teamESPButtonText = t.teamESPButtonOn
                    WindUI:Notify({
                        Title = t.teamESPTitle,
                        Content = t.teamESPContent,
                        Duration = 3
                    })
                    
                    -- تحديث النص في الزر
                    teamESPButton:Destroy()
                    teamESPButton = Tabs.MVS_Tools:Button({
                        Title = teamESPButtonText,
                        Icon = "users",
                        Size = "xlarge",
                        Callback = function()
                            MVS.TeamESPEnabled = not MVS.TeamESPEnabled
                            
                            if not MVS.TeamESPEnabled then
                                teamESPButtonText = t.teamESPButtonOff
                                WindUI:Notify({
                                    Title = t.teamESPTitle,
                                    Content = t.teamESPContentOff,
                                    Duration = 3
                                })
                                
                                -- تنظيف Team ESP
                                MVS:ClearTeamESP()
                                
                                -- تحديث النص في الزر
                                teamESPButton:Destroy()
                                teamESPButton = Tabs.MVS_Tools:Button({
                                    Title = teamESPButtonText,
                                    Icon = "users",
                                    Size = "xlarge",
                                    Callback = function()
                                        MVS.TeamESPEnabled = not MVS.TeamESPEnabled
                                        
                                        if MVS.TeamESPEnabled then
                                            teamESPButtonText = t.teamESPButtonOn
                                            WindUI:Notify({
                                                Title = t.teamESPTitle,
                                                Content = t.teamESPContent,
                                                Duration = 3
                                            })
                                            
                                            -- تحديث النص في الزر
                                            teamESPButton:Destroy()
                                            teamESPButton = Tabs.MVS_Tools:Button({
                                                Title = teamESPButtonText,
                                                Icon = "users",
                                                Size = "xlarge",
                                                Callback = function() end
                                            })
                                        end
                                    end
                                })
                            end
                        end
                    })
                end
            end
        })

        -- زر تحديث الميزات
        Tabs.MVS_Tools:Button({
            Title = languageChoice == "Arabic" and "🔄 تحديث الميزات" or "🔄 Refresh Features",
            Icon = "refresh-cw",
            Size = "xlarge",
            Callback = function()
                WindUI:Notify({
                    Title = languageChoice == "Arabic" and "🔄 تحديث" or "🔄 Refresh",
                    Content = languageChoice == "Arabic" and "جاري تحديث ميزات MVS..." or "Refreshing MVS features...",
                    Duration = 3
                })
                MVS:InitializeWeaponScript()
            end
        })

        -- معلومات النظام
        Tabs.MVS_Tools:Paragraph({
            Title = languageChoice == "Arabic" and "ℹ️ معلومات النظام" or "ℹ️ System Info",
            Desc = languageChoice == "Arabic" and "نظام MVS مصمم خصيصاً لألعاب اطلاق النار مثل:\n• Arsenal\n• Phantom Forces\n• أي لعبة اطلاق نار أخرى" 
                   or "MVS System designed specifically for shooting games like:\n• Arsenal\n• Phantom Forces\n• Any other shooting game",
            Image = "target",
            ImageSize = 60,
            Color = Color3.fromHex("#00BFFF")
        })
    end

    -- =============================================
    -- دوال نظام MVS
    -- =============================================
    function MVS:InitializeWeaponScript()
        local character = player.Character
        if not character then
            self.CurrentWeapon = nil
            self.IsWeaponEquipped = false
            self.Notifications.weaponNotFound()
            return
        end

        local equippedTool
        for _, child in ipairs(character:GetChildren()) do
            if child:IsA("Tool") and child:FindFirstChild("fire") then
                equippedTool = child
                break
            end
        end

        if equippedTool and self.CurrentWeapon ~= equippedTool then
            self.CurrentWeapon = equippedTool
            self.IsWeaponEquipped = true
            self.Notifications.weaponDetected(equippedTool.Name)
            self:UpdateWeaponScripts()
        elseif not equippedTool and self.IsWeaponEquipped then
            self.CurrentWeapon = nil
            self.IsWeaponEquipped = false
            self.Notifications.weaponNotFound()
        end
    end

    function MVS:UpdateWeaponScripts()
        pcall(function()
            if not self.CurrentWeapon or not self.CurrentWeapon.Parent or not self.CurrentWeapon:IsDescendantOf(game) then
                return
            end

            local scripts = {}
            for _, descendant in ipairs(self.CurrentWeapon:GetDescendants()) do
                if descendant:IsA("Script") or descendant:IsA("LocalScript") then
                    table.insert(scripts, descendant)
                end
            end
            
            -- إضافة النصوص الشائعة
            local commonScripts = {"Client", "GunScript", "GunControl", "WeaponScript"}
            for _, scriptName in ipairs(commonScripts) do
                local script = self.CurrentWeapon:FindFirstChild(scriptName)
                if script then
                    table.insert(scripts, script)
                end
            end

            for _, script in ipairs(scripts) do
                if script and script.Disabled ~= self.BlockClick then
                    script.Disabled = self.BlockClick
                end
            end
        end)
    end

    function MVS:UpdateBlockState()
        if (self.ClickShootEnabled or self.AutoShootEnabled) and self.BlockClick then
            self.BlockClick = false
            self:UpdateWeaponScripts()
        elseif not (self.ClickShootEnabled or self.AutoShootEnabled) and not self.BlockClick then
            self.BlockClick = true
            self:UpdateWeaponScripts()
        end
    end

    -- Backpack listeners
    if player.Backpack then
        player.Backpack.ChildRemoved:Connect(function(child)
            if child:IsA("Tool") and child == MVS.CurrentWeapon then
                MVS.IsWeaponEquipped = false
                task.wait(0.1)
                MVS:InitializeWeaponScript()
            end
        end)
        
        player.Backpack.ChildAdded:Connect(function(child)
            if child:IsA("Tool") and child:FindFirstChild("fire") and MVS.CurrentWeapon and child.Name == MVS.CurrentWeapon.Name then
                MVS.CurrentWeapon = nil
                MVS.IsWeaponEquipped = false
                MVS:UpdateWeaponScripts()
            end
        end)
    end

    -- إدارة الأسلحة بشكل تلقائي
    task.spawn(function()
        while task.wait(0.5) do
            MVS:InitializeWeaponScript()
        end
    end)

    -- =============================================
    -- دوال اكتشاف الفرق واللاعبين
    -- =============================================
    function MVS:GetTeamFrames()
        local success, redFrame, blueFrame = pcall(function()
            local playerGui = player:FindFirstChildOfClass("PlayerGui")
            if not playerGui then return nil, nil end
            local main = playerGui:FindFirstChild("Main")
            if not main then return nil, nil end
            local mainGameFrame = main:FindFirstChild("MainGameFrame")
            if not mainGameFrame then return nil, nil end
            local playersFrame = mainGameFrame:FindFirstChild("PlayersFrame")
            if not playersFrame then return nil, nil end
            return playersFrame:FindFirstChild("TeamRedFrame"), playersFrame:FindFirstChild("TeamBlueFrame")
        end)
        if success then
            return redFrame, blueFrame
        end
        return nil, nil
    end

    function MVS:GetFrameChildrenNames(frame)
        local names = {}
        if not frame then return names end
        for _, child in ipairs(frame:GetChildren()) do
            if child:IsA("Frame") then
                table.insert(names, child.Name)
            end
        end
        return names
    end

    function MVS:SanitizeName(name)
        return tostring(name):gsub("%s+", "")
    end

    function MVS:GetEnemyPlayers()
        local redFrame, blueFrame = self:GetTeamFrames()
        if not redFrame or not blueFrame then return {}, {} end

        local redPlayers = self:GetFrameChildrenNames(redFrame)
        local bluePlayers = self:GetFrameChildrenNames(blueFrame)

        local isLocalPlayerInRed = false
        local isLocalPlayerInBlue = false

        local sanitizedLocalPlayerName = self:SanitizeName(player.Name)

        for _, name in ipairs(redPlayers) do
            if self:SanitizeName(name) == sanitizedLocalPlayerName then
                isLocalPlayerInRed = true
                break
            end
        end

        for _, name in ipairs(bluePlayers) do
            if self:SanitizeName(name) == sanitizedLocalPlayerName then
                isLocalPlayerInBlue = true
                break
            end
        end

        if not isLocalPlayerInRed and not isLocalPlayerInBlue then
            return {}, {}
        end

        local enemyFrame = isLocalPlayerInRed and blueFrame or redFrame
        local enemyNames = self:GetFrameChildrenNames(enemyFrame)

        local enemyPlayerInstances = {}
        for _, name in ipairs(enemyNames) do
            local enemyPlayer = Players:FindFirstChild(name)
            if enemyPlayer and enemyPlayer ~= player then
                table.insert(enemyPlayerInstances, enemyPlayer)
            end
        end

        local enemyTeamColor = isLocalPlayerInRed and Color3.fromRGB(0, 0, 255) or Color3.fromRGB(255, 0, 0)

        return enemyPlayerInstances, enemyTeamColor
    end

    function MVS:GetClosestPlayer()
        local players, _ = self:GetEnemyPlayers()
        if not players or type(players) ~= "table" then return nil end

        local character = player.Character
        if not character or not character:IsDescendantOf(Workspace) then return nil end
        local hrp = character:FindFirstChild("HumanoidRootPart")
        if not hrp then return nil end

        local closestPlayer, minDistance = nil, self.AimDistance or 250
        for _, enemyPlayer in ipairs(players) do
            if enemyPlayer and typeof(enemyPlayer) == "Instance" and enemyPlayer:IsA("Player") then
                local targetCharacter = enemyPlayer.Character
                if targetCharacter and targetCharacter:IsDescendantOf(Workspace) then
                    local targetHrp = targetCharacter:FindFirstChild("HumanoidRootPart")
                    local targetHumanoid = targetCharacter:FindFirstChild("Humanoid")
                    if targetHrp and targetHumanoid and targetHumanoid.Health > 0 then
                        local success, distance = pcall(function() return (hrp.Position - targetHrp.Position).Magnitude end)
                        if success and distance < minDistance then
                            minDistance = distance
                            closestPlayer = enemyPlayer
                        end
                    end
                end
            end
        end
        return closestPlayer
    end

    function MVS:CanSeeTarget(targetPlayer)
        if not targetPlayer then return false end

        local localCharacter = player.Character
        if not localCharacter or not localCharacter:IsDescendantOf(Workspace) then return false end
        local targetCharacter = targetPlayer.Character
        if not targetCharacter or not targetCharacter:IsDescendantOf(Workspace) then return false end

        local localHrp = localCharacter:FindFirstChild("HumanoidRootPart")
        local targetHrp = targetCharacter:FindFirstChild("HumanoidRootPart")
        local targetHumanoid = targetCharacter:FindFirstChild("Humanoid")

        if not localHrp or not targetHrp or not targetHumanoid then return false end

        local success, distance = pcall(function() return (localHrp.Position - targetHrp.Position).Magnitude end)
        if not success or distance > (self.AimDistance or 250) then return false end

        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
        raycastParams.FilterDescendantsInstances = { localCharacter }
        raycastParams.IgnoreWater = true
        raycastParams.CollisionGroup = "Default"

        local direction = targetHrp.Position - localHrp.Position
        local raycastResult = Workspace:Raycast(localHrp.Position, direction.Unit * math.min(distance, self.AimDistance or 250), raycastParams)

        if not raycastResult then return true end
        local hitPart = raycastResult.Instance
        if hitPart and hitPart:IsDescendantOf(targetCharacter) then
            return true
        end

        return false
    end

    -- =============================================
    -- دوال التصويب
    -- =============================================
    function MVS:ShootLogic()
        if tick() - self.LastFireTime < self.FireDelay then return end
        self.LastFireTime = tick()
        if not self.IsWeaponEquipped then return end
        local target = self:GetClosestPlayer()
        if not target then return end
        if not self:CanSeeTarget(target) then return end

        local localCharacter = player.Character
        if not localCharacter or not localCharacter:IsDescendantOf(Workspace) then return end
        local localHrp = localCharacter:FindFirstChild("HumanoidRootPart")
        if not localHrp then return end

        local targetCharacter = target.Character
        if not targetCharacter or not targetCharacter:IsDescendantOf(Workspace) then return end
        local targetHrp = targetCharacter:FindFirstChild("HumanoidRootPart")
        if not targetHrp then return end

        local predictedPosition = targetHrp.Position
        local handle = self.CurrentWeapon:FindFirstChild("Handle") or self.CurrentWeapon
        if not handle then return end

        pcall(function() self.CurrentWeapon.fire:FireServer() end)
        pcall(function()
            local beamRemote = ReplicatedStorage:FindFirstChild("LocalBeam")
            if beamRemote then
                beamRemote:Fire(handle, predictedPosition)
            end
            local showBeamRemote = self.CurrentWeapon:FindFirstChild("showBeam")
            if showBeamRemote then
                showBeamRemote:FireServer(predictedPosition, localHrp.Position, handle)
            end
        end)
        pcall(function()
            local direction = (predictedPosition - localHrp.Position).Unit
            self.CurrentWeapon.kill:FireServer(target, direction, localHrp.Position)
        end)
    end

    -- Auto Shoot System
    task.spawn(function()
        while RunService.Heartbeat:Wait() do
            if MVS.AutoShootEnabled and MVS.IsWeaponEquipped and MVS.CurrentWeapon and not MVS.BlockClick then
                local target = MVS:GetClosestPlayer()
                if target and MVS:CanSeeTarget(target) then
                    MVS:ShootLogic()
                end
            end
        end
    end)

    -- Click Shoot System
    local Mouse = player:GetMouse()
    Mouse.Button1Down:Connect(function()
        if MVS.ClickShootEnabled and MVS.IsWeaponEquipped and MVS.CurrentWeapon and not MVS.BlockClick then
            local target = MVS:GetClosestPlayer()
            if target and MVS:CanSeeTarget(target) then
                MVS:ShootLogic()
            end
        end
    end)

    -- =============================================
    -- نظام Team ESP
    -- =============================================
    function MVS:RemoveTeamHighlight(targetPlayer)
        if self.MVSEspHighlights[targetPlayer] then
            pcall(function() self.MVSEspHighlights[targetPlayer]:Destroy() end)
            self.MVSEspHighlights[targetPlayer] = nil
        end
    end

    function MVS:UpdateTeamHighlight(targetPlayer, color)
        local character = targetPlayer.Character
        if not character then self:RemoveTeamHighlight(targetPlayer) return end

        local highlight = self.MVSEspHighlights[targetPlayer]
        if not highlight or not highlight.Parent then
            if highlight then self:RemoveTeamHighlight(targetPlayer) end
            highlight = Instance.new("Highlight")
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            highlight.FillTransparency = 0.5
            highlight.OutlineTransparency = 0
            highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
            highlight.Parent = character
            self.MVSEspHighlights[targetPlayer] = highlight
        end

        highlight.Adornee = character
        highlight.FillColor = color
    end

    function MVS:UpdateAllTeamHighlights()
        if not self.TeamESPEnabled then
            for targetPlayer, _ in pairs(self.MVSEspHighlights) do 
                self:RemoveTeamHighlight(targetPlayer) 
            end
            return
        end

        local enemyPlayers, enemyColor = self:GetEnemyPlayers()
        if not enemyPlayers then return end

        local currentEnemies = {}
        for _, enemyPlayer in ipairs(enemyPlayers) do
            currentEnemies[enemyPlayer] = true
            self:UpdateTeamHighlight(enemyPlayer, enemyColor)
        end

        for targetPlayer, _ in pairs(self.MVSEspHighlights) do
            if not currentEnemies[targetPlayer] then 
                self:RemoveTeamHighlight(targetPlayer) 
            end
        end
    end

    function MVS:ClearTeamESP()
        for targetPlayer, highlight in pairs(self.MVSEspHighlights) do
            pcall(function() highlight:Destroy() end)
        end
        self.MVSEspHighlights = {}
    end

    -- تحديث Team ESP تلقائياً
    task.spawn(function()
        while RunService.Heartbeat:Wait() do
            if MVS.TeamESPEnabled then
                pcall(function() MVS:UpdateAllTeamHighlights() end)
            end
        end
    end)

    -- Track player characters for Team ESP
    local function trackCharacterForESP(targetPlayer)
        local function onCharAdded(character)
            task.wait(0.1)
            if MVS.TeamESPEnabled then 
                pcall(function() MVS:UpdateAllTeamHighlights() end)
            end
        end
        targetPlayer.CharacterAdded:Connect(onCharAdded)
    end

    for _, otherPlayer in ipairs(Players:GetPlayers()) do
        if otherPlayer ~= player then 
            trackCharacterForESP(otherPlayer) 
        end
    end

    Players.PlayerAdded:Connect(function(newPlayer)
        if newPlayer ~= player then 
            trackCharacterForESP(newPlayer) 
        end
    end)

    player.CharacterAdded:Connect(function()
        task.wait(0.1)
        if MVS.TeamESPEnabled then 
            pcall(function() MVS:UpdateAllTeamHighlights() end)
        end
    end)

    Players.PlayerRemoving:Connect(function(leavingPlayer)
        MVS:RemoveTeamHighlight(leavingPlayer)
    end)

    -- =============================================
    -- تبويب الإعدادات (Config)
    -- =============================================
    do
        Tabs.Config:Dropdown({
            Title = t.themeDropdown,
            Values = t.themeValues,
            Value = t.themeValues[1],
            Size = "xlarge",
            Callback = function(value)
                local themeMap = {
                    [t.themeValues[1]] = "Dark",
                    [t.themeValues[2]] = "Light", 
                    [t.themeValues[3]] = "Sapphire",
                    [t.themeValues[4]] = "Emerald",
                    [t.themeValues[5]] = "Ruby"
                }
                WindUI:SetTheme(themeMap[value] or "Dark")
            end
        })

        -- زر تحديث السكربت
        Tabs.Config:Button({
            Title = t.updateButton,
            Icon = "refresh-cw",
            Size = "xlarge",
            Callback = function()
                WindUI:Notify({
                    Title = t.updateTitle,
                    Content = t.updateContent,
                    Duration = 3
                })
                wait(2)
                loadstring(game:HttpGet("https://raw.githubusercontent.com/AlphaBay00/Test/main/Faq.txt", true))()
            end
        })

        -- زر إغلاق السكربت
        Tabs.Config:Button({
            Title = t.closeButton,
            Icon = "power",
            Size = "xlarge",
            Callback = function()
                -- تنظيف جميع السكربتات
                for _, obj in pairs(game:GetService("Workspace"):GetChildren()) do
                    if obj.Name:find("_EnemyChams") or obj.Name:find("_EnemyName") then
                        obj:Destroy()
                    end
                end
                
                local gui = player:FindFirstChild("PlayerGui")
                if gui then
                    local hitboxGui = gui:FindFirstChild("HitboxControl")
                    if hitboxGui then
                        hitboxGui:Destroy()
                    end
                end
                
                MVS:ClearTeamESP()
                
                for _, otherPlayer in pairs(Players:GetPlayers()) do
                    if otherPlayer ~= player and otherPlayer.Character then
                        local hrp = otherPlayer.Character:FindFirstChild("HumanoidRootPart")
                        if hrp and hrp:IsA("BasePart") then
                            hrp.Size = Vector3.new(2, 1, 1)
                            hrp.Transparency = 0
                            hrp.CanCollide = true
                            hrp.Color = Color3.fromRGB(255, 255, 255)
                        end
                    end
                end
                
                local humanoid = player.Character and player.Character:FindFirstChild("Humanoid")
                if humanoid then
                    humanoid.PlatformStand = false
                end
                
                Window:Destroy()
                WindUI:Notify({
                    Title = t.closeTitle,
                    Content = t.closeContent,
                    Duration = 3
                })
            end
        })

        -- زر دخول الديسكورد
        Tabs.Config:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "xlarge",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
                WindUI:Notify({
                    Title = t.discordCopied,
                    Content = t.discordCopiedContent,
                    Duration = 4
                })
            end
        })
    end

    -- =============================================
    -- تبويب المطور (Credits)
    -- =============================================
    do
        Tabs.Developer:Paragraph({
            Title = t.developerName,
            Desc = t.developerDesc,
            Image = "user",
            ImageSize = 80,
            Color = Color3.fromHex("#FF6B35")
        })

        -- معلومات المطور
        Tabs.Developer:Button({
            Title = t.versionButton,
            Icon = "tag",
            Size = "xlarge",
            Callback = function()
                WindUI:Notify({
                    Title = "PhantomXPro v2.1.0",
                    Content = languageChoice == "Arabic" and "أحدث إصدار من السكربت" or "Latest script version",
                    Duration = 4
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.discordButton,
            Icon = "message-circle",
            Size = "xlarge",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
                WindUI:Notify({
                    Title = t.discordCopied,
                    Content = t.discordCopiedContent,
                    Duration = 4
                })
            end
        })

        Tabs.Developer:Button({
            Title = t.contactButton,
            Icon = "mail",
            Size = "xlarge",
            Callback = function()
                setclipboard("cypherclient")
                WindUI:Notify({
                    Title = t.contactCopied,
                    Content = t.contactContent,
                    Duration = 4
                })
            end
        })
    end

    -- التنظيف عند الإغلاق
    Window:OnDestroy(function()
        MVS:ClearTeamESP()
        WindUI:Notify({
            Title = languageChoice == "Arabic" and "إغلاق السكربت" or "Script Closed",
            Content = languageChoice == "Arabic" and "تم إغلاق PhantomXPro بنجاح" or "PhantomXPro closed successfully",
            Duration = 3
        })
    end)

    -- إشعار الترحيب النهائي
    task.wait(4)
    WindUI:Notify({
        Title = t.finalTitle,
        Content = t.finalContent,
        Duration = 8
    })
else
    print("Failed to load WindUI")
end