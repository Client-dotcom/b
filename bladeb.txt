-- Argon Hub X - النسخة الكاملة مع سكربتات زرير
-- تم تصميمه بالكامل بدون مكتبات خارجية

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local VirtualInputManager = game:GetService("VirtualInputManager")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- ========== [ المتغيرات العامة ] ==========
_G.ArgonSettings = {
    AutoParry = true,
    AutoSpam = true,
    ESP = false,
    AI_Player = false,
    AutoRewards = false,
    AutoWalk = false,
    PlayerSafety = false,
    Visualizer = false,
    BallTrail = false,
    HitSound = true,
    NightMode = false,
    AntiAFK = false,
    AntiLag = false,
    RemoveFog = false,
    AutoSwordCrate = false,
    AutoExplosionCrate = false
}

_G.ArgonValues = {
    ParryDistance = 30,
    SpamPower = 1,
    FollowSpeed = 1,
    Volume = 5,
    FPSLimit = 144,
    AutoWalkDistanceX = 10,
    AutoWalkDistanceZ = 10
}

-- ========== [ نظام الإشعارات ] ==========
local function ShowNotification(title, message, duration)
    duration = duration or 3
    
    local ScreenGui = CoreGui:FindFirstChild("ArgonNotifications") or Instance.new("ScreenGui")
    ScreenGui.Name = "ArgonNotifications"
    ScreenGui.Parent = CoreGui
    ScreenGui.ResetOnSpawn = false
    
    local Notification = Instance.new("Frame")
    Notification.Name = "Notification"
    Notification.Size = UDim2.new(0, 350, 0, 100)
    Notification.Position = UDim2.new(1, 400, 0.8, 0)
    Notification.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    Notification.BorderSizePixel = 0
    Notification.Parent = ScreenGui
    
    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 10)
    UICorner.Parent = Notification
    
    local DropShadow = Instance.new("ImageLabel")
    DropShadow.Name = "DropShadow"
    DropShadow.Size = UDim2.new(1, 12, 1, 12)
    DropShadow.Position = UDim2.new(0, -6, 0, -6)
    DropShadow.BackgroundTransparency = 1
    DropShadow.Image = "rbxassetid://1316045217"
    DropShadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
    DropShadow.ImageTransparency = 0.8
    DropShadow.Parent = Notification
    
    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Size = UDim2.new(1, -20, 0, 30)
    TitleLabel.Position = UDim2.new(0, 10, 0, 10)
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Text = "🔹 " .. title
    TitleLabel.TextColor3 = Color3.fromRGB(0, 200, 255)
    TitleLabel.TextSize = 18
    TitleLabel.Font = Enum.Font.GothamBold
    TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    TitleLabel.Parent = Notification
    
    local MessageLabel = Instance.new("TextLabel")
    MessageLabel.Size = UDim2.new(1, -20, 0, 50)
    MessageLabel.Position = UDim2.new(0, 10, 0, 45)
    MessageLabel.BackgroundTransparency = 1
    MessageLabel.Text = message
    MessageLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
    MessageLabel.TextSize = 14
    MessageLabel.Font = Enum.Font.Gotham
    MessageLabel.TextXAlignment = Enum.TextXAlignment.Left
    MessageLabel.TextYAlignment = Enum.TextYAlignment.Top
    MessageLabel.TextWrapped = true
    MessageLabel.Parent = Notification
    
    -- ظهور متحرك
    TweenService:Create(Notification, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Position = UDim2.new(1, -370, 0.8, 0)
    }):Play()
    
    -- اختفاء بعد المدة
    task.wait(duration)
    
    TweenService:Create(Notification, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
        Position = UDim2.new(1, 400, 0.8, 0)
    }):Play()
    
    task.wait(0.5)
    Notification:Destroy()
end

-- ========== [ سكربتات زرير الأساسية ] ==========
local function LoadZrayerScripts()
    ShowNotification("Argon Hub X", "جاري تحميل سكربتات زرير...", 2)
    
    -- سكربت الأوتو باري الأساسي
    spawn(function()
        while _G.ArgonSettings.AutoParry do
            task.wait(0.1)
            pcall(function()
                local balls = Workspace:FindFirstChild("Balls")
                local character = Players.LocalPlayer.Character
                
                if balls and character and character:FindFirstChild("HumanoidRootPart") then
                    for _, ball in pairs(balls:GetChildren()) do
                        if ball:IsA("Part") and ball:GetAttribute("realBall") then
                            local distance = (character.HumanoidRootPart.Position - ball.Position).Magnitude
                            local target = ball:GetAttribute("target")
                            
                            if target == Players.LocalPlayer.Name and distance < _G.ArgonValues.ParryDistance then
                                -- إرسال ضربة الباري
                                VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
                                task.wait(0.1)
                                VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
                            end
                        end
                    end
                end
            end)
        end
    end)
    
    -- سكربت الأوتو سبام
    spawn(function()
        while _G.ArgonSettings.AutoSpam do
            task.wait(0.2)
            pcall(function()
                if _G.ArgonSettings.AutoParry then
                    -- إرسال ضربات متتالية
                    for i = 1, _G.ArgonValues.SpamPower do
                        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
                        task.wait(0.05)
                        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
                        task.wait(0.05)
                    end
                end
            end)
        end
    end)
    
    -- سكربت الأوتو ووك
    spawn(function()
        while _G.ArgonSettings.AutoWalk do
            task.wait(0.5)
            pcall(function()
                local character = Players.LocalPlayer.Character
                if character and character:FindFirstChild("Humanoid") then
                    local balls = Workspace:FindFirstChild("Balls")
                    if balls then
                        for _, ball in pairs(balls:GetChildren()) do
                            if ball:IsA("Part") then
                                local targetPos = ball.Position + Vector3.new(
                                    _G.ArgonValues.AutoWalkDistanceX,
                                    0,
                                    _G.ArgonValues.AutoWalkDistanceZ
                                )
                                character.Humanoid:MoveTo(targetPos)
                                break
                            end
                        end
                    end
                end
            end)
        end
    end)
    
    -- سكربت الفيزيولايزر
    spawn(function()
        local sphere
        while _G.ArgonSettings.Visualizer do
            task.wait()
            pcall(function()
                local character = Players.LocalPlayer.Character
                if character and character:FindFirstChild("HumanoidRootPart") then
                    if not sphere then
                        sphere = Instance.new("Part")
                        sphere.Shape = Enum.PartType.Ball
                        sphere.Anchored = true
                        sphere.CanCollide = false
                        sphere.Material = Enum.Material.ForceField
                        sphere.Color = Color3.fromRGB(0, 255, 0)
                        sphere.Transparency = 0.7
                        sphere.Size = Vector3.new(15, 15, 15)
                        sphere.Name = "VisualizerSphere"
                        sphere.Parent = Workspace
                    end
                    
                    sphere.Position = character.HumanoidRootPart.Position
                    
                    -- تغيير اللون حسب المسافة من الكرات
                    local balls = Workspace:FindFirstChild("Balls")
                    if balls then
                        local minDistance = math.huge
                        for _, ball in pairs(balls:GetChildren()) do
                            if ball:IsA("Part") then
                                local distance = (character.HumanoidRootPart.Position - ball.Position).Magnitude
                                if distance < minDistance then
                                    minDistance = distance
                                end
                            end
                        end
                        
                        if minDistance < 20 then
                            sphere.Color = Color3.fromRGB(255, 0, 0)
                        elseif minDistance < 50 then
                            sphere.Color = Color3.fromRGB(255, 255, 0)
                        else
                            sphere.Color = Color3.fromRGB(0, 255, 0)
                        end
                    end
                elseif sphere then
                    sphere:Destroy()
                    sphere = nil
                end
            end)
        end
        
        if sphere then
            sphere:Destroy()
        end
    end)
    
    -- سكربت تريل الكرة
    spawn(function()
        while _G.ArgonSettings.BallTrail do
            task.wait(1)
            pcall(function()
                local balls = Workspace:FindFirstChild("Balls")
                if balls then
                    for _, ball in pairs(balls:GetChildren()) do
                        if ball:IsA("Part") and not ball:FindFirstChild("ArgonTrail") then
                            local trail = Instance.new("Trail")
                            trail.Name = "ArgonTrail"
                            trail.Color = ColorSequence.new(Color3.fromRGB(255, 50, 50))
                            trail.Transparency = NumberSequence.new(0.5)
                            trail.Lifetime = 1
                            trail.Parent = ball
                            
                            local att1 = Instance.new("Attachment")
                            att1.Position = Vector3.new(0, 1, 0)
                            att1.Parent = ball
                            
                            local att2 = Instance.new("Attachment")
                            att2.Position = Vector3.new(0, -1, 0)
                            att2.Parent = ball
                            
                            trail.Attachment0 = att1
                            trail.Attachment1 = att2
                        end
                    end
                end
            end)
        end
    end)
    
    -- سكربت أصوات الضربات
    spawn(function()
        local hitSound = Instance.new("Sound")
        hitSound.Volume = _G.ArgonValues.Volume
        hitSound.Parent = Workspace
        
        while true do
            task.wait()
            if _G.ArgonSettings.HitSound then
                -- يمكنك إضافة أصوات هنا
                hitSound.SoundId = "rbxassetid://3570573623" -- صوت افتراضي
            end
        end
    end)
    
    -- سكربت نايت مود
    spawn(function()
        while _G.ArgonSettings.NightMode do
            task.wait(1)
            Lighting.ClockTime = 0
            Lighting.Ambient = Color3.fromRGB(50, 50, 100)
            Lighting.OutdoorAmbient = Color3.fromRGB(50, 50, 100)
        end
        
        -- إعادة الإعدادات عند الإيقاف
        if not _G.ArgonSettings.NightMode then
            Lighting.ClockTime = 14
            Lighting.Ambient = Color3.fromRGB(1, 1, 1)
            Lighting.OutdoorAmbient = Color3.fromRGB(1, 1, 1)
        end
    end)
    
    -- سكربت Anti-AFK
    spawn(function()
        while _G.ArgonSettings.AntiAFK do
            task.wait(30)
            pcall(function()
                local character = Players.LocalPlayer.Character
                if character and character:FindFirstChild("Humanoid") then
                    character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end)
        end
    end)
    
    -- سكربت Anti-Lag
    spawn(function()
        if _G.ArgonSettings.AntiLag then
            for _, obj in pairs(Workspace:GetDescendants()) do
                if obj:IsA("Part") and not obj:FindFirstAncestorWhichIsA("Model") then
                    obj.Material = Enum.Material.SmoothPlastic
                    obj.Reflectance = 0
                elseif obj:IsA("Decal") or obj:IsA("Texture") then
                    obj:Destroy()
                end
            end
        end
    end)
    
    -- سكربت Remove Fog
    spawn(function()
        if _G.ArgonSettings.RemoveFog then
            Lighting.FogEnd = 100000
            Lighting.FogStart = 100000
        end
    end)
    
    -- سكربت شراء الصناديق التلقائي
    spawn(function()
        while _G.ArgonSettings.AutoSwordCrate do
            task.wait(5)
            pcall(function()
                local crate = Workspace:FindFirstChild("Spawn"):FindFirstChild("Crates"):FindFirstChild("NormalSwordCrate")
                if crate then
                    ReplicatedStorage:FindFirstChild("Remote"):FindFirstChild("RemoteFunction"):InvokeServer(
                        "PromptPurchaseCrate", crate
                    )
                end
            end)
        end
    end)
    
    spawn(function()
        while _G.ArgonSettings.AutoExplosionCrate do
            task.wait(5)
            pcall(function()
                local crate = Workspace:FindFirstChild("Spawn"):FindFirstChild("Crates"):FindFirstChild("NormalExplosionCrate")
                if crate then
                    ReplicatedStorage:FindFirstChild("Remote"):FindFirstChild("RemoteFunction"):InvokeServer(
                        "PromptPurchaseCrate", crate
                    )
                end
            end)
        end
    end)
    
    ShowNotification("Argon Hub X", "تم تحميل جميع سكربتات زرير بنجاح! ✅", 3)
end

-- ========== [ إنشاء واجهة Argon ] ==========
local function CreateArgonGUI()
    -- تنظيف أي واجهة قديمة
    local oldUI = CoreGui:FindFirstChild("ArgonHubX_Full")
    if oldUI then oldUI:Destroy() end
    
    -- إنشاء الشاشة الرئيسية
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "ArgonHubX_Full"
    ScreenGui.Parent = CoreGui
    ScreenGui.ResetOnSpawn = false
    
    -- النافذة الرئيسية
    local MainWindow = Instance.new("Frame")
    MainWindow.Name = "MainWindow"
    MainWindow.Size = UDim2.new(0, 800, 0, 600)
    MainWindow.Position = UDim2.new(0.5, -400, 0.5, -300)
    MainWindow.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    MainWindow.BorderSizePixel = 0
    MainWindow.Parent = ScreenGui
    
    -- إضافة ظل
    local Shadow = Instance.new("ImageLabel")
    Shadow.Name = "Shadow"
    Shadow.Size = UDim2.new(1, 20, 1, 20)
    Shadow.Position = UDim2.new(0, -10, 0, -10)
    Shadow.BackgroundTransparency = 1
    Shadow.Image = "rbxassetid://1316045217"
    Shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
    Shadow.ImageTransparency = 0.7
    Shadow.Parent = MainWindow
    
    -- الزوايا المستديرة
    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 12)
    Corner.Parent = MainWindow
    
    -- شريط العنوان
    local TitleBar = Instance.new("Frame")
    TitleBar.Name = "TitleBar"
    TitleBar.Size = UDim2.new(1, 0, 0, 45)
    TitleBar.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    TitleBar.BorderSizePixel = 0
    TitleBar.Parent = MainWindow
    
    local TitleCorner = Instance.new("UICorner")
    TitleCorner.CornerRadius = UDim.new(0, 12, 0, 0)
    TitleCorner.Parent = TitleBar
    
    -- نص العنوان
    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Size = UDim2.new(1, -100, 1, 0)
    TitleLabel.Position = UDim2.new(0, 20, 0, 0)
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Text = "⚔️ Argon Hub X | زرير إصدار"
    TitleLabel.TextColor3 = Color3.fromRGB(0, 200, 255)
    TitleLabel.TextSize = 20
    TitleLabel.Font = Enum.Font.GothamBold
    TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    TitleLabel.Parent = TitleBar
    
    -- أزرار التحكم
    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Name = "CloseBtn"
    CloseBtn.Size = UDim2.new(0, 35, 0, 35)
    CloseBtn.Position = UDim2.new(1, -40, 0, 5)
    CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
    CloseBtn.Text = "✕"
    CloseBtn.TextColor3 = Color3.white
    CloseBtn.TextSize = 18
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.Parent = TitleBar
    
    local MinimizeBtn = Instance.new("TextButton")
    MinimizeBtn.Name = "MinimizeBtn"
    MinimizeBtn.Size = UDim2.new(0, 35, 0, 35)
    MinimizeBtn.Position = UDim2.new(1, -80, 0, 5)
    MinimizeBtn.BackgroundColor3 = Color3.fromRGB(60, 150, 255)
    MinimizeBtn.Text = "─"
    MinimizeBtn.TextColor3 = Color3.white
    MinimizeBtn.TextSize = 18
    MinimizeBtn.Font = Enum.Font.GothamBold
    MinimizeBtn.Parent = TitleBar
    
    -- تبويبات القائمة
    local TabButtonsFrame = Instance.new("Frame")
    TabButtonsFrame.Name = "TabButtonsFrame"
    TabButtonsFrame.Size = UDim2.new(0, 180, 1, -45)
    TabButtonsFrame.Position = UDim2.new(0, 0, 0, 45)
    TabButtonsFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 28)
    TabButtonsFrame.BorderSizePixel = 0
    TabButtonsFrame.Parent = MainWindow
    
    -- محتوى التبويبات
    local ContentFrame = Instance.new("Frame")
    ContentFrame.Name = "ContentFrame"
    ContentFrame.Size = UDim2.new(1, -180, 1, -45)
    ContentFrame.Position = UDim2.new(0, 180, 0, 45)
    ContentFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 25)
    ContentFrame.BorderSizePixel = 0
    ContentFrame.Parent = MainWindow
    
    -- قائمة التبويبات
    local Tabs = {
        {Name = "🏠 الرئيسية", ID = "home"},
        {Name = "⚔️ القتال", ID = "combat"},
        {Name = "🚶 الحركة", ID = "movement"},
        {Name = "✨ المؤثرات", ID = "visual"},
        {Name = "🛒 التسوق", ID = "shop"},
        {Name: "⚙️ الإعدادات", ID = "settings"}
    }
    
    local CurrentTab = "home"
    
    -- وظيفة تبديل التبويب
    local function SwitchTab(tabID)
        CurrentTab = tabID
        
        -- إخفاء كل المحتويات
        for _, child in pairs(ContentFrame:GetChildren()) do
            if child:IsA("Frame") then
                child.Visible = false
            end
        end
        
        -- إظهار المحتوى المطلوب
        local targetContent = ContentFrame:FindFirstChild(tabID .. "_content")
        if targetContent then
            targetContent.Visible = true
        end
    end
    
    -- إنشاء أزرار التبويبات
    for i, tab in ipairs(Tabs) do
        local TabButton = Instance.new("TextButton")
        TabButton.Name = tab.ID .. "_tab"
        TabButton.Size = UDim2.new(1, 0, 0, 50)
        TabButton.Position = UDim2.new(0, 0, 0, (i-1)*50)
        TabButton.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
        TabButton.Text = tab.Name
        TabButton.TextColor3 = Color3.fromRGB(200, 200, 220)
        TabButton.TextSize = 16
        TabButton.Font = Enum.Font.Gotham
        TabButton.Parent = TabButtonsFrame
        
        -- إضافة خط تحت النص
        local Underline = Instance.new("Frame")
        Underline.Name = "Underline"
        Underline.Size = UDim2.new(1, -20, 0, 2)
        Underline.Position = UDim2.new(0, 10, 1, -2)
        Underline.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
        Underline.BorderSizePixel = 0
        Underline.Visible = false
        Underline.Parent = TabButton
        
        TabButton.MouseButton1Click:Connect(function()
            SwitchTab(tab.ID)
            -- إظهار الخط تحت التبويب النشط
            for _, btn in pairs(TabButtonsFrame:GetChildren()) do
                if btn:IsA("TextButton") then
                    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
                    btn.TextColor3 = Color3.fromRGB(200, 200, 220)
                    btn:FindFirstChild("Underline").Visible = false
                end
            end
            
            TabButton.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
            TabButton.TextColor3 = Color3.fromRGB(0, 200, 255)
            Underline.Visible = true
        end)
    end
    
    -- ========== [ محتوى التبويب: الرئيسية ] ==========
    local homeContent = Instance.new("ScrollingFrame")
    homeContent.Name = "home_content"
    homeContent.Size = UDim2.new(1, 0, 1, 0)
    homeContent.BackgroundTransparency = 1
    homeContent.ScrollBarThickness = 4
    homeContent.Parent = ContentFrame
    homeContent.Visible = true
    
    local welcomeLabel = Instance.new("TextLabel")
    welcomeLabel.Size = UDim2.new(1, -40, 0, 100)
    welcomeLabel.Position = UDim2.new(0, 20, 0, 20)
    welcomeLabel.BackgroundTransparency = 1
    welcomeLabel.Text = "مرحباً بك في Argon Hub X! 🚀\n\nإصدار زرير الكامل مع جميع الميزات\n\nمطور بواسطة: AgentX771"
    welcomeLabel.TextColor3 = Color3.fromRGB(0, 200, 255)
    welcomeLabel.TextSize = 22
    welcomeLabel.Font = Enum.Font.GothamBold
    welcomeLabel.TextWrapped = true
    welcomeLabel.Parent = homeContent
    
    -- زر تحميل السكربتات
    local loadScriptsBtn = Instance.new("TextButton")
    loadScriptsBtn.Name = "LoadScriptsBtn"
    loadScriptsBtn.Size = UDim2.new(0.8, 0, 0, 60)
    loadScriptsBtn.Position = UDim2.new(0.1, 0, 0, 150)
    loadScriptsBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    loadScriptsBtn.Text = "🚀 تحميل جميع السكربتات"
    loadScriptsBtn.TextColor3 = Color3.white
    loadScriptsBtn.TextSize = 18
    loadScriptsBtn.Font = Enum.Font.GothamBold
    loadScriptsBtn.Parent = homeContent
    
    -- ========== [ محتوى التبويب: القتال ] ==========
    local combatContent = Instance.new("ScrollingFrame")
    combatContent.Name = "combat_content"
    combatContent.Size = UDim2.new(1, 0, 1, 0)
    combatContent.BackgroundTransparency = 1
    combatContent.ScrollBarThickness = 4
    combatContent.Parent = ContentFrame
    combatContent.Visible = false
    
    -- وظيفة إنشاء عناصر التحكم
    local function CreateToggle(name, yPosition, settingName)
        local toggleFrame = Instance.new("Frame")
        toggleFrame.Name = name .. "ToggleFrame"
        toggleFrame.Size = UDim2.new(1, -40, 0, 50)
        toggleFrame.Position = UDim2.new(0, 20, 0, yPosition)
        toggleFrame.BackgroundTransparency = 1
        toggleFrame.Parent = combatContent
        
        local label = Instance.new("TextLabel")
        label.Name = name .. "Label"
        label.Size = UDim2.new(0.7, 0, 1, 0)
        label.BackgroundTransparency = 1
        label.Text = name
        label.TextColor3 = Color3.fromRGB(220, 220, 220)
        label.TextSize = 16
        label.Font = Enum.Font.Gotham
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Parent = toggleFrame
        
        local toggleBtn = Instance.new("TextButton")
        toggleBtn.Name = name .. "ToggleBtn"
        toggleBtn.Size = UDim2.new(0, 60, 0, 30)
        toggleBtn.Position = UDim2.new(1, -70, 0.5, -15)
        toggleBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
        toggleBtn.Text = ""
        toggleBtn.Parent = toggleFrame
        
        local toggleCircle = Instance.new("Frame")
        toggleCircle.Name = name .. "Circle"
        toggleCircle.Size = UDim2.new(0, 24, 0, 24)
        toggleCircle.Position = UDim2.new(0, 3, 0.5, -12)
        toggleCircle.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
        toggleCircle.Parent = toggleBtn
        
        local circleCorner = Instance.new("UICorner")
        circleCorner.CornerRadius = UDim.new(1, 0)
        circleCorner.Parent = toggleCircle
        
        toggleBtn.MouseButton1Click:Connect(function()
            _G.ArgonSettings[settingName] = not _G.ArgonSettings[settingName]
            
            if _G.ArgonSettings[settingName] then
                TweenService:Create(toggleCircle, TweenInfo.new(0.2), {
                    Position = UDim2.new(0, 33, 0.5, -12),
                    BackgroundColor3 = Color3.fromRGB(60, 255, 60)
                }):Play()
                ShowNotification("تم التفعيل", name .. " مفعل الآن", 2)
            else
                TweenService:Create(toggleCircle, TweenInfo.new(0.2), {
                    Position = UDim2.new(0, 3, 0.5, -12),
                    BackgroundColor3 = Color3.fromRGB(255, 60, 60)
                }):Play()
                ShowNotification("تم الإيقاف", name .. " متوقف الآن", 2)
            end
        end)
        
        return toggleFrame
    end
    
    -- إضافة عناصر القتال
    CreateToggle("الأوتو باري", 20, "AutoParry")
    CreateToggle("الأوتو سبام", 80, "AutoSpam")
    CreateToggle("كشف الإنفينتي", 140, "AutoParry") -- مثال
    
    -- إضافة سلايدر للمسافة
    local sliderFrame = Instance.new("Frame")
    sliderFrame.Name = "ParryDistanceSlider"
    sliderFrame.Size = UDim2.new(1, -40, 0, 80)
    sliderFrame.Position = UDim2.new(0, 20, 0, 200)
    sliderFrame.BackgroundTransparency = 1
    sliderFrame.Parent = combatContent
    
    local sliderLabel = Instance.new("TextLabel")
    sliderLabel.Size = UDim2.new(1, 0, 0, 30)
    sliderLabel.BackgroundTransparency = 1
    sliderLabel.Text = "مسافة الباري: " .. _G.ArgonValues.ParryDistance
    sliderLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
    sliderLabel.TextSize = 16
    sliderLabel.Font = Enum.Font.Gotham
    sliderLabel.TextXAlignment = Enum.TextXAlignment.Left
    sliderLabel.Parent = sliderFrame
    
    local track = Instance.new("Frame")
    track.Size = UDim2.new(1, 0, 0, 8)
    track.Position = UDim2.new(0, 0, 0, 40)
    track.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
    track.Parent = sliderFrame
    
    local thumb = Instance.new("Frame")
    thumb.Size = UDim2.new(0, 20, 0, 20)
    thumb.Position = UDim2.new((_G.ArgonValues.ParryDistance - 10) / 90, -10, 0, 34)
    thumb.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
    thumb.Parent = sliderFrame
    
    local thumbCorner = Instance.new("UICorner")
    thumbCorner.CornerRadius = UDim.new(1, 0)
    thumbCorner.Parent = thumb
    
    -- ========== [ محتوى التبويب: الحركة ] ==========
    local movementContent = Instance.new("ScrollingFrame")
    movementContent.Name = "movement_content"
    movementContent.Size = UDim2.new(1, 0, 1, 0)
    movementContent.BackgroundTransparency = 1
    movementContent.ScrollBarThickness = 4
    movementContent.Parent = ContentFrame
    movementContent.Visible = false
    
    CreateToggle("الأوتو ووك", 20, "AutoWalk")
    CreateToggle("سلامة اللاعب", 80, "PlayerSafety")
    CreateToggle("مضاد AFK", 140, "AntiAFK")
    
    -- ========== [ محتوى التبويب: المؤثرات ] ==========
    local visualContent = Instance.new("ScrollingFrame")
    visualContent.Name = "visual_content"
    visualContent.Size = UDim2.new(1, 0, 1, 0)
    visualContent.BackgroundTransparency = 1
    visualContent.ScrollBarThickness = 4
    visualContent.Parent = ContentFrame
    visualContent.Visible = false
    
    CreateToggle("الفيزيولايزر", 20, "Visualizer")
    CreateToggle("تريل الكرة", 80, "BallTrail")
    CreateToggle("أصوات الضربات", 140, "HitSound")
    CreateToggle("النايت مود", 200, "NightMode")
    CreateToggle("إزالة الضباب", 260, "RemoveFog")
    CreateToggle("مضاد اللاق", 320, "AntiLag")
    
    -- ========== [ محتوى التبويب: التسوق ] ==========
    local shopContent = Instance.new("ScrollingFrame")
    shopContent.Name = "shop_content"
    shopContent.Size = UDim2.new(1, 0, 1, 0)
    shopContent.BackgroundTransparency = 1
    shopContent.ScrollBarThickness = 4
    shopContent.Parent = ContentFrame
    shopContent.Visible = false
    
    CreateToggle("شراء سيوف تلقائي", 20, "AutoSwordCrate")
    CreateToggle("شراء انفجارات تلقائي", 80, "AutoExplosionCrate")
    
    -- زر شراء يدوي
    local buySwordBtn = Instance.new("TextButton")
    buySwordBtn.Size = UDim2.new(0.8, 0, 0, 50)
    buySwordBtn.Position = UDim2.new(0.1, 0, 0, 150)
    buySwordBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
    buySwordBtn.Text = "🛒 شراء صندوق سيوف"
    buySwordBtn.TextColor3 = Color3.white
    buySwordBtn.TextSize = 16
    buySwordBtn.Font = Enum.Font.GothamBold
    buySwordBtn.Parent = shopContent
    
    local buyExplosionBtn = Instance.new("TextButton")
    buyExplosionBtn.Size = UDim2.new(0.8, 0, 0, 50)
    buyExplosionBtn.Position = UDim2.new(0.1, 0, 0, 220)
    buyExplosionBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 100)
    buyExplosionBtn.Text = "💥 شراء صندوق انفجارات"
    buyExplosionBtn.TextColor3 = Color3.white
    buyExplosionBtn.TextSize = 16
    buyExplosionBtn.Font = Enum.Font.GothamBold
    buyExplosionBtn.Parent = shopContent
    
    -- ========== [ محتوى التبويب: الإعدادات ] ==========
    local settingsContent = Instance.new("ScrollingFrame")
    settingsContent.Name = "settings_content"
    settingsContent.Size = UDim2.new(1, 0, 1, 0)
    settingsContent.BackgroundTransparency = 1
    settingsContent.ScrollBarThickness = 4
    settingsContent.Parent = ContentFrame
    settingsContent.Visible = false
    
    -- FPS Slider
    local fpsFrame = Instance.new("Frame")
    fpsFrame.Size = UDim2.new(1, -40, 0, 80)
    fpsFrame.Position = UDim2.new(0, 20, 0, 20)
    fpsFrame.BackgroundTransparency = 1
    fpsFrame.Parent = settingsContent
    
    local fpsLabel = Instance.new("TextLabel")
    fpsLabel.Size = UDim2.new(1, 0, 0, 30)
    fpsLabel.BackgroundTransparency = 1
    fpsLabel.Text = "حد الـ FPS: " .. _G.ArgonValues.FPSLimit
    fpsLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
    fpsLabel.TextSize = 16
    fpsLabel.Font = Enum.Font.Gotham
    fpsLabel.TextXAlignment = Enum.TextXAlignment.Left
    fpsLabel.Parent = fpsFrame
    
    -- Volume Slider
    local volumeFrame = Instance.new("Frame")
    volumeFrame.Size = UDim2.new(1, -40, 0, 80)
    volumeFrame.Position = UDim2.new(0, 20, 0, 120)
    volumeFrame.BackgroundTransparency = 1
    volumeFrame.Parent = settingsContent
    
    local volumeLabel = Instance.new("TextLabel")
    volumeLabel.Size = UDim2.new(1, 0, 0, 30)
    volumeLabel.BackgroundTransparency = 1
    volumeLabel.Text = "الصوت: " .. _G.ArgonValues.Volume
    volumeLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
    volumeLabel.TextSize = 16
    volumeLabel.Font = Enum.Font.Gotham
    volumeLabel.TextXAlignment = Enum.TextXAlignment.Left
    volumeLabel.Parent = volumeFrame
    
    -- زر الخروج
    local exitBtn = Instance.new("TextButton")
    exitBtn.Size = UDim2.new(0.8, 0, 0, 60)
    exitBtn.Position = UDim2.new(0.1, 0, 0, 400)
    exitBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
    exitBtn.Text = "🚪 خروج من Argon Hub X"
    exitBtn.TextColor3 = Color3.white
    exitBtn.TextSize = 18
    exitBtn.Font = Enum.Font.GothamBold
    exitBtn.Parent = settingsContent
    
    -- ========== [ أحداث الأزرار ] ==========
    -- زر تحميل السكربتات
    loadScriptsBtn.MouseButton1Click:Connect(function()
        LoadZrayerScripts()
    end)
    
    -- أزرار التحكم في النافذة
    CloseBtn.MouseButton1Click:Connect(function()
        ScreenGui:Destroy()
        ShowNotification("Argon Hub X", "تم إغلاق الواجهة", 2)
    end)
    
    MinimizeBtn.MouseButton1Click:Connect(function()
        local isVisible = ContentFrame.Visible
        ContentFrame.Visible = not isVisible
        TabButtonsFrame.Visible = not isVisible
        MainWindow.Size = isVisible and UDim2.new(0, 800, 0, 45) or UDim2.new(0, 800, 0, 600)
    end)
    
    -- أزرار التسوق
    buySwordBtn.MouseButton1Click:Connect(function()
        pcall(function()
            local crate = Workspace:FindFirstChild("Spawn"):FindFirstChild("Crates"):FindFirstChild("NormalSwordCrate")
            if crate then
                ReplicatedStorage:FindFirstChild("Remote"):FindFirstChild("RemoteFunction"):InvokeServer(
                    "PromptPurchaseCrate", crate
                )
                ShowNotification("التسوق", "تم شراء صندوق السيوف", 2)
            end
        end)
    end)
    
    buyExplosionBtn.MouseButton1Click:Connect(function()
        pcall(function()
            local crate = Workspace:FindFirstChild("Spawn"):FindFirstChild("Crates"):FindFirstChild("NormalExplosionCrate")
            if crate then
                ReplicatedStorage:FindFirstChild("Remote"):FindFirstChild("RemoteFunction"):InvokeServer(
                    "PromptPurchaseCrate", crate
                )
                ShowNotification("التسوق", "تم شراء صندوق الانفجارات", 2)
            end
        end)
    end)
    
    -- زر الخروج
    exitBtn.MouseButton1Click:Connect(function()
        ScreenGui:Destroy()
        ShowNotification("Argon Hub X", "شكراً لاستخدامك الزرير إصدار!", 3)
    end)
    
    -- ========== [ نظام السحب والإفلات ] ==========
    local dragging
    local dragInput
    local dragStart
    local startPos
    
    local function update(input)
        local delta = input.Position - dragStart
        MainWindow.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
    
    TitleBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = MainWindow.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    
    TitleBar.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            dragInput = input
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input == dragInput then
            update(input)
        end
    end)
    
    -- ========== [ اختصار لوحة المفاتيح ] ==========
    UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if not gameProcessed then
            if input.KeyCode == Enum.KeyCode.RightControl then
                MainWindow.Visible = not MainWindow.Visible
                if MainWindow.Visible then
                    ShowNotification("Argon Hub X", "تم إظهار الواجهة (Ctrl الأيمن)", 2)
                end
            end
        end
    end)
    
    -- تحميل السكربتات تلقائياً
    task.wait(1)
    LoadZrayerScripts()
    
    ShowNotification("Argon Hub X", "تم تحميل الواجهة بنجاح! 🚀", 3)
    
    return ScreenGui
end

-- ========== [ نظام الحماية ] ==========
local function SetupProtection()
    if hookmetamethod and getnamecallmethod then
        local oldNamecall
        oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
            local method = getnamecallmethod()
            if tostring(method):lower() == "kick" then
                ShowNotification("الحماية", "تم منع محاولة طرد!", 2)
                return nil
            end
            return oldNamecall(self, ...)
        end)
        
        ShowNotification("الحماية", "نظام الحماية مفعل ✅", 2)
    end
end

-- ========== [ التشغيل الرئيسي ] ==========
ShowNotification("Argon Hub X", "جاري التحميل...", 1)
task.wait(1)

SetupProtection()
CreateArgonGUI()

print([[
╔══════════════════════════════════════╗
║      Argon Hub X - زرير إصدار       ║
║      تم التحميل بنجاح! 🚀          ║
║      Ctrl الأيمن لإظهار/إخفاء      ║
╚══════════════════════════════════════╝
]])